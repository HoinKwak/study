# 백테스트: COIN-M/USDT-M 평균체결규모(ATS) z-score 괴리 게이트 + 1h EMA20 추세 스캘프 [15m]

- **전략 스펙**: `research/strategies/coinm-usdtm-average-trade-size-divergence-scalp.md`
- **출처**: 자체 설계(정량 백테스트 원문 없음, 스펙 스스로 명시). 이 프로젝트에서 유일하게
  통과선(net PF≥1.3)에 근접했던 ATS 축(`average-trade-size-zscore-dedicated-revalidation-swing`,
  OOS PF 1.337·t=+3.258, 현재 HOLD)을 신규 데이터소스(COIN-M)와 결합한 첫 시도.
- **검증일(UTC)**: 2026-08-20
- **구현**:
  - `research/impl/coinm_ats_download.py` — UM/CM 15m klines 월별 덤프 다운로드(2022-01~2026-07,
    7종목) → 심볼별 pickle 캐시
  - `research/impl/coinm_ats_common.py` — 데이터 병합·ATS z-score·gap·1h EMA20 confirm(룩어헤드
    방지)·크로싱 신호 계산
  - `research/impl/coinm_ats_engine.py` — 이벤트 기반 시뮬레이션 엔진(진입 shift(1)/SL·TP·무효화·
    시간청산/방향반전 지원)
  - `research/impl/coinm_ats_analyze.py` — 진단 CLI(sanity/corr/freq/gate_split/loo/topn/sens/
    reverse/nocost/declust/crosscorr/trendcheck/tautology/shuffle/lookahead/winrate)
  - 세션 스크래치패드 사본(캐시 포함, 워크트리 소멸 시 재현용):
    `/tmp/claude-0/-home-user-study/5c4aa354-51d8-5c5e-afb3-edb007c0a106/scratchpad/coinm_ats/`
    (`cache/*.pkl` 7종목 원본 + `cache/prepared.pkl` 전처리 캐시)

## 재현 커맨드

```
cd research/impl   # 또는 스크래치패드 coinm_ats/ (import 경로상 스크래치패드 쪽이 더 간단)
python3 coinm_ats_download.py BTC ETH BNB SOL XRP DOGE ADA   # 최초 1회, 캐시 있으면 스킵
python3 coinm_ats_analyze.py sanity
python3 coinm_ats_analyze.py corr
python3 coinm_ats_analyze.py freq
python3 coinm_ats_analyze.py gate_split
python3 coinm_ats_analyze.py tautology
python3 coinm_ats_analyze.py trendcheck
python3 coinm_ats_analyze.py winrate
python3 coinm_ats_analyze.py nocost
python3 coinm_ats_analyze.py reverse
python3 coinm_ats_analyze.py declust
python3 coinm_ats_analyze.py crosscorr
python3 coinm_ats_analyze.py loo
python3 coinm_ats_analyze.py topn
python3 coinm_ats_analyze.py sens
python3 coinm_ats_analyze.py lookahead
```

(`.venv` 없는 격리 워크트리라 시스템 `python3` 사용. `import` 경로가 상대 파일명이라
스크래치패드 사본(`common.py`/`engine.py`/`analyze.py`)에서 실행하는 것을 권장 — `research/impl/`
사본은 `coinm_ats_` 접두사가 붙어 있어 그대로 실행하려면 파일명을 맞추거나 심볼릭 링크 필요.)

## 데이터·비용·사이징

- **유니버스**: BTC/ETH/BNB/SOL/XRP/DOGE/ADA(7종목, 전부 `major_bases`=30x) — COIN-M PERP
  (`<SYM>USD_PERP`) 심볼이 전부 존재함을 재확인(스펙 서술과 일치). 실측 결과 SOL이 가장 늦게
  COIN-M 15m 덤프가 시작(2022-01)돼 이것이 7종목 공통 시작월의 제약 요인.
- **기간**: **IS 2022-01-01 00:00:00 ~ 2024-06-30 23:59:59** / **OOS 2024-07-01 00:00:00 ~
  2026-06-30 23:59:59**(23:59:59 명시로 경계 사각지대 차단). 신규 진입은 `entry_time<=OOS_END`로
  엔진에서 원천 차단(`entry_cap_ts`), 청산은 OOS_END 이후(최대 max_hold=16봉=4h)도 데이터가
  있는 2026-07-31까지 커버(보유기간 꼬리 절단 방지). **IS(25,296)+OOS(18,908)=FULL(44,204)
  종목별·풀링 전체 정확히 일치**(§1에서 실측 확인).
- **데이터**: `data.binance.vision` 선물 `um/monthly/klines/<SYM>USDT/15m`(가격·ATR·EMA20 확인TF
  용, `quote_volume`·`count`로 ATS_usdtm 계산) + `cm/monthly/klines/<SYM>USD_PERP/15m`(`volume`
  [계약수]·`count`로 ATS_coinm 계산). **두 소스 모두 15m 그리드의 `open_time`이 정확히 일치**
  (같은 UTC 15분 그리드, 컬럼 헤더도 이미 CSV에 포함돼 있어 `open_time,open,high,low,close,
  volume,close_time,quote_volume,count,taker_buy_volume,taker_buy_quote_volume,ignore` 스키마를
  UM/CM 모두 직접 확인) — `merge_asof`·`Timedelta` 불요, `open_time` 그대로 inner join.
  주문·가격은 전부 USDT-M 위에서 이뤄진다는 스펙 설계(COIN-M은 신호원 전용)를 그대로 따라 CM의
  OHLC는 사용하지 않고 `volume`·`count`만 사용.
- **비용**: 왕복 0.14%(테이커 0.05%×2 + 슬리피지 0.02%×2, `SleeveBacktester` 기본값과 동일).
  신호(15m 봉 t 확정: gap crossing+1h EMA20+방향봉) → **다음 15m봉(t+1) 시가 체결**(`shift(1)`).
  SL/TP는 진입봉(t+1)부터 인트라바 고/저로 체크(SL/TP 동시 도달 시 SL 우선). 무효화(반대 임계
  재돌파)는 확정 다음봉 시가 체결(진입과 동일 관례로 룩어헤드 방지). 시간청산(16봉)은 마지막
  보유봉 종가.
- **사이징**: 계좌 1% 리스크 ÷ SL거리(ATR14×1.2), 레버리지 상한 30x(전 종목 major_bases)로 명목가치
  클램프(실측상 SL거리가 넓어 레버리지 캡에 걸리는 경우는 거의 없음), 복리(거래마다 청산손익을
  다음 사이징에 반영), 종목별 시작자본 $10,000.
- **회계정합**: `max|raw_pnl − fees − pnl| = 0.0`(44,204건 전수).
- **파라미터(base, 스펙 그대로)**: `zscore_window`=90봉, `gap_threshold`=1.2, `atr_mult`=1.2,
  `rr_target`=1.5, `max_hold_bars`=16.

## 0. pandas/시간축 함정 점검

- UM/CM `open_time`(ms epoch int) → `pd.to_datetime(unit="ms")` → `.astype("datetime64[ns]")`로
  명시 통일. `prepare_symbol` 내부에서 `assert df.index.dtype == np.dtype("datetime64[ns]")`을
  UM/CM 원본, 병합 프레임, 1h 리샘플 프레임 전부에 실행해 확인.
- 1h confirm 매핑은 `Timedelta` 덧셈을 `.asi8 + delta_ns` 정수 연산으로만 처리하고(us 업캐스트
  경로 회피), `SleeveBacktester._confirm_slices`와 동일한 `searchsorted(ends, sig_ts, side="right")`
  관례로 "완결된 상위봉까지만" 사용 — 룩어헤드 방지(§10에서 절단 재실행으로 별도 확증).

## 1. Sanity — 표본·회계정합·종목별 결과

```
회계정합 max|raw_pnl-fees-pnl| = 0.0
풀링(7종목): IS n=25,296  OOS n=18,908  FULL n=44,204  IS+OOS==FULL? True
```

| 구간 | n | PF_gross(주1) | PF_net | t_net | 승률 |
|---|---|---|---|---|---|
| IS | 25,296 | 0.896 | 0.660 | −14.85 | 34.9% |
| OOS | 18,908 | 0.890 | 0.659 | −6.34 | 36.0% |
| FULL | 44,204 | 0.896 | 0.660 | −14.83 | 35.4% |

(주1) 이 표의 `PF_gross`는 **슬리피지는 반영하되 수수료는 미반영**한 값이다(체결가에 슬리피지가
이미 녹아있음). 순수 무비용(수수료·슬리피지 모두 0)의 진짜 gross 엣지는 §7 참고 — 표기 혼동을
피하기 위해 본문에서 구분한다.

**종목별(전부 개별적으로 실패)**:

| 종목 | 구간 | n | PF_gross(주1) | PF_net | t_net | 승률 |
|---|---|---|---|---|---|---|
| BTC | IS/OOS | 3,360/2,690 | 0.870/0.841 | 0.570/0.485 | −6.88/−7.43 | 32.2%/32.5% |
| ETH | IS/OOS | 3,069/2,246 | 0.960/0.883 | 0.691/0.588 | −5.01/−7.17 | 35.0%/37.2% |
| BNB | IS/OOS | 3,722/3,028 | 0.870/0.833 | 0.611/0.519 | −6.20/−7.32 | 33.1%/33.6% |
| SOL | IS/OOS | 3,629/2,644 | 0.967/0.892 | 0.760/0.661 | −4.63/−6.07 | 39.0%/38.1% |
| XRP | IS/OOS | 3,895/2,861 | 0.850/0.814 | 0.615/0.563 | −6.31/−6.50 | 34.4%/35.4% |
| DOGE | IS/OOS | 3,911/2,643 | 0.833/0.887 | 0.623/0.666 | −5.91/−6.14 | 35.7%/38.5% |
| ADA | IS/OOS | 3,710/2,796 | 0.863/0.842 | 0.668/0.615 | −5.35/−6.34 | 34.8%/37.2% |

**7종목 전부, IS·OOS 양쪽 모두 gross PF부터 1 미만**이다 — 수수료 이전부터 무엣지에 가깝고,
net PF는 0.48~0.76 구간에 밀집, t는 전부 −4.6~−7.7로 강하게 유의하게 나쁘다.

⚠️ **계좌소진 아티팩트 경고**: 종목별 net_pnl이 IS·FULL 구간에서 거의 정확히 `−$10,000.00`
(시작자본과 동일)로 나타난다 — 1% 리스크 복리 사이징에서 연속 손실로 계좌가 거의 완전히
소진된 결과다. 이 경우 후반부 거래는 포지션 크기가 거의 0으로 수렴해 달러 PnL에 왜곡을 줄 수
있으므로, **주 지표는 등가중 R-배수로 별도 재계산**했다(아래).

### R-배수(등가중) 재계산 — 계좌소진 아티팩트 배제

```
IS   n=25,296  R_mean=−0.3239  t=−48.46  PF[R]=0.517
OOS  n=18,908  R_mean=−0.3142  t=−40.48  PF[R]=0.532
FULL n=44,204  R_mean=−0.3197  t=−63.14  PF[R]=0.524
```

달러 PF보다 **R-배수 PF가 오히려 더 나쁘다**(0.52 vs 0.66) — 계좌소진이 결과를 유리하게
왜곡한 것이 아니라 오히려 후반 소액 거래들의 악영향을 희석시켜 달러 PF를 다소 완화해 보이게
했을 뿐이다. **등가중 기준으로도 결론은 불변(오히려 강화)**된다. 이하 진단은 R-배수와 달러
PF를 병기한다.

> **[리뷰어 정정]** "이하 진단은 R-배수와 달러 PF를 병기한다"는 §4(게이트 이분)에서만 지켜졌고
> **§10(LOO)·§11(top-N)·§8(de-clustering)은 실제로는 달러 PF만 썼다**(병기 안 됨). 리뷰어가
> 직접 계좌 궤적을 추적한 결과 BTC 계좌자본이 **IS 구간 중 트레이드#2166(2166/6050)에서 이미
> $1 밑으로, #2731(2023-12-10, 여전히 IS)에서 $0.01 밑으로, 백테스트 종료 시점엔 $5.96e-10로**
> 사실상 완전히 소진돼 있었다 — 즉 **OOS BTC 트레이드 2,690건 전부가 이미 거의 0으로 수렴한
> 계좌 위에서 계산된 것**이라 달러 단위로는 사실상 무의미하다(BTC OOS 트레이드 평균 pnl은
> −1.8×10⁻⁷달러 수준). 다른 종목도 정도만 다를 뿐 유사(§10·§11 정정 참고).

## 2. 신호빈도 (스펙 예상 대비)

```
BTC : long=3,822 short=3,770 합계=7,592  → 1,658.5건/년
ETH : long=3,520 short=3,136 합계=6,656  → 1,454.0건/년
BNB : long=4,490 short=4,038 합계=8,528  → 1,862.9건/년
SOL : long=3,954 short=4,093 합계=8,047  → 1,757.9건/년
XRP : long=4,280 short=4,468 합계=8,748  → 1,911.0건/년
DOGE: long=4,037 short=4,399 합계=8,436  → 1,842.9건/년
ADA : long=3,893 short=4,399 합계=8,292  → 1,811.4건/년
```

스펙 예상(BTC·ETH 기준 종목당 연 200~500건) 대비 **실측은 종목당 연 1,450~1,900건으로 약
3~9배 높다**. 과거 여러 라운드에서 반복된 "예상의 수십~수백배" 급 정의결함 수준은 아니지만,
스펙이 기대한 "두 z-score 크로싱의 상쇄 효과로 빈도가 크게 줄어들 것"이라는 가정은 실측으로
지지되지 않는다 — 15m z-score(90봉=22.5h 윈도우)의 자연 변동성이 스펙 예상보다 훨씬 높다.

## 3. 상관·전제 점검 — 사전 폐기조건 (a)(b)(d)

### 3-1. corr(z_ats_coinm, z_ats_usdtm) — 폐기조건(a): ≥0.7 이면 FAIL

`pandas.corr()`(pairwise) 사용:

```
BTC 0.441 | ETH 0.499 | BNB 0.309 | SOL 0.339 | XRP 0.308 | DOGE 0.320 | ADA 0.319
pooled: 0.362
```

**전부 0.7 미만 — 폐기조건(a) 미트리거.** COIN-M ATS와 USDT-M ATS는 동일 정보의 재포장이
아니다(스펙의 설계 전제는 이 부분에서 지지됨).

### 3-2. 강추세 구간 자기잠식 — 폐기조건(b): 강추세에서 crossing 발생률이 유의하게 낮으면 FAIL

1h EMA20 4시간 슬로프 절대값 상위 20% 구간 vs 나머지 80% 구간의 gap crossing(롱+숏) 발생률:

```
BTC  강추세=0.0358 나머지=0.0504(비율 0.71) | ETH 0.0346/0.0434(0.80) | BNB 0.0422/0.0561(0.75)
SOL  0.0384/0.0534(0.72) | XRP 0.0421/0.0580(0.73) | DOGE 0.0411/0.0556(0.74) | ADA 0.0414/0.0544(0.76)
평균: 강추세=0.0394 나머지=0.0530
풀링 2비율 z-검정: z = −26.44 (n=1,120,896봉, 압도적으로 유의)
```

**7종목 전부 일관되게 강추세 구간에서 crossing 발생률이 약 25~29% 낮다 — 폐기조건(b) 명확히
트리거.** 스펙이 스스로 우려한 "가장 신호가 필요한 강추세 구간에서 게이트가 오히려 덜
발화한다"는 자기잠식 가설이 실측으로 확인된다.

### 3-3. COIN-M 15m count 결측/0 비율 — 폐기조건(d): >30% 면 해당 종목 제외

```
BTC 0.0175% | ETH 0.0181% | BNB 0.0181% | SOL 0.0187% | XRP 0.0187% | DOGE 0.0187% | ADA 0.0187%
```

**전 종목 0.02% 미만 — 폐기조건(d) 미트리거.** 스펙이 우려한 "알트 COIN-M count 희소성"은
15m 해상도에서도 실제로는 문제가 되지 않는다(7종목 전부 데이터 품질 우수).

### 3-4. 실측 승률 vs 손익분기 승률 — 폐기조건(c): 유의하게 낮으면 FAIL

```
평균 ATR%(전종목, 15m) = 0.5423%  (스펙 가정 0.30%보다 실측이 더 큼)
1R = ATR_mult(1.2) × ATR% = 0.6508%
cost_R = 0.14% / 0.6508% = 0.2151
재계산 손익분기 승률 wr* = (1+cost_R)/(1+RR) = (1.2151)/(2.5) = 48.61%
(스펙 원안 가정 손익분기 승률: 약 55.6%)

실측 승률: FULL 35.37% | OOS 36.00%
```

**실측 승률(35~36%)이 재계산 손익분기(48.6%)보다도 12~13%p 낮고, 스펙 원안(55.6%)과는
19~20%p 격차 — 폐기조건(c) 명확히 트리거.** n=44,204로 이 격차의 유의성은 압도적이다(뒤에서
t=−63로도 재확인).

**요약: 4개 사전 폐기조건 중 (b)·(c) 2개가 명확히 트리거됐다.** 스펙은 "이 중 하나라도 걸리면
FAIL"이라고 명시했으므로, 이미 이 시점에 폐기 대상이다. 사후에 이 임계값들을 다른 기준으로
바꾸지 않고 그대로 적용한다.

## 4. ⭐ 게이트 부가가치 — 동일 트리거 풀 내 gate=True/False 이분(R-배수)

리뷰어 지침에 따라 대조군 풀에 채택안이 포함되는 문제(base ⊆ pool)를 피하기 위해, **게이트 없는
"정렬 진입"(1h EMA20 위/아래 + 방향봉, 상태전이) 트리거 풀 전체**를 만들고, 그중 **같은 봉에
gap crossing도 동시에 발생했는지(gate=True)** 여부로 이분해 동일 SL/TP/시간청산 규칙으로
비교했다(같은 시뮬레이션 경로, 같은 청산 로직 — 진입 조건의 엄격도만 다름):

```
gate=True(채택 조건 충족)   FULL n=14,928  R_mean=−0.3173 t=−36.30 PF[R]=0.527 | OOS n=6,263  R_mean=−0.3086 t=−22.76 PF[R]=0.540
gate=False(게이트 없었으면) FULL n=159,635 R_mean=−0.2960 t=−116.07 PF[R]=0.528 | OOS n=71,029 R_mean=−0.2869 t=−74.17 PF[R]=0.545
```

**gate=True와 gate=False의 R_mean·PF[R]이 사실상 동일하다**(OOS PF[R] 0.540 vs 0.545, 차이는
오차범위 내). ATS gap crossing이라는 추가 조건은 **아무런 부가가치를 만들지 못한다** — 게이트를
걸든 안 걸든 결과가 통계적으로 구분되지 않으며, 둘 다 똑같이 크게 실패한다.

## 5. 동어반복(tautology) 점검

`gap`이 기존 지표(거래량 z, ROC, 체결건수 z)의 재포장인지 `pandas.corr()`로 확인:

```
BTC : gap~vol_z=−0.242 gap~roc=−0.004 gap~cnt_z=−0.179
ETH : gap~vol_z=−0.216 gap~roc=−0.014 gap~cnt_z=−0.147
BNB : gap~vol_z=−0.354 gap~roc=−0.017 gap~cnt_z=−0.245
SOL : gap~vol_z=−0.260 gap~roc=−0.038 gap~cnt_z=−0.186
XRP : gap~vol_z=−0.314 gap~roc=−0.031 gap~cnt_z=−0.215
DOGE: gap~vol_z=−0.321 gap~roc=−0.038 gap~cnt_z=−0.224
ADA : gap~vol_z=−0.322 gap~roc=−0.046 gap~cnt_z=−0.218
```

**모든 상관이 |r|≤0.35 — 기존 지표의 단순 재포장은 아니다**(신규성 자체는 있다는 근거).
다만 "신규 정보"라는 것이 곧 "유용한 정보"를 의미하지 않는다는 것은 아래 §6·§4에서 이미
반증됐다.

### 정보원 무력화 대조군 — COIN-M `volume`/`count` 블록셔플(30일 블록, 20회)

각 종목의 COIN-M `volume`·`count` 시계열을 30일(2,880봉) 블록 단위로 무작위 재배열해
`ats_coinm`을 재계산 → `gap` 재계산 → 신호·엔진 재실행(20회):

```
base(원본) OOS PF_net = 0.659 (n=18,908)
셔플 20회 OOS PF_net 범위: 0.628~0.740, 원본은 셔플 분포의 45번째 백분위(정확히 중앙)
```

**원본과 셔플 데이터가 통계적으로 구분되지 않는다** — COIN-M `volume`/`count`의 정확한 시간
정렬을 파괴해도 결과가 달라지지 않는다. 이는 §4(게이트 부가가치 없음)와 §7(무비용 진단에서도
gross 엣지 없음)을 세 번째 독립 경로로 재확인하는 것이다: **COIN-M ATS 신호는 실제 정보를
담고 있지 않다.**

⚠️ **한계(정직히 기록)**: 셔플 시 블록 경계(약 59개 지점)에서 90봉 롤링 윈도우가 서로 다른
블록의 데이터를 걸치며 일시적 미스매치를 만들어, 셔플군 신호빈도가 원본 대비 약 30~40% 더
많다(BTC 예시: 원본 7,592건 vs 셔플 예시 10,739건). 이 빈도 차이 때문에 PF 비교의 정밀도는
제한적이나(§상기 지시사항이 우려한 "빈도 불일치는 R-배수로" 원칙에 따라 첨언), 결과 분포가
워낙 넓게 겹치고(45번째 백분위) §4·§7의 독립적 증거와 방향이 일치해 정성적 결론(정보 없음)은
견고하다.

⚠️ **설계 결함 발견·기록**: 애초 계획했던 "일별 손익을 30일 블록으로 셔플"하는 방식은
**설계 결함**임을 스스로 발견했다 — 이미 실현된 일별 손익 합계를 블록 단위로 재배열해도
전체 부호·합계 구성(어느 날이 양수/음수인지)은 그대로 보존되므로 PF가 항상 원본과 동일하게
나오는 **트리비얼한(무정보) 검정**이었다. 대신 위와 같이 **신호를 만드는 원천 데이터(COIN-M
volume/count)**를 셔플하는 방식으로 교체해 재실행했다 — 이 프로젝트의 다른 라운드에서
관찰된 "신호원 셔플"과 동일한 계열의 올바른 설계다.

> **[리뷰어 정정 — 재현성 결함]** `coinm_ats_analyze.py`의 `shuffle` 명령(재현 커맨드 목록에는
> 애초 빠져 있음)이 실제로 저장·커밋된 코드는 위에서 "설계 결함이라 폐기했다"고 서술한 바로 그
> **일별 손익(R-배수) 30일 블록 셔플**이다(`_daily_return_blocks` + `cmd_shuffle`, 100회 루프).
> 리뷰어가 직접 이 저장된 코드를 실행해보니 서술 그대로 **모든 셔플의 PF가 base와 소수점까지
> 완전히 동일**함을 확인했다(순열은 합계·부호구성을 보존하므로 자명). 즉 **§5에서 인용한
> "45번째 백분위, 범위 0.628~0.740" 수치를 만들어낸 COIN-M volume/count 원천셔플 코드는
> 저장소 어디에도 남아있지 않다** — 재현 커맨드로는 재현 불가능한 임시(ad-hoc) 실행 결과다.
> 리뷰어가 report의 서술을 근거로 **`prepare_symbol` 로직을 그대로 따라 cm의 volume/count만
> 30일(2,880봉) 블록으로 재배열**하는 독립 스크립트(`verify_shuffle.py`)를 새로 작성해
> 32회(seed 0~31) 재실행한 결과:
> ```
> base(재구현) OOS n=18,908 PF_net=0.6590   (원 리포트 base 0.659와 일치)
> 32회 범위: 0.5726~0.7068,  base 백분위=59.4%
> 신호빈도: 셔플군 n≈25,600~26,200 (원본 대비 +35~38%, 원 리포트의 "30~40%"와 일치)
> ```
> **결론은 원 리포트와 방향이 같다**(base가 셔플 분포 중앙 부근 — 정보 없음 확인) — 다만 이는
> 리뷰어가 서술만 보고 **독립적으로 재구현·재확인**한 결과이지, 저장된 코드를 실행해 나온
> 결과가 아니다. 향후 재현 요청 시 이 결과가 다시 나오지 않으면 코드 소실이 원인일 수 있다는
> 점을 기록해 둔다(과거 여러 라운드에서 지적된 "`research/impl`이 `.gitignore` 대상이라 재현
> 불가" 구조적 취약점과 같은 계열의 문제).

## 6. ⭐ 무비용 진단(fee=0, slippage=0) — gross 엣지 자체의 존부

```
IS   n=25,190  PF=0.986  t=−0.89(비유의)
OOS  n=18,812  PF=1.002  t=+0.10(비유의)
FULL n=44,002  PF=0.993  t=−0.52(비유의)
```

**수수료·슬리피지를 완전히 제거해도 PF는 정확히 1.0 근방이고 t는 비유의하다** — 이는 "작은
gross 엣지가 비용에 잠식됐다"는 이야기가 아니라, **원신호 자체가 애초에 동전 던지기(coin-flip)
수준의 기대값**이라는 뜻이다. §1의 net PF 0.48~0.76이라는 큰 실패폭은 전부 비용(수수료+슬리피지)
때문이며, 그 비용이 이토록 크게 작용하는 이유는 §2에서 확인한 **비정상적으로 높은 신호빈도**
(연 1,450~1,900건/종목) 때문이다 — 무정보 신호를 고빈도로 거래하면 비용만 누적된다는 전형적
패턴이다.

## 7. 방향반전 대조군

SL/TP·무효화·시간청산 전 분기가 `direction`(반전 여부까지 반영해 확정된 최종 방향 변수)만
참조하도록 구현(원신호 크로싱 플래그는 무효화 판정에서 방향에 따라 재매핑 — 코드로 확인,
`inval_col_for_dir = inval_long if direction==LONG else inval_short`).

```
정방향: FULL n=44,204 PF_net=0.660 t=−14.83
반전  : FULL n=44,773 PF_net=0.602 t=−17.35
zero_hold_frac: 정방향=11.58% 반전=12.92%
```

**반전도 정방향과 비슷하거나 더 나쁘다**(둘 다 크게 실패) — 방향 자체에 정보가 없다는 §6의
결론과 일치한다. `zero_hold_frac`이 정방향·반전 모두 11~13%로 비슷한 수준(0%나 90%+로
퇴화하지 않음)이라 반전 로직 버그(과거 여러 라운드에서 반복된 "원신호 플래그 재참조" 안티패턴)
의 징후는 없다. ⚠️ 다만 이 지표가 "0이 나오면 버그없음"을 자동 보장하는 것은 아니라는 과거
지적에 따라, 코드를 직접 읽어 무효화 판정이 `direction`(최종 확정 방향)만 참조함을 별도
확인했다(위 근거).

## 8. de-clustering(일/주 단위, OOS, net)

```
일단위: 고유일수=729 t=−5.58
주단위: 고유주수=105 t=−5.03
```

트레이드 단위 t(−6.34)와 부호·크기가 일관되게 유지된다 — **de-clustering 후에도 반전되지
않음**(과거 여러 라운드에서 발견된 의사반복(pseudo-replication) 인공물이 아니라 진짜 실패).

## 9. 종목 간 R/손익 상관(OOS)

```
일별 손익 상관(off-diag 평균): 0.242
진입일 이진 상관(off-diag 평균): 0.054
```

과거 매크로 클러스터링 실패 사례(상관 0.7~0.9대)보다 뚜렷이 낮다 — 아래 LOO가 착시 표본이
아니라는 근거가 된다.

## 10. LOO(종목 1개씩 제외, OOS, 달러 PF)

```
BTC 제외: n=16,218 PF_net=0.659 t=−6.34
ETH 제외: n=16,662 PF_net=0.660 t=−6.28
BNB 제외: n=15,880 PF_net=0.659 t=−6.34
SOL 제외: n=16,264 PF_net=0.622 t=−11.12
XRP 제외: n=16,047 PF_net=0.659 t=−6.32
DOGE 제외: n=16,265 PF_net=0.659 t=−6.26
ADA 제외: n=16,112 PF_net=0.660 t=−6.20
```

~~**7가지 제외 시나리오 전부 PF 0.62~0.66, t −6.2~−11.1로 실패** — 단일 종목 의존이 아니라
구조적·전종목 공통 실패다.~~

> **[리뷰어 정정]** 이 표는 **달러 PF**(§1에서 "R-배수와 병기"하겠다고 했으나 실제로는 미이행)라
> §1에서 이미 경고한 계좌소진 아티팩트에 그대로 노출돼 있다. `SOL 제외`만 유독 t=−11.12로 튀는
> 이유를 리뷰어가 직접 추적한 결과, **종목별 OOS 트레이드 평균 pnl 스케일이 BTC 1.8×10⁻⁷ /
> BNB 4.9×10⁻⁷ / ETH 6.3×10⁻⁵ / XRP 2.0×10⁻⁵ / ADA 1.2×10⁻⁴ / DOGE 7.4×10⁻⁵ / **SOL 5.4×10⁻³**로
> 4~5자릿수 차이가 나 사실상 SOL만 계좌가 덜 소진된 상태 — 풀링된 달러 통계가 SOL 한 종목에
> 지배당한다(SOL 제외 시 이 지배력이 사라져 t가 급변한 것이지, "SOL이 특별한 정보를 담고
> 있다/없다"는 의미가 아니다). **리뷰어가 R-배수로 LOO를 재계산**하면(스케일 정규화):
> ```
> BTC 제외: n=16,218 PF[R]=0.5645 t=−34.00 | ETH 제외: n=16,662 PF[R]=0.5261 t=−38.63
> BNB 제외: n=15,880 PF[R]=0.5515 t=−35.05 | SOL 제외: n=16,264 PF[R]=0.5187 t=−39.07
> XRP 제외: n=16,047 PF[R]=0.5332 t=−37.13 | DOGE 제외: n=16,265 PF[R]=0.5158 t=−39.42
> ADA 제외: n=16,112 PF[R]=0.5172 t=−39.05
> ```
> **7가지 전부 PF[R] 0.52~0.56, t −34~−39로 훨씬 더 균질하다** — "구조적·전종목 공통 실패"라는
> 결론 자체는 R-배수 기준으로 보면 오히려 원래 리포트보다 더 견고하게 확인되지만, 원 리포트가
> 제시한 근거(달러 PF 표)는 그 결론을 뒷받침하기에 부적절한 지표였다.

## 11. top-N 승리거래 제거(OOS)

```
top-0: n=18,908 PF_net=0.659 | top-1: 0.655 | top-2: 0.650 | top-3: 0.646
top-5: n=18,903 PF_net=0.637 | top-10: 0.617 | top-20: 0.578
```

~~승리거래를 제거할수록 PF가 계속 나빠진다(정상적인 단조 패턴) — 소수 대형승리에 의존하는
구조가 아니라 애초에 승리 자체가 구조적으로 희박하다.~~

> **[리뷰어 정정]** 이 top-N도 §10과 같은 결함(달러 PF 풀링) — 리뷰어가 직접 top-20 트레이드를
> 대조해보니 **20건 전부 SOL, 그것도 전부 2024-07-01~19(OOS 시작 직후 3주)에 몰려 있다**(다른
> 6종목은 이미 §1에서 정정한 대로 그 시점 계좌가 사실상 소진된 상태). "소수 대형승리 의존이
> 아니다"는 서술은 **검증된 적이 없다** — 달러 기준 top-20 제거는 사실상 "SOL 초반 3주치만
> 제거"와 같아서, 이 진단은 애초에 "여러 종목·시기에 걸친 소수 대형승리 의존 여부"를 판별할
> 수 있는 설계가 아니었다. **리뷰어가 R-배수 기준으로 재계산**하면:
> ```
> top-0: PF[R]=0.5321 | top-1: 0.5320 | top-2: 0.5318 | top-3: 0.5317
> top-5: 0.5315 | top-10: 0.5309 | top-20: 0.5298
> ```
> top-20(R기준) 심볼분포는 XRP 6·DOGE 6·ADA 4·BNB 3·SOL 1로 **여러 종목에 고르게 분산**돼 있고,
> PF[R]는 0.5321→0.5298로 **거의 변하지 않는다**(달러 버전 0.659→0.578보다 훨씬 완만) — 결과적
> 결론("소수 대형승리 의존 아님")은 R-배수로 다시 확인해도 유지되지만, **원 리포트가 제시한
> 근거(달러 top-N)는 우연히 방향이 맞았을 뿐 실제로는 그 결론을 입증하지 못하는 진단이었다**.

## 12. 파라미터 민감도(10변형)

| 변형 | IS n | IS PF | OOS n | OOS PF | OOS t |
|---|---|---|---|---|---|
| base(1.2/1.2/1.5/16/90) | 25,296 | 0.660 | 18,908 | 0.659 | −6.34 |
| atr_mult=1.0 | 26,533 | 0.640 | 19,801 | 0.659 | −5.71 |
| atr_mult=2.0 | 22,425 | 0.696 | 16,792 | 0.715 | −8.16 |
| rr=1.2 | 25,840 | 0.645 | 19,276 | 0.681 | −6.10 |
| rr=2.5 | 24,304 | 0.695 | 18,153 | 0.666 | −5.84 |
| max_hold=8 | 26,145 | 0.638 | 19,507 | 0.618 | −7.00 |
| max_hold=24 | 25,118 | 0.664 | 18,776 | 0.669 | −6.32 |
| gap_th=0.8 | 35,201 | 0.638 | 27,683 | 0.599 | −6.97 |
| gap_th=2.0 | 10,677 | 0.634 | 7,245 | 0.665 | −9.78 |
| zscore_window=60 | 26,419 | 0.676 | 19,574 | 0.623 | −6.76 |
| zscore_window=180 | 24,149 | 0.661 | 18,117 | 0.671 | −6.22 |

**10개 변형 전부 OOS net PF 0.60~0.72, t −5.7~−9.8 범위에 갇혀 있다** — 스윕 범위 내에
통과선(1.3)에 근접하는 파라미터 섬조차 없다. `gap_threshold`(핵심 파라미터)를 0.8~2.0으로
크게 흔들어도 결과가 거의 변하지 않는다는 것은, 문제가 임계값 튜닝으로 해결될 수준이 아니라
**신호 정의 자체에 구조적 문제**가 있음을 시사한다(§6·§4와 일치).

## 13. 룩어헤드 절단 재실행

```
BTC cut=30,000봉: 비교 n=30,000  z_usdtm/z_coinm/gap/ema20_1h/atr14 max|diff|=0.0 (전부)
ETH cut=50,000봉: 비교 n=50,000  위와 동일, 전부 0.0
ADA cut=20,000봉: 비교 n=20,000  위와 동일, 전부 0.0
```

**3종목 절단 재실행 전부 5개 지표 컬럼이 소수점 완전 일치(불일치 0건)** — 절단 이후 데이터를
참조하지 않음을 확인. 1h EMA20 confirm 매핑(`searchsorted` 관례)도 이 재현에 자연히 포함되어
함께 검증됐다.

## 14. 종합 판정: **FAIL**

**사전 폐기조건 적용(스펙 §사전 폐기조건, 사후 변경 없이 그대로 적용, OR 조건)**:

| 폐기조건 | 임계값 | 실측값 | 충족 여부 |
|---|---|---|---|
| (a) corr(z_coinm,z_usdtm) | ≥0.7 | 0.31~0.50(pooled 0.362) | 미충족 |
| (b) 강추세 구간 gap crossing 자기잠식 | 유의하게 낮음 | 강추세 0.0394 vs 나머지 0.0530(z=−26.44) | **충족** |
| (c) 실측승률 vs 손익분기 | 유의하게 낮음 | 35.4%(FULL) vs 48.6%(재계산)/55.6%(원안), n=44,204 | **충족** |
| (d) COIN-M 15m count 결측/0 | >30% | 전 종목 0.02% 미만 | 미충족 |

**4개 중 2개(폐기조건 b·c)가 명확히·통계적으로 압도적인 크기로 트리거됐다.** 스펙은 "이 중
하나라도 걸리면 FAIL로 폐기한다"고 명시했다.

**표본**: FULL n=44,204(OOS n=18,908) — 검증 임계(약 20건)를 압도적으로 초과, 판단불가 사유
없음.

**부가 근거(사전조건 외에도 결론을 강하게 뒷받침)**:
1. **7종목 전부 개별적으로 gross PF<1**(수수료 이전부터 무엣지) — 예외 없음.
2. **무비용(fee=0·slippage=0) 진단에서 PF≈1.0(t 비유의)** — 원신호 자체가 동전 던지기 수준.
3. **게이트 부가가치 없음**: 동일 트리거 풀 내 gate=True/False 이분 R-배수가 사실상 동일
   (OOS PF[R] 0.540 vs 0.545) — ATS gap crossing 조건이 없는 신호와 구분되는 가치를 만들지
   못한다.
4. **COIN-M 데이터 블록셔플(20회)에서도 원본이 45번째 백분위** — COIN-M `volume`/`count`의
   특정 시간 정렬이 실제 정보를 담고 있다는 근거가 없다.
5. **de-clustering(일/주)·LOO(7가지)·top-N 제거·파라미터 스윕(10변형) 전부 예외 없이 실패**
   — 우연·소수표본·특정 파라미터 의존·클러스터링 인공물 어느 것으로도 설명되지 않는 구조적
   실패.
6. **방향반전 대조군도 유사하게 실패**(정방향보다 나은 방향 없음) — 방향 자체에 정보 없음.
7. **회계정합 완전 일치, 룩어헤드 없음(3종목 절단), 단위(ns) 통일 확인** — 데이터/구현 결함으로
   인한 착시가 아니다.
8. **계좌소진 아티팩트를 R-배수 재계산으로 배제**했고, 오히려 R-배수 기준(PF 0.52)이 달러
   기준(PF 0.66)보다 더 나쁘다 — 결과를 유리하게 왜곡한 방향이 아니다.

**신규 데이터소스(COIN-M)를 결합했다는 이번 시도의 차별점**은 데이터 가용성 측면에서는
성공적이었다(48종 COIN-M PERP 풀 확인, 7종목 전부 15m 데이터 품질 우수, count 결측 사실상
0%). 다만 **"COIN-M 참여자 구성 차이가 정보 우위를 만든다"는 핵심 가설 자체가 이 조합
(15m ATS z-score gap crossing + 1h EMA20 + 방향봉)에서는 지지되지 않는다** — 무비용 진단·
게이트 이분·블록셔플 세 가지 독립 방법이 모두 "정보 없음"이라는 같은 결론에 수렴한다.

**라이브에 반영하지 않는다.**

## 15. 백테스트 불가/근사 요소

- COIN-M은 스펙 설계대로 신호원(참고 지표) 전용으로만 사용, 실제 체결·가격은 전부 USDT-M
  klines 기준 — 오더북/틱/실시간청산 불필요, 근사 요소 없음.
- TWAP 분할 진입 없음(15m 신호봉 확정 → 다음봉 시가 일괄 체결, 슬리피지로 근사 — 다른
  스캘프/스윙 리포트와 동일 관례).
- 라이브 실행 시 COIN-M 데이터를 실시간으로 추가 폴링해야 한다는 스펙의 명시 사항 그대로이며,
  이번 백테스트는 과거 정적 덤프만 사용해 COIN-M 라이브 가용성(지역차단·레이트리밋) 자체는
  검증하지 않았다. 다만 판정이 FAIL이라 이 문제는 실무적으로 무의미해졌다.

## backtest-reviewer 검증: **VALID**

**최종 판정: 원 리포트의 FAIL 결론에 동의한다(VALID).** 다만 §1·§10·§11의 "R-배수와 달러 PF를
병기한다"는 약속이 실제로는 §4에서만 지켜졌고, §10(LOO)·§11(top-N)·§8(de-clustering)은 달러 PF만
사용해 계좌소진 아티팩트에 그대로 노출돼 있었다(본문에 [리뷰어 정정]으로 표시). **R-배수로
재계산해도 FAIL 결론은 뒤집히지 않고 오히려 더 강하게 확인된다**(LOO 전 종목 PF[R] 0.52~0.56·
t −34~−39, top-N 제거해도 PF[R] 0.532→0.530로 거의 불변).

### 체크리스트

| 항목 | 판정 | 근거 |
|---|---|---|
| 1. 정합(parity) | N/A | 신규 전략(라이브 미반영 전제, COIN-M은 신호원 전용·주문은 USDT-M) — 비교 대상 라이브 로직 없음. 1h confirm 매핑이 `SleeveBacktester._confirm_slices`와 동일한 `searchsorted(ends, sig_ts, side="right")` 관례를 씀은 코드로 확인(PASS 준함). |
| 2. 단위(ns/ms/us) | PASS | `_to_ns_index`에서 `unit="ms"`→`datetime64[ns]` 명시 통일, `assert dtype==ns` 전 프레임에 적용. 리뷰어가 직접 `pd.__version__`(3.0.5)·인덱스 dtype 재조회로 확인. 1h confirm은 `Timedelta.value`(ns) 정수 덧셈만 사용해 us 업캐스트 경로 회피 — 코드 직접 대조. |
| 3. 룩어헤드 | PASS | 리포트의 3종목 절단 재실행(컬럼 max\|diff\|=0.0)과 별개로, 리뷰어가 **독립적으로** BTC 신호 1건(2022-02-07 11:00)의 1h EMA20 confirm값을 수기 재계산(완결된 1h봉만 사용)해 엔진 산출값과 완전 일치 확인, 진입가=다음봉 시가도 확인. |
| 4. 표본크기·유의성 | PASS | FULL n=44,204(OOS 18,908), 종목당 OOS 2,246~3,028건 — 압도적으로 충분. |
| 5. OOS 분리 | PASS | IS/OOS 정확히 일치(`len(IS)+len(OOS)==len(FULL)` 리뷰어 재실행 재확인), `IS_END`=23:59:59.999999999로 경계 사각지대 없음, `entry_cap_ts=OOS_END`로 신규진입 상한 엔진에서 강제. FAIL이 IS·OOS 양쪽에서 일관(엣지 소멸이 아니라 애초에 없었음). |
| 6. 유니버스/선택 편중 | PASS | 7종목 전부 개별 gross PF<1(예외 없음), LOO 전종목(원 지표든 리뷰어의 R-배수 재계산이든) 결과 불변. |
| 7. 수수료·슬리피지 | PASS(단, 표기 정밀도 CONCERN) | net(수수료+슬리피지) 기준 결론이며, 무비용 진단(PF≈1.00)으로 원신호 자체가 무정보임을 별도 확인. 다만 리포트의 "PF_gross"(슬리피지 반영·수수료 미반영)는 fee가 compounding 사이징에 미치는 2차 효과 때문에 "무비용 대비 슬리피지만의 순수 효과"가 아니다 — 리뷰어가 fee/slippage를 개별로 끈 4가지 조합(전체비용/슬리피지만/수수료만/무비용)을 전부 재실행해 이 사실을 확인(무비용 IS 0.9864/OOS 1.0023은 리포트와 소수점까지 일치, 슬리피지만은 IS 0.857/OOS 0.880). 결론에 영향 없음(모든 조합에서 net PF는 1 미만이거나 무비용은 비유의). |
| 8. 워밍업·지표 정확성 | PASS | zscore_window=90봉 min_periods=90, ATR14 ewm min_periods=14, EMA20(1h) n<20 시 신호 배제 — 코드 확인. |
| 9. 데이터 소스 | PASS | UM/CM 컬럼 스키마 실물 확인. **리뷰어가 COIN-M 계약 액면가를 직접 실측**(`(quote_volume×close)/volume`)해 BTC=$100.00~100.004, ETH/BNB/SOL/XRP/DOGE/ADA=$10.000~10.001로 **2022~2026 전 구간 5개년 전부 완전히 고정**됨을 확인 — 스펙이 우려한 "COIN-M volume은 계약수라 USDT-M과 단위가 다르다"는 리스크는, 고정 배율이 z-score 계산상 스케일 불변이라 **실질적으로 무해함**을 정량 검증(선결 우려 해소, 구현 무효 아님). |
| 10. 재현성 | CONCERN | 대부분의 진단(sanity/corr/freq/gate_split/loo/topn/reverse/nocost/declust/crosscorr/trendcheck/tautology/lookahead/winrate)은 리뷰어가 스크래치패드 캐시로 전부 재실행해 **모든 수치가 소수점까지 일치**함을 확인. 다만 **2건의 재현성 결함**을 발견: ① **§5의 "COIN-M volume/count 원천셔플, 45번째 백분위" 수치는 저장된 코드로 재현 불가**(저장된 `cmd_shuffle`은 폐기했다고 서술한 트리비얼한 일별 R-배수 블록셔플 그대로임을 리뷰어가 실행해 확인 — 모든 셔플이 base와 완전 동일한 PF를 냄). ② **§12 파라미터 스윕의 `gap_th=0.8/2.0`·`zscore_window=60/180` 4행도 저장된 `cmd_sens()`가 명시적으로 건너뛰는 변형**(`if "gap_th" in ov: continue`, `if name.startswith("zwin"): continue`)이라 재현 커맨드로는 안 나온다. 두 건 모두 **리뷰어가 서술을 바탕으로 별도 스크립트를 새로 작성해 독립 재구현**했고, ①은 32회 재실행으로 결론(45~59번째 백분위, 정보 없음) 재확인, ②는 4개 변형 전부 리포트 수치와 **완전 일치**(gap=0.8: IS 0.638/OOS 0.599 t=-6.974 등, 전부 소수점까지 일치)를 확인해 **수치 자체는 진짜**임을 검증했다. 다만 **재현 커맨드만으로는 이 두 결과를 재생산할 수 없다**는 점은 기록해 둔다(같은 패턴이 반복 — ad-hoc 실행 결과를 코드로 영속화하지 않는 습관). |
| 11. 외부 주장 | N/A | 백테스트 근거 없음(자체 설계, 스펙이 정직히 명시). 바이낸스 블로그 인용은 정성적 설명일 뿐 정량 주장이 없어 재현 대상 아님. |

### 치명 결함
없음. §10·§11의 재현성/표기 결함은 FAIL 판정 자체를 무효화하지 않는다 — R-배수로 재계산한
결과가 오히려 더 뚜렷하게 실패를 보여준다(위 표 참고).

### 직접 재확인한 것 (재현 커맨드 15개 중 14개 + 독립 검증 5건)
- `sanity/corr/freq/gate_split/loo/topn/sens/reverse/nocost/declust/crosscorr/trendcheck/tautology/winrate` 전부 재실행 → 리포트 수치와 완전 일치(`lookahead`는 별도 방식으로 독립 재확인).
- COIN-M 계약 액면가 5개년 고정 확인(z-score 스케일 불변성 검증).
- BTC 신호 1건 EMA20(1h) confirm 값 수기 재계산 일치(룩어헤드 없음 추가 확인).
- fee/slippage 4조합 분리 재실행으로 PF_gross(0.89)와 무비용(1.00) 간극의 성격 규명(슬리피지
  실효과 + fee의 compounding 2차효과 혼재, 결론 불변).
- §5 셔플: 저장코드가 트리비얼함을 실증(모든 셔플 PF=base와 완전 동일), 독립 재구현 32회로
  원 결론 재확인.
- §12 파라미터 스윕 중 저장 코드가 건너뛰는 `gap_th`·`zscore_window` 4변형을 독립 재구현해
  전부 리포트 수치와 완전 일치 확인(수치 진위 검증, 재현성 결함은 별도 기록).
- §10 LOO·§11 top-N을 R-배수로 재계산 — 결론 불변(오히려 강화), 단 원래 제시된 달러 기준 표는
  근거로서 결함이 있었음(top-20 전부가 SOL 2024-07 3주치라는 사실도 리뷰어가 직접 확인).

**한계**: `lookahead`(3종목 절단, 리포트 방식 그대로의 전체 재실행)는 리뷰어가 별도 방식
(수기 EMA20 재계산 1건)으로 대체 검증했고 리포트가 쓴 절단 스크립트 자체를 그대로 돌려보진
않았다(코드 검토로 로직은 확인). 라이브 COIN-M 데이터 가용성(지역차단 등)은 리포트와 마찬가지로
검증 범위 밖(판정 FAIL이라 실무적 영향 없음).
