# 백테스트: 1-2-3 리버설 패턴 (추세 소진 3피벗 구조 브레이크)

- **전략 스펙**: `research/strategies/123-reversal-pattern-pivot-breakout-swing.md`
- **원전 근거**: 없음(정량 근거 미확인, 스펙 단계에서 "75% 승률" 주장 미인용 확정) — 순수
  자체 백테스트로만 판정.
- **구현**: 자체 완결형 스크립트, **ZigZag 유틸·채널 프리컴퓨트를 피치포크 전략과 완전 공유**
  (`precompute_channels()` 가 반환하는 P0/P1/P2 배열을 이 스펙의 P1/P2/P3 로 그대로 리매핑해 사용).
  - `/tmp/claude-0/-home-user-study/5c4aa354-51d8-5c5e-afb3-edb007c0a106/scratchpad/pitchfork/bt_123.py`
  - `/tmp/claude-0/-home-user-study/5c4aa354-51d8-5c5e-afb3-edb007c0a106/scratchpad/pitchfork/analyze_123.py`,
    `sweep_123.py`
  - (zigzag.py·bt_pitchfork.py·download.py 는 피치포크 리포트와 공용, 데이터도 동일 pkl 재사용)
- **판정: 탈락 (FAIL)** — **gross PF 부터 FULL/IS/OOS 전 구간 <1**(0.86/0.87/0.86), net 은
  더 나쁨(0.81/0.81/0.80). t=-2.4~-3.6 으로 통계적으로 유의하게 손실. 파라미터 스윕 12변형
  전부 OOS PF<0.87. 방향반전 대조군도 <1(0.82~0.87) — 방향 자체에 정보 없음. 표본은 충분
  (OOS n=747)하므로 "판단불가"가 아니라 **명확한 FAIL**.

## 방법 / 재현성

- **데이터/기간/수수료/사이징/OOS_END**: 피치포크 리포트와 완전 동일(같은 pkl 파일 재사용) —
  4h, BTC/ETH/BNB/SOL/XRP/DOGE/ADA(전부 major_bases, 30x), 2022-01~2026-06, 왕복 0.14%,
  RiskManager 1% 리스크 사이징, shift(1) 다음 봉 시가 체결.
- **ZigZag 룩어헤드 방지**: 피치포크와 동일한 `zigzag.py` 사용(피벗은 confirmed_at 이후만 사용).
  셋업(P1/P2/P3 트리플 + `P3>P1`[불리시]/`P3<P1`[베어리시] + `p3_proximity` 필터)이 성립한
  구간에서 **종가가 P2 를 최초로 돌파하는 봉만** 신호로 채택(같은 트리플에서 중복 신호 방지),
  체결은 신호봉 다음 봉 시가.
- **청산**: 목표(measured-move: `entry ± (P2-P1)` 폭 투영, 고정 레벨) / 손절(`P3 저가∓buf×ATR`,
  진입 시점 ATR 고정, 상시 ATR×2.5 트레일링으로 단조 강화[`max(sl_initial, run_ext-trail×ATR)`,
  피치포크 이탈형과 동일 패턴]) / **P1 재이탈 시 즉시 손절(스탑 전이라도, 스펙 명시대로 구현)** /
  `max_hold`=20봉.
- **재현 커맨드**:
  ```
  cd .../scratchpad/pitchfork
  python3 bt_123.py                 # 단일 스팟체크
  python3 analyze_123.py            # IS/OOS·LOO·top-N·반전대조군·클러스터링·필터영향
  python3 sweep_123.py              # 파라미터 스윕
  ```

## 결과 — 기본 파라미터(zigzag=4%, p3_proximity=0.5, atr_buf=0.3, trail=2.5, max_hold=20)

| 구간 | n | PF_gross | PF_net | R_net_mean | t(R) | 승률 | 순손익 |
|---|---:|---:|---:|---:|---:|---:|---:|
| FULL | 1,719 | 0.865 | 0.809 | -0.061 | -3.59 | 41.1% | -10,003 |
| IS | 972 | 0.871 | 0.813 | -0.059 | -2.67 | 41.3% | -5,585 |
| OOS | 747 | 0.859 | 0.803 | -0.065 | -2.40 | 40.8% | -4,418 |

**회계 정합**: `raw_pnl_accum(-10003.43) - fees == pnl(-10003.43)` 일치 확인. **gross PF 가
FULL/IS/OOS 모두 <1** — 수수료 이전부터 무엣지, 방향에 통계적으로 유의한(음의) 편향까지 있음.

### 종목별 OOS

| 종목 | n | PF_gross | PF_net | t(R) | 승률 |
|---|---:|---:|---:|---:|---:|
| BTC | 59 | 0.362 | 0.325 | **-3.64** | 27.1% |
| ETH | 85 | 0.854 | 0.793 | -0.89 | 42.4% |
| BNB | 79 | 1.144 | 1.049 | 0.19 | 43.0% |
| SOL | 126 | 0.782 | 0.734 | -1.46 | 42.1% |
| XRP | 117 | 1.004 | 0.941 | -0.25 | 39.3% |
| DOGE | 141 | 0.851 | 0.807 | -0.90 | 39.7% |
| ADA | 140 | 0.879 | 0.824 | -0.99 | 45.7% |

**7종목 중 5종목 PF<1**(BNB·XRP 만 근접 손익분기, 둘 다 t 비유의). BTC 는 특히 심각한 손실
(PF 0.32, t=-3.64) — 단일 종목 우연이라 보기엔 통계적으로 강한 신호이나 방향은 손실 쪽.

## LOO / top-N 제거 (OOS, net PF)

- **LOO**: 어느 종목을 빼도 OOS 풀 PF 는 **0.777~0.839 범위**(전부 <1) — 특정 종목 의존이 아니라
  **구조적으로 전 종목에서 무너짐**.
- **top-N 제거**: top-1 제거 0.775 → top-10 제거 **0.707** — 상위 승리 트레이드를 빼면 오히려
  더 악화(당연히, 애초에 net<1이므로 승리를 빼면 남는 손실 비중이 커짐).

## 반전 대조군 (SL/TP 진입가 기준 대칭 재배치 + 청산조건 방향 반전)

| 구간 | n | PF_gross | PF_net | t(R) | **zero_hold_frac** |
|---|---:|---:|---:|---:|---:|
| FULL | 1,701 | 0.878 | 0.823 | -3.38 | 5.2% |
| OOS | 740 | 0.927 | 0.867 | -1.62 | 4.7% |

`zero_hold_frac` 4.7~5.2%로 낮아 청산조건 버그(반전거래 즉시청산) **재현 없음** — 구현 건전.
반전 방향도 <1(정방향과 거의 대등하게 나쁨, OOS net 0.803 vs 0.867) — **방향 자체에 신뢰할 만한
정보가 없고, 트리거(3피벗 추세소진 구조) 자체가 방향 무관하게 무정보**임을 시사.

## 파라미터 민감도 (OOS 풀링)

| 변형 | IS_n | IS_PF | OOS_n | OOS_PF | OOS_t |
|---|---:|---:|---:|---:|---:|
| base(zz=4%,prox=0.5,buf=0.3,hold=20) | 972 | 0.813 | 747 | 0.803 | -2.40 |
| zigzag=2.5% | 1,466 | 0.809 | 1,205 | 0.814 | -3.59 |
| zigzag=3% | 1,227 | 0.821 | 1,070 | 0.863 | -2.08 |
| zigzag=5% | 760 | 0.694 | 568 | 0.808 | -1.85 |
| zigzag=6% | 599 | 0.630 | 443 | 0.789 | -1.92 |
| p3_proximity=0.3(엄격) | 586 | 0.728 | 465 | 0.846 | -1.63 |
| p3_proximity=0.7(완화) | 1,099 | 0.782 | 907 | 0.821 | -2.41 |
| p3_proximity=1.0(무필터) | 1,112 | 0.770 | 916 | 0.822 | -2.42 |
| atr_stop_buffer=0.2 | 973 | 0.827 | 748 | 0.802 | -2.40 |
| atr_stop_buffer=0.5 | 970 | 0.803 | 746 | 0.803 | -2.40 |
| max_hold=15 | 994 | 0.778 | 763 | 0.832 | -1.95 |
| max_hold=30 | 947 | 0.825 | 733 | 0.819 | -2.17 |

**12개 변형 전부 OOS PF 0.79~0.86 범위에 평탄하게 갇혀 있고 전부 <1**(1을 넘는 변형이 단 하나도
없음). `p3_proximity` 를 완전히 꺼도(1.0=무필터) 결과가 거의 변하지 않아(0.822, base 0.803과
유사) — 필터가 결과를 좌우하는 게 아니라 **트리거 자체(3피벗 추세소진+P2 돌파)가 구조적으로
무정보**임을 강하게 시사.

## 전제의 내적 일관성 (p3_proximity 필터 결합 과잉 여부)

스펙이 사전 경고한 "결합조건이 독립기대치의 1/500만 발생" 함정을 점검: 무필터(prox≤1.0) 대비
필터(prox≤0.5) 적용 시 표본 감소율은 BTC 24.2%·ETH 21.6%·BNB 26.9% — **약 1/4 감소로 그침**
(과거 라운드에서 적발된 1/60~1/500 수준의 병리적 붕괴는 없음). 필터는 설계 의도대로 온건하게
작동하며, 이 전략의 실패 원인은 필터 과잉이 아니라 **트리거 자체의 무정보성**(위 스윕 표에서
무필터도 결과 거의 불변)에 있다.

## 예상 표본 빈도 대조

종목당 연 **54.7건**(풀링 연 382.6건) — 스카우트 예상(종목당 연 15~35건, 풀링 100~250건)의
**약 1.5~3.8배**. `p3_proximity` 필터가 과잉 작동하지 않았고(위 항목), ZigZag 4% 임계가
4h 크립토에서 스카우트 예상보다 잦은 피벗(피치포크 리포트와 동일한 패턴 — ZigZag 피벗
빈도 자체가 사전 추정보다 3~10배 많음)을 만들어낸 결과.

## 룩어헤드 감사

BTC 데이터 마지막 50봉 절단 후 재계산, 절단 경계 30봉 이전 구간에서 신호 리스트(171건)가
**원본과 완전 일치**(bit 단위) — 미래 정보 참조 없음. 회계 정합(`raw_pnl_accum-fees==pnl`)도
소수점까지 일치.

## 종합 판단: 탈락 (FAIL)

1. **gross PF 부터 FULL/IS/OOS 전 구간 <1** — 수수료·슬리피지 이전부터 무엣지, 이 프로젝트
   기준으로 가장 명확한 탈락 패턴(과최적화 우려조차 필요 없음).
2. **파라미터 스윕 12변형 전부 OOS PF<0.87** — 어떤 조합으로도 통과선 근처에도 못 감. 표본도
   충분(OOS n=747, LOO 전 종목 <1)해 "판단불가"가 아니라 데이터가 명확히 FAIL 을 뒷받침.
3. **방향반전 대조군도 <1**(0.82~0.87)이고 zero_hold_frac 4.7~5.2%로 청산조건 버그 재현 없음 —
   방향 자체에 정보가 없는, 순수하게 무정보 트리거로 판정.
4. `p3_proximity` 필터·룩어헤드·회계정합 모두 정상 확인 — 구현 결함이 아니라 **"추세 소진 후
   되돌림 고점 돌파는 추세전환의 최소 확인"이라는 다우이론적 가설 자체가 우리 데이터에서
   재현되지 않음.**

**라이브 미반영.**

## 두 전략 종합 메모

앤드류스 피치포크(터치-반등형)와 1-2-3 리버설은 **같은 ZigZag 피벗·같은 3피벗 구조**를 사용하나
결과가 다르다 — 피치포크 터치형은 IS 손실·OOS 미미한(비유의) 양의 방향(FAIL, 근소한 미달),
1-2-3 은 gross 부터 명확한 음의 방향(FAIL, 확고). 두 전략 모두 "ZigZag 피벗 자체는 예상보다
3~10배 잦다"는 공통 발견을 했고, 두 전략 모두 청산조건 버그(zero_hold_frac 급등)나 룩어헤드가
없음을 독립적으로 확인했다 — **엣지 부재는 구현 문제가 아니라 가설 자체의 한계**로 판단.

## backtest-reviewer 검증: VALID

**종합 판정**: **VALID** — 원 리포트의 FAIL(gross부터 <1, 명확한 탈락) 판정에 **동의**한다.
피치포크 리포트와 ZigZag 유틸·데이터 pkl을 공유하므로 두 리포트를 함께 감사했다(감사 상세는
피치포크 리포트의 검증 블록 참조). `bt_123.py`·`analyze_123.py`·`sweep_123.py`를 직접
재실행/재구성해 리포트 표 전체(FULL/IS/OOS PF·회계정합·종목별 OOS·LOO 7종목 전체·top-N
4단계·반전대조군·`p3_proximity` 필터 영향·스윕)를 소수점 단위로 일치 확인했다.

### 체크리스트

1. **정합(parity)**: N/A — 라이브 미반영 신규 후보. 사이징은 피치포크 리포트와 동일하게
   `RiskManager` 1% 리스크 방식(라이브 증거금 기준과의 파이프라인 공통 구조적 괴리, PF
   결론 불변).
2. **단위**: PASS — 피치포크와 동일 pkl(`download.py`) 재사용, 검증 내용 동일.
3. **룩어헤드**: PASS — `zigzag.py`·`precompute_channels()`가 피치포크와 완전 동일 코드라
   피치포크 리포트에서 수행한 독립 룩어헤드 재현(ETH/ADA/XRP, 3개 다른 절단길이)이 그대로
   적용된다. **추가로 `find_signals_123`을 별도로 절단 재실행**(ETH cut=77: 180건 완전 일치,
   ADA cut=133: 277건 완전 일치, XRP cut=20: 241건 완전 일치)해 1-2-3 고유 신호 생성 로직
   (`P3>P1`/`P3<P1` 방향 조건 + `p3_proximity` 필터 + P2 최초 돌파)에도 룩어헤드가 없음을
   별도로 확인했다(피치포크 채널만 검증하고 123 고유 신호 로직은 미검증일 위험을 배제).
4. **표본크기·유의성**: PASS — OOS n=747(종목당 59~141건), t=-2.40으로 통계적으로 유의하게
   <1. 표본 부족으로 인한 판단불가가 아니라는 원 리포트 주장에 동의.
5. **OOS 분리**: PASS — `OOS_END` 필터가 코드에 존재하나 데이터 자체가 2026-06-27에서 자연
   종료돼 실질적으로 경계 밖 진입은 0건(피치포크와 동일 구조).
6. **유니버스/선택 편중**: PASS — LOO **전 종목 제외 시에도 OOS PF 0.777~0.839**(직접
   재계산, 리포트와 일치)로 특정 종목 의존이 아니라 구조적 무엣지임을 확인. BTC 단일 종목의
   PF 0.362(t=-3.64)가 유독 나쁘나, 나머지 6종목만으로도 여전히 <1(LOO excl BTC=0.839)이라
   BTC 제외가 결론을 바꾸지 않음.
7. **수수료·슬리피지**: PASS — gross PF부터 FULL/IS/OOS 전 구간 <1(0.865/0.871/0.859, 직접
   재현)이라 수수료 이전부터 무엣지. net은 참고사항일 뿐 판정의 핵심 근거가 아님.
8. **워밍업·지표 정확성**: PASS — ATR·ZigZag 워밍업은 피치포크와 동일 코드.
9. **데이터 소스**: PASS — 피치포크와 동일.
10. **재현성**: PASS — FULL/IS/OOS 3구간, 종목별 OOS 7개, 회계정합(raw_pnl_accum-fees==pnl),
    LOO 7종목 전체(0.777~0.839), top-N 4단계(0.775/0.753/0.738/0.707), 반전대조군
    zero_hold_frac(5.23%/4.73%), `p3_proximity` 필터 영향(BTC 24.2%/ETH 21.6%/BNB 26.9%
    감소율)까지 전부 독립 재계산해 **리포트 수치와 완전 일치**(불일치 0건).
11. **외부 주장**: N/A — 스펙 자체가 "75% 승률" 주장을 검증 실패로 미인용, 인용 대상 없음.

### 반전 대조군 코드 검증

`bt_123.py`의 `simulate_123()`을 코드 레벨로 확인 — `sl_initial`/`tp_target`/`invalid_level`
셋 다 `2*entry_fill - X` 형태로 진입가 기준 가격 반사되고, exit 비교(`low<=sl_v`/`high>=sl_v`,
`invalidated` 판정, `high>=tp_target`/`low<=tp_target`)가 **전부 반전 후 최종 `is_long` 변수
하나로 분기**된다 — 과거 버그 클래스(같은 부등호를 양방향에 씀) 재발 없음을 코드로 확인.
zero_hold_frac을 pkl에서 직접 재계산(FULL 5.23%, OOS 4.73%)해 리포트치와 일치.

### 클러스터링 (리포트 미기재분 보완)

OOS 고유일 388일 중 동시진입(≥2종목) 50.3%(195일), 7종목 동시 0건. 상당한 클러스터링이
존재하나(스펙이 사전 경고한 매크로 동시성 위험이 실측됨), gross부터 유의하게<1이라 t의
독립성 가정 완화가 결론(FAIL)을 바꾸지 않는다.

### ZigZag 피벗 빈도 판단 (정의 결함 vs 4h 해상도의 자연스러운 결과)

피치포크·123 두 리포트 공통으로 ZigZag 피벗 빈도가 스카우트 사전 추정(월 2~4개, 연 24~48개)
보다 **3~10배**(종목별 143.6~496.3건/년, 직접 재계산으로 일치 확인) 많다. 이를 정의 결함
신호로 볼 근거는 약하다 — 원인은 (1) 5%/4% 되돌림이라는 임계값 자체가 낮은 편이고, (2) 4h
캔들이 15m~1h 대비 봉당 변동폭이 커 상대적으로 자주 되돌림 임계를 넘으며, (3) 실제 *신호*
빈도(터치 26.9건/년/종목, 123 셋업 필터 후 54.7건/년/종목)는 스카우트 예상 범위(15~35건)에
근접하거나 합리적 배수 내에 있다는 점에서, **파생 신호 단계에서는 병리적 폭증이 없다**.
과거 적발된 "결합조건이 독립기대치의 1/500만 발생" 같은 구조적 결함(회전율레짐·체결건수흡수
건)과 달리, 이번 건은 원 리포트가 이미 "전제의 내적 일관성" 절에서 무필터 대비 필터 감소율이
1/4 수준(1/60~1/500이 아님)임을 실측 확인했고, 이 리뷰도 BTC/ETH/BNB 3종목에서 동일 감소율
(24.2%/21.6%/26.9%)을 독립 재현했다 — **정의 결함이 아니라 4h 해상도·낮은 임계값의 자연스러운
결과**로 판단, 원 리포트 결론에 동의.

### 직접 재확인한 것 (요약)

- `bt_123.py`/`analyze_123.py`/`sweep_123.py`를 pkl 캐시로 재구성/재실행해 FULL·IS·OOS PF,
  회계정합, 종목별 OOS 7개, LOO 7종목 전체, top-N 4단계, 반전대조군 PF·zero_hold_frac,
  `p3_proximity` 필터 감소율(3종목) 전부 **소수점 단위 일치**.
- 룩어헤드: 피치포크 채널 로직뿐 아니라 `find_signals_123` 고유 로직까지 별도 절단 재실행
  으로 확인(피치포크 검증만으로 유추하지 않고 1-2-3 고유 신호 함수를 직접 검증).
- 반전 대조군의 진입가 대칭 재배치 + 청산조건 방향 반전이 코드에 실제로 구현돼 있음을 코드
  추적으로 확인.
- ZigZag 피벗 빈도 3~10배 급증이 정의 결함인지 4h 해상도의 자연스러운 결과인지 판단하기 위해
  `p3_proximity` 무필터/필터 감소율을 3종목에서 독립 재계산, 원 리포트 결론(자연스러운 결과)에
  동의.

### 미확인/한계

- `data.binance.vision` 덤프 재다운로드에 의한 완전 독립 데이터 소스 검증은 하지 않음(원본
  리포트와 동일 pkl 캐시 재사용) — 파이프라인 공통 한계이며 방향성·유의성 결론에는 영향 없을
  것으로 판단.
- BTC의 유독 나쁜 PF(0.362, t=-3.64)의 원인(예: 특정 트리플·기간 편중)까지는 트레이드 단위로
  파고들지 않았음 — 다만 LOO상 BTC 제외해도 여전히 FAIL이라 판정에 영향 없음.
