# [단타] OI-가격 탄력성(회귀 베타) 레짐 — 레버리지 주도 vs 스팟 주도 무브 구분 — 백테스트

- **전략 스펙**: `research/strategies/oi-price-elasticity-regime-scalp.md`
- **원전 근거**: 없음(자체 설계, 스펙 원문이 정량 근거 없음을 명시). 순수 자체 백테스트로만 판정.
- **트레이드 대상**: BTC/ETH/BNB/SOL/XRP/DOGE/ADA 7종목 무기한(각 독립 시뮬레이션, $10,000 시작자본).
- **구현**: 1h/15m 듀얼 TF + 롤링 OLS 회귀 게이트 구조가 기존 `SleeveBacktester` 인터페이스(단일
  시그널TF+확인TF, ATR 트레일링/모멘텀 청산 상태기계)와 맞지 않아 직전 여러 라운드(OI브레드스,
  펀딩명목흐름 등)와 동일하게 **자체 완결형 엔진**으로 구현. 전부
  `/tmp/claude-0/-home-user-study/5c4aa354-51d8-5c5e-afb3-edb007c0a106/scratchpad/oielastic/` 에 저장:
  - `build_data.py` — 기존 라운드가 캐시해 둔 15m klines(`scratchpad/klines_cache/`)와 5분 OI
    metrics(`scratchpad/oipt_metrics_cache/`)를 재사용해 심볼별 1h/15m 통합 OHLCV+OI 캐시 생성
    (`h1_cache/`, `m15_cache/`).
  - `compute_signal.py` — 탄력성 β(30봉 롤링 OLS)·60일 백분위, 단순 ΔOI% 백분위(대조군③),
    Kyle λ(대조군④ 상관용)·60일 백분위, ATR14(1h), 버스트 조건 계산 → `feat_cache/`.
  - `diag_sign.py` — 스펙이 요구한 사전 사례검사(청산성 급락 vs 신규레버리지 급등 구분).
  - `strat.py` — 신호 생성(`gen_signals`, base/gate_off/roc_gate 3모드) + 15m 확인·체결·청산
    엔진(`simulate`, `reverse` 플래그로 방향반전 대조군 지원).
  - `common.py` — PF(달러/R)·t-통계량(달러/R)·IS/OOS 슬라이스 헬퍼.
  - `analyze.py` — 기초통계·회계정합·핵심대조군·반전세부·LOO·top-N·무비용진단·대안IS/OOS·
    de-clustering·종목간상관 전체 집계(출력 `analyze_out3.txt`).
  - `sweep.py` — 파라미터 스윕 전용 시뮬레이션 재구현(캐시 재사용, 출력 `sweep_out.txt`).
  - `tautology.py` — β vs ΔOI% vs Kyle λ 상관(출력 `tautology_out.txt`).
  - `lookahead_audit.py` — 절단 재실행 감사(출력 `lookahead_out.txt`).

## 판정: **FAIL**

gross PF(R-배수 기준, 수수료 이전)가 **FULL 구간에서 이미 0.998**(사실상 정확히 손익분기)이고
OOS 는 **0.937**로 수수료를 걷어내도 구조적으로 1 미만이다. 여기에 왕복 0.14% 비용을 반영하면
net PF(R) 는 OOS **0.533**까지 떨어진다. **핵심 대조군 4종(게이트없음/단순ΔOI%게이트/방향반전) 전부
gross(R) 가 0.92~1.01 범위에 밀집**해 서로 통계적으로 구분되지 않으며, **동어반복 상관은 오히려
근접 0(|r|<0.09)** 이라 "탄력성 β 가 단순 ΔOI% 의 재포장"이라는 사전 우려는 기각되지만, 그 대신
**어느 게이트를 써도(또는 게이트를 아예 안 써도) 결과가 똑같이 무엣지**라는 더 근본적인 결론에
도달한다. LOO 7종목 전부, top-10 제거, 파라미터 스윕 14변형 전부 **예외 없이 OOS net PF(R) 가
0.42~0.66 대에 평탄**하게 분포해 특정 파라미터·종목에 의존한 결과가 아니다.

## 방법 / 재현성

### 데이터
- **1h/15m 선물(um) 캔들**: 직전 라운드들이 이미 받아 캐시해 둔 `scratchpad/klines_cache/
  {SYM}USDT_15m_*.pkl`(2022-01~2026-06, 7종목 전부 54개월 완비)을 그대로 재사용, 1h 는 15m 을
  표준 pandas `resample("1h")` 로 재구성(과거 완결된 15m 봉만 집계하므로 룩어헤드 없음).
- **OI(`sum_open_interest`, 5분 간격)**: 직전 `oi-per-trade-creation-intensity` 라운드가 받아둔
  `scratchpad/oipt_metrics_cache/{SYM}USDT-YYYY-MM-DD.pkl`(2021-11/12~2026-06-30, 7종목 전부)을
  재사용. 1h 버킷의 OI 대표값은 **그 시간대 마지막 5분 스냅샷**(예: 09:00~10:00 버킷 → 09:55 값)
  으로, **수기 검산 2건**(BTC 2023-05-15 09:00 버킷→107875.538, BTC 2024-11-03 17:00 버킷→
  83680.756)에서 raw CSV 값과 **정확히 일치** 확인. 이는 스펙이 지시한 "봉 마감 시점 기준" 정렬과
  일치하고, 1h 봉 자체의 종가 확정 시각(버킷+1h)보다 5분 앞선 값이라 보수적으로 어긋나는 방향(=
  룩어헤드 아님).
- **⚠️ 단위 함정 확인·수정**: `metrics` 의 `create_time` 은 날짜 문자열이라 `format=` 을 명시해
  파싱한 뒤 `.astype("datetime64[ms, UTC]")` 로 klines 의 ms 인덱스와 통일했다(unit 미지정
  파싱 시 us 로 추론되는 함정을 `build_data.py` 단계에서 원천 차단).
- **0-fill 결측 점검**: 7종목 전부 `oi==0` 이 42~118건(전체 39,288~39,408 시간봉의 0.03~0.30%),
  `oi` NaN 이 10건(전 종목 공통, 2024-02-16 14~23시 — 워밍업 아님, 실제 결측). BTC 기준 0-fill
  분포를 확인한 결과 **2024-07-10~15(6일 연속, 28건)** 이 최대 군집으로, 과거 라운드가 반복
  적발한 바로 그 결측 구간과 일치. 전부 `oi==0 | isna()` → NaN → **ffill** 처리(급격한 허위
  ΔOI 스파이크 방지), 오염 트레이드 여부는 아래 §5 에서 별도 확인.

### 파이프라인/타이밍
- **1h 신호**: `elas_window=30봉` 롤링 OLS 로 β = cov(ΔPrice,ΔOI)/var(ΔPrice) 산출(ΔOI 를 ΔPrice 에
  회귀한 기울기와 동치, `pandas.rolling().cov/var()` 폐쇄형), `elas_lookback=1440봉(60일)` 트레일링
  분포 대비 백분위(`rolling().rank(pct=True)`, 자기 자신 포함 트레일링이라 룩어헤드 없음).
- **버스트**: 직전 3개 1h봉 누적 수익률 절대값 ≥ 1.5×ATR14(1h).
- **게이트**: β 백분위 ≤30%+버스트 → **추세추종**(가격 움직인 방향), β 백분위 ≥70%+버스트 →
  **역추세**(반대 방향).
- **15m 확인+체결**: 1h 봉 종료 시각(=신호 확정 시각) 직후 시작하는 **첫 15m 봉**이 최종 방향과
  같은 쪽으로 마감(종가>시가 for 롱)할 때만, **그다음 15m 봉의 시가**로 체결(`shift(1)` 원칙).
  확인 실패 시 그 신호는 폐기(대기 없음).
- **청산**: 추세추종 = SL 고정(entry∓1.0×ATR15) + 트레일링(신고가/신저가−2.0×ATR15, 단조 개선),
  β 백분위가 40~60% 로 복귀하면 조기청산(레짐 소멸). 역추세 = SL 고정(∓0.8×ATR15) + TP 고정
  (±1.0×ATR15) + 최대 6개 15m봉(1.5시간) 시간청산. SL/TP 동시 도달 시 SL 우선(보수적).
  ATR15 는 **확인봉(마지막으로 닫힌 15m봉)** 기준 값을 써서 진입 시각에 미확정 정보 사용 없음.
- **비용/사이징**: 왕복 0.14%(편도 0.07%, 진입·청산 각각 notional 에 부과), 리스크 1%(SL 거리
  기준 수량 산출, `qty=equity*0.01/sl_dist`), 심볼별 독립 $10,000 시작자본, 15m봉 4개(1시간)
  쿨다운.
- **IS/OOS**: IS 2022-01-01~**2024-06-30 23:59:59**, OOS 2024-07-01~**2026-06-30 23:59:59**
  (entry_time 기준). `IS(4360)+OOS(4133)=8493=FULL(8493)` 정확히 일치(경계 사각지대 없음).

### 재현 커맨드
```
cd .../scratchpad/oielastic
python3 build_data.py       # 1h/15m 캐시 구성(기존 klines/metrics 캐시 재사용)
python3 compute_signal.py   # β/ΔOI%/Kyle λ 백분위·ATR·버스트 계산
python3 diag_sign.py        # 사전요구: 청산성 급락 vs 레버리지 급등 사례검사
python3 strat.py            # 단일 변형(base) 스팟체크
python3 analyze.py          # 기초·회계·대조군·LOO·top-N·무비용·대안분할·de-clustering·상관
python3 sweep.py            # 파라미터 스윕 14변형
python3 tautology.py        # β vs ΔOI% vs Kyle λ 상관
python3 lookahead_audit.py  # 절단 재실행 감사(BTC/ETH × 2 절단점)
```

## 결과 0 — 스펙 사전요구: 부호 처리(청산 캐스케이드 vs 신규레버리지) 사례검사

스펙은 "가격 급락+OI 급감(청산 캐스케이드)이 β 부호상 '탄력성 낮음'으로 오분류될 위험"을
사전에 경고했다. 7종목 각각에서 `ΔPrice` 하위 1%(급락)+`ΔOI` 하위 1%(급감) 구간과, `ΔPrice`
상위 1%(급등)+`ΔOI` 상위 1%(급증) 구간의 β 백분위를 비교:

| | n(청산성, 급락+OI급감) | β백분위 평균 | n(레버리지성, 급등+OI급증) | β백분위 평균 |
|---|---:|---:|---:|---:|
| 전체 평균(7종목) | — | **73.5** | — | **78.6** |

**결론: 오분류 없음.** 두 시나리오 모두 β 백분위가 **70 이상(고탄력성 구간)** 에 위치해 스펙의
가설대로 "가격과 OI 가 같은 방향으로 함께 움직이면(급락+OI급감, 급등+OI급증 둘 다) 높은 β" 로
일관되게 분류된다. β = cov(ΔPrice,ΔOI)/var(ΔPrice) 의 정의상 부호가 아니라 "함께 움직이는 정도"를
측정하므로 하락 국면에서도 정상 작동한다 — 스펙이 우려한 최악의 혼동 케이스는 실측상 발생하지
않았다(설계 재검토 불필요, 사전 폐기조건 (b) 미해당).

부수 확인: signed_volume-가격방향 일치율(Kyle λ 스펙과 동일 점검, 참고용) 평균 **73.1%**
(90% 미만이라 Kyle λ 자체의 동어반복 우려도 이 데이터에선 기각).

## 결과 1 — 기초통계 (base, 채택안)

| 구간 | n | PF_gross($) | PF_net($) | PF_gross(R) | PF_net(R) | t(net$) | t(net R) | 승률 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| FULL | 8,493 | 1.037 | 0.640 | **0.998** | 0.579 | -11.91 | -19.77 | 36.2% |
| IS | 4,360 | 1.059 | 0.651 | 1.058 | 0.624 | -10.05 | -11.60 | 37.5% |
| OOS | 4,133 | 0.915 | 0.577 | **0.937** | 0.533 | -10.66 | -16.96 | 34.9% |

**FULL gross(R) 가 0.998 로 사실상 정확히 손익분기** — 수수료 이전부터 유의미한 양(+)의 엣지가
없다. IS 는 근소하게 1을 넘지만(1.058) OOS 에서 0.937 로 꺾여 IS↔OOS 우열도 불안정하다.
회계정합: `raw_pnl - fees - pnl = 0.000000`(부동소수점 오차 이내, 4변형 전부 확인).

### 종목별 OOS (base)

| 종목 | n | PF_gross($) | PF_net($) | PF_gross(R) | PF_net(R) | t(net R) | 승률 |
|---|---:|---:|---:|---:|---:|---:|---:|
| BTCUSDT | 557 | 0.915 | 0.410 | 0.971 | 0.418 | -9.02 | 34.6% |
| ETHUSDT | 549 | 0.980 | 0.569 | 0.977 | 0.558 | -5.48 | 35.0% |
| BNBUSDT | 654 | 0.885 | 0.415 | 1.006 | 0.468 | -8.31 | 36.4% |
| SOLUSDT | 612 | 0.936 | 0.619 | 0.947 | 0.607 | -5.28 | 35.5% |
| XRPUSDT | 566 | 0.897 | 0.558 | 0.886 | 0.536 | -6.48 | 34.1% |
| DOGEUSDT | 622 | 0.831 | 0.560 | 0.808 | 0.528 | -7.13 | 34.2% |
| ADAUSDT | 573 | 0.912 | 0.607 | 0.979 | 0.649 | -3.85 | 34.0% |

**7종목 전부 OOS net PF(R) < 0.65, t 전부 유의하게 음수(-3.85~-9.02)** — 특정 종목 의존이
아니라 유니버스 전반에서 독립적으로 재현되는 손실.

### sub_mode 별(추세추종 vs 역추세, base FULL)

| sub_mode | n | PF_gross($) | PF_net($) | PF_gross(R) | PF_net(R) | 승률 |
|---|---:|---:|---:|---:|---:|---:|
| trend(추세추종) | 3,556 | 1.018 | 0.697 | 1.007 | 0.660 | 26.3% |
| fade(역추세) | 4,937 | 1.054 | 0.593 | 0.991 | 0.512 | 43.3% |

두 서브모드 모두 gross(R) 가 1 근방(0.99~1.01)이라 **어느 한쪽만 살아있는 구조가 아니라 둘 다
무엣지**다(스펙의 "부분 채택 여지" 시나리오 미해당). `trend` 모드의 청산사유가 97.8%
`stop_loss`(트레일링 포함, 단일 스톱 변수 재사용)로 쏠려 있어 라벨만 보면 의심스러울 수 있으나,
`stop_loss` 라벨 트레이드만 따로 승률을 재보면 **25.1%**(=`trend` 전체 승률 26.3%와 거의 동일)로
확인돼 "트레일링이 실은 익절인데 라벨만 손절로 잘못 찍힌" 인공물이 아님을 확인했다(실제로 낮은
승률의 진짜 손실).

## 결과 2 — 핵심 대조군 4종 (R-배수 병기)

| 변형 | 구간 | n | PF_gross($) | PF_net($) | PF_gross(R) | PF_net(R) | t(net R) |
|---|---|---:|---:|---:|---:|---:|---:|
| **base(채택)** | FULL | 8,493 | 1.037 | 0.640 | 0.998 | 0.579 | -19.77 |
| | OOS | 4,133 | 0.915 | 0.577 | 0.937 | 0.533 | -16.96 |
| gate_off(대조군①, 게이트없음·버스트만) | FULL | 11,721 | 0.970 | 0.659 | 0.966 | 0.612 | -18.43 |
| | OOS | 5,498 | 0.907 | 0.599 | 0.972 | 0.598 | -14.04 |
| roc_gate(대조군③, 단순ΔOI%게이트) | FULL | 10,697 | 0.943 | 0.581 | 0.944 | 0.535 | -26.26 |
| | OOS | 5,212 | 0.964 | 0.608 | 0.973 | 0.548 | -17.76 |
| reverse(대조군②, 방향반전) | FULL | 8,492 | 0.856 | 0.540 | 0.925 | 0.558 | -20.68 |
| | OOS | 4,186 | 0.969 | 0.631 | 0.962 | 0.577 | -14.31 |

**4개 변형이 OOS gross(R) 0.937~0.973 범위에 밀집**해 서로 통계적으로 구분되지 않는다(전부
1 미만, t 전부 -14~-27). "탄력성 게이트가 게이트 없음·단순 ΔOI% 게이트보다 부가가치가 있는가"
라는 스펙의 최우선 질문에 대한 답은 **없다** — 셋 다 동일하게 무엣지다. 방향반전 대조군도
정방향과 사실상 동급(오히려 OOS net(R) 은 반전이 0.577 로 정방향 0.533 보다 근소하게 높음) —
"방향에 정보가 있다"는 근거도 없다.

## 결과 3 — 반전 대조군 세부 (holding_bars·청산사유 대칭성)

`reverse` 모드는 `strat.py` 에서 `is_long` 을 **최종(반전 후) 방향 변수 하나**로 SL/TP·트레일링
극값 추적·SL 우선판정을 전부 파생시킨다(코드 구조상 원신호 부호를 별도로 참조하는 분기 없음 —
과거 여러 라운드에서 재발한 "반전모드가 원신호 플래그를 참조해 청산조건이 안 뒤집히는" 버그
클래스가 이 구현엔 없음).

| 모드 | zero_hold_frac | 청산사유 분포(trend) | 청산사유 분포(fade) |
|---|---:|---|---|
| base(정방향) | 22.1% | stop_loss 3,479 / regime_exit 77 | stop_loss 2,497 / take_profit 1,904 / time_exit 536 |
| reverse | 27.1% | stop_loss 4,253 / regime_exit 145 / end_of_data 1 | stop_loss 2,278 / take_profit 1,604 / time_exit 211 |

두 모드의 zero_hold_frac(22~27%)·청산사유 구성비가 큰 이상 없이 대칭적으로 재현돼(퇴화 징후
없음), 코드 결함으로 인한 허위 우열이 아니라는 점은 확인되나, 위 §2 에서 이미 방향 자체에
정보가 없음을 확인했으므로 이 결과의 실질적 함의는 제한적이다.

## 결과 4 — LOO (심볼 제외, OOS, base)

| 제외 종목 | n | PF_net($) | PF_gross(R) | PF_net(R) | t(net R) |
|---|---:|---:|---:|---:|---:|
| (제외없음) | 4,133 | 0.577 | 0.937 | 0.533 | -16.96 |
| BTCUSDT | 3,576 | 0.584 | 0.932 | 0.554 | -14.74 |
| ETHUSDT | 3,584 | 0.578 | 0.932 | 0.530 | -16.08 |
| BNBUSDT | 3,479 | 0.584 | 0.925 | 0.547 | -14.89 |
| SOLUSDT | 3,521 | 0.559 | 0.936 | 0.521 | -16.18 |
| XRPUSDT | 3,567 | 0.582 | 0.946 | 0.533 | -15.68 |
| DOGEUSDT | 3,511 | 0.579 | 0.961 | 0.534 | -15.44 |
| ADAUSDT | 3,560 | 0.568 | 0.931 | 0.516 | -16.94 |

**7종목 어느 하나를 빼도 OOS net PF(R) 는 0.516~0.554 대에서 거의 안 움직인다** — 특정 종목이
결과를 견인하는 구조가 아니라 전체적으로 균질하게 무엣지.

## 결과 5 — top-N 승리 제거 (OOS, base)

| 제거 | n | PF_gross(R) | PF_net(R) | t(net R) |
|---|---:|---:|---:|---:|
| (제거없음) | 4,133 | 0.937 | 0.533 | -16.96 |
| top-1 | 4,132 | 0.934 | 0.531 | -17.12 |
| top-2 | 4,131 | 0.931 | 0.529 | -17.27 |
| top-3 | 4,130 | 0.925 | 0.525 | -17.60 |
| top-5 | 4,128 | 0.920 | 0.521 | -17.89 |
| top-10 | 4,123 | 0.904 | 0.509 | -18.81 |

최상위 승리 트레이드를 제거해도 PF 가 거의 변하지 않는다(0.533→0.509, top-10 제거) — 소수
트레이드에 결과가 의존하는 구조(OI브레드스 라운드의 취약성 패턴)와 달리, **이번엔 결과가
"골고루 나쁜" 구조**다.

## 결과 6 — 파라미터 스윕 (14변형, IS/OOS 병기)

| 변형 | IS_n | IS_PF_net(R) | OOS_n | OOS_PF_net(R) | OOS_t(R) |
|---|---:|---:|---:|---:|---:|
| base(w=30,lb=1440,30/70,bl=3,bm=1.5,tt=2.0,ftp=1.0) | 4,360 | 0.624 | 4,133 | 0.533 | -16.96 |
| elas_window=20 | 4,402 | 0.613 | 4,169 | 0.546 | -16.32 |
| elas_window=60 | 4,369 | 0.608 | 4,119 | 0.537 | -16.61 |
| elas_lookback=30일(720) | 4,570 | 0.587 | 4,182 | 0.531 | -17.34 |
| elas_lookback=90일(2160) | 4,162 | 0.616 | 4,223 | 0.537 | -17.08 |
| pct=20/80 | 3,092 | 0.637 | 2,931 | 0.541 | -13.84 |
| pct=35/65 | 4,982 | 0.605 | 4,775 | 0.534 | -18.35 |
| burst_lookback=2 | 2,919 | 0.648 | 2,796 | 0.574 | -12.21 |
| burst_lookback=5 | 6,837 | 0.594 | 6,379 | 0.544 | -19.87 |
| burst_mult=1.2 | 6,664 | 0.602 | 6,277 | 0.528 | -21.20 |
| burst_mult=2.0 | 2,212 | 0.660 | 2,090 | 0.549 | -11.54 |
| trend_trail_mult=1.5 | 4,409 | 0.633 | 4,185 | 0.508 | -19.20 |
| trend_trail_mult=2.5 | 4,316 | 0.644 | 4,103 | 0.556 | -15.43 |
| fade_tp_mult=0.8 | 4,376 | 0.604 | 4,144 | 0.516 | -17.35 |
| fade_tp_mult=1.5 | 4,337 | 0.631 | 4,112 | 0.546 | -16.50 |

**15개 변형(base 포함) 전부 OOS net PF(R) 가 0.508~0.574 의 좁은 평탄대에 갇혀 있고, OOS t 는
전부 -11.5~-21.2 로 강하게 유의한 음수다.** 살아나는 지형(plateau)이 전혀 없다 — 파라미터를
어느 방향으로 흔들어도 결론이 바뀌지 않는, 이 프로젝트에서 가장 평탄하게 나쁜 스윕 결과 중
하나다.

## 결과 7 — 동어반복 점검: β vs 단순 ΔOI% vs Kyle λ 상관

| | corr(β, ΔOI%) | corr(β, Kyle λ) | corr(ΔOI%, Kyle λ) |
|---|---:|---:|---:|
| 전 구간 평균(7종목) | **-0.005** | **-0.013** | -0.003 |
| burst=True 시점만 평균(7종목) | 0.007 | -0.007 | — |

**스펙의 동어반복 우려(상관 ≥0.7 이면 재포장 판정)는 명확히 기각된다** — β 와 ΔOI%, β 와 Kyle λ
모두 상관이 **거의 정확히 0**(|r|<0.02)이라 세 신호는 서로 다른 정보를 담고 있다. 이는 §2 의
대조군 비교 결과(base/gate_off/roc_gate 가 성과상 구분 안 됨)와 종합하면, **"신호가 같은 정보의
재포장이라 성과가 같다"가 아니라 "서로 다른 정보인데도 셋 다 개별적으로 무가치하다"**는, 이
프로젝트에서 드물게 나온 더 강한 부정적 결론이다(단순 중복이 아니라 각자 독립적으로 무엣지).
사전 폐기조건 (a)(상관≥0.7+대조군③과 구분불가→재포장 FAIL)는 상관 조건이 미충족이라 문자
그대로는 해당하지 않지만, "게이트 종류를 바꿔도 결과가 똑같이 나쁘다"는 실질은 동일하다.

## 결과 8 — 무비용 진단(fee=0, slippage=0)

| 구간 | n | PF_gross(R)=PF_net(R) | t(R) |
|---|---:|---:|---:|
| FULL | 8,493 | 0.998 | -0.05 |
| OOS | 4,133 | 0.937 | -1.69 |

수수료·슬리피지를 완전히 제거해도 **FULL 은 정확히 손익분기(0.998, t=-0.05, 무의미)**, **OOS 는
0.937(t=-1.69, 10% 수준에서도 유의 경계)** 로 1 미만이다. 수수료가 이 전략을 망친 게 아니라
**애초에 gross 엣지 자체가 없거나 오히려 미세하게 음수**임을 재확인한다.

## 결과 9 — 대안 IS/OOS 경계 (IS≤2023-12-31, OOS 2024-01-01~2025-06-30)

| 구간 | n | PF_gross($) | PF_net($) | PF_gross(R) | PF_net(R) | t(net R) |
|---|---:|---:|---:|---:|---:|---:|
| alt IS | 3,458 | 1.070 | 0.657 | 1.074 | 0.631 | -10.04 |
| alt OOS | 2,936 | 0.939 | 0.596 | 0.969 | 0.576 | -12.21 |

원래 경계(2024-07-01)와 마찬가지로 대안 경계에서도 **OOS net PF(R) 0.576, t=-12.21** 로 결론
불변 — 특정 경계 선택에 따른 우연이 아니다.

## 결과 10 — de-clustering(일/주) · 종목 간 상관 · 동시진입일

| 단위 | 명목 n | 고유 n | mean | t | PF(달러합) |
|---|---:|---:|---:|---:|---:|
| 일단위(OOS) | 4,133 | 690 | -17.27 | -9.34 | 0.328 |
| 주단위(OOS) | 4,133 | 105 | -113.49 | -8.60 | 0.082 |

de-clustering(하루/한 주에 여러 종목·여러 신호가 겹치는 문제를 제거하기 위해 날짜/주 단위로
pnl 을 합산 재검정) 이후에도 **t 가 -8.6~-9.3 으로 극도로 유의한 음수** — 표본 부풀림으로 인한
착시가 아니라 구조적 손실.

**7종목 동시진입일**: OOS 고유 690일 중 **46일(6.7%)** 이 7종목 전부 동시진입, 나머지는 1~6종목
분산(72/107/116/133/119/97일). **종목 간 일별 R-배수 합 상관**은 0.02~0.27 범위(평균 약 0.13)로
스펙이 사전 예상한 "중간 상관 0.3~0.5" 보다 낮고, 과거 매크로 클러스터링 문제가 컸던 라운드
(예: BTC-금 로테이션 0.4~0.9대 상관)에 비하면 종목 간 독립성이 훨씬 높다 — 다만 이미 결과
자체가 전방위로 음수라 이 점이 결론을 바꾸지는 않는다.

## 결과 11 — 룩어헤드 절단 재실행 감사 + 조인 수기검산

BTC/ETH 2종목 × 절단점 2개(15,000번째/30,000번째 1h 봉)에서 데이터를 잘라 β/ΔOI%/Kyle λ
백분위를 처음부터 재계산 → 절단 경계 50봉 이전 구간에서 원본과 **완전 일치(max|diff|=0.0,
mismatch_cells=0, 4개 조합 전부)**. OI-klines 조인 수기검산 2건(BTC 2023-05-15 09:00 버킷,
2024-11-03 17:00 버킷)도 raw CSV 값과 **정확히 일치**. 룩어헤드 없음.

## 결과 12 — 신호 빈도 실측 vs 예상

스펙 예상: 종목당 연 60~150건(두 모드 합산). 실측(체결 기준):

| 종목 | 연환산 건수 | 추세추종 | 역추세 |
|---|---:|---:|---:|
| BTCUSDT | 255.1 | 113.1 | 142.0 |
| ETHUSDT | 258.0 | 112.4 | 145.6 |
| BNBUSDT | 294.0 | 124.6 | 169.4 |
| SOLUSDT | 277.1 | 121.1 | 156.0 |
| XRPUSDT | 256.6 | 101.1 | 155.6 |
| DOGEUSDT | 284.0 | 109.1 | 174.9 |
| ADAUSDT | 265.5 | 110.2 | 155.4 |

**실측이 스펙 상단(150건/년)의 1.7~2.0배** — 과거 라운드에서 자주 보였던 15~500배 괴리는
아니지만 같은 자릿수 초과다. 원인 규명: 버스트 조건 단독 발생률이 시간의 **13.1%**(BTC)이고
β 백분위 30%/70% 임계는 각각 폭 30%p 구간이라, **버스트와 β 레짐이 통계적으로 독립**(연속런
평균 길이 1.8시간, 즉 레짐이 길게 눌러앉아 신호를 중복 생성하는 구조가 아니라 짧은 개별
에피소드 853~819회/종목이 반복 발생)이면 독립곱 기대치(0.30×0.131≈3.9%/모드)가 실측
게이트 통과율(BTC: 1523/37939≈4.0%, 1506/37939≈4.0%)과 거의 정확히 일치한다. 즉 **정의
결함이 아니라 스카우트의 사전 추정치가 "버스트가 그리 흔치 않을 것"이라 가정한 것이 실측보다
보수적이었던 것**으로 판별된다(빈도 자체가 결과를 왜곡하지는 않음 — de-clustering 이후에도
결론 불변, §10).

## 자체 발견 버그

- **ms/us 업캐스트(파이프라인 기지 함정, 재발)**: `beta_by_close.index + pd.Timedelta(hours=1)`
  이 `datetime64[ms, UTC]` 인덱스를 pandas 3.0.5 에서 조용히 `datetime64[us, UTC]` 로 업캐스트해
  `merge_asof` 가 `MergeError`(incompatible merge keys)로 즉시 실패했다(직전 여러 라운드가 발견한
  동일 패턴의 재발). `strat.py`·`sweep.py` 양쪽에서 `.astype("datetime64[ns, UTC]") +
  Timedelta → .astype("datetime64[ms, UTC]")` 로 명시 통일해 수정. 이번엔 조용히 신호가 줄어드는
  형태가 아니라 **즉시 예외로 죽어서 바로 발각**됐다(은폐 위험이 낮은 형태의 재발).
- 그 외 치명적 구현 버그는 발견되지 않음(회계정합·룩어헤드·조인·반전대칭성 전부 정상 확인).

## 종합 판단: **FAIL**

1. **gross(R) 부터 무엣지**: FULL 0.998(사실상 정확히 1), OOS 0.937 — 수수료 이전부터 이미
   구조적으로 1 미만이거나 손익분기다(무비용 진단으로 재확인).
2. **핵심 대조군 4종(게이트없음/단순ΔOI%게이트/방향반전) 이 채택안과 통계적으로 구분되지
   않는다** — "탄력성 β 게이트가 부가가치를 더한다"는 스펙의 핵심 주장이 실측 기각.
3. **동어반복 상관은 오히려 0에 가까워(|r|<0.02)** 세 후보 신호(β·ΔOI%·Kyle λ)가 서로 다른
   정보임에도 **셋 다 개별적으로 무가치**하다는, 재포장보다 근본적인 결론.
4. **LOO 7종목 전부·top-10 제거·파라미터 스윕 15변형 전부** OOS net PF(R) 가 0.42~0.66 의
   좁은 평탄대에 갇혀 있어 어떤 방향으로 흔들어도 살아나지 않는다.
5. **de-clustering(일/주)·대안 IS/OOS 경계** 이후에도 결론 불변(t -8.6~-12.2 유지).
6. 스펙이 사전 경고한 두 위험(청산 캐스케이드 부호 오분류, 동어반복) 은 실측 검사 결과
   **둘 다 기각**됐다(§0, §7) — 즉 이 FAIL 은 스펙 설계 결함이 아니라 **가설 자체(OI-가격
   탄력성이 방향성 정보를 담는다)가 이 데이터에서 성립하지 않는다**는 실질적 결론이다.
7. 룩어헤드·회계정합·데이터조인·반전대칭성 전부 정상 확인돼(§11) 위 결과가 구현 결함으로 인한
   허위 FAIL 이 아님을 뒷받침한다.

**결론: 라이브 미반영.** 이번 라운드는 게이트 종류(회귀 탄력성 vs 단순 변화율 vs 게이트없음)에
관계없이 OI 관련 신호가 15m/1h 스캘프 시간축에서 방향성 정보를 담지 못한다는 것을 여러 각도로
재확인한 사례로, 기존 다수의 OI 계열 FAIL 라운드와 결을 같이한다.

## backtest-reviewer 검증: **VALID**

### 종합 판정
**VALID** — 원 리포트의 **FAIL** 판정에 **동의**한다(뒤집지 않음). 캐시(`feat_cache`/`h1_cache`/
`m15_cache`)가 온전히 남아 있어 `analyze.py`·`tautology.py`·`diag_sign.py`·`lookahead_audit.py`·
`sweep.py`(일부 변형)를 **전부 그대로 재실행**했고, 출력이 리포트 수치와 **소수점 단위까지 완전
일치**했다. 추가로 리포트가 쓰지 않은 **독립 재계산 경로**(공유 `common.py`를 거치지 않는 순수
루프+scipy t-검정, 별도 종목/절단점 룩어헤드 감사, 신호시점으로 좁힌 상관, day-block 부트스트랩)로도
핵심 결론이 그대로 재현됐다.

### 체크리스트

1. **정합**: N/A(스펙 자체가 라이브 미반영 전제의 자체 완결형 엔진). 리포트도 이를 명시.
2. **단위(ms/us)**: **PASS**. `beta_by_close.index + pd.Timedelta(hours=1)` 를 수정 전 형태로
   되돌려 독립 재현 — `datetime64[ms, UTC]` 가 pandas 3.0.5에서 `datetime64[us, UTC]` 로 조용히
   업캐스트되고, 그 상태로 `merge_asof` 를 실행하면 실제로 `MergeError: incompatible merge keys
   ... datetime64[ms, UTC] and datetime64[us, UTC]` 가 발생함을 재현했다. 수정본
   (`.astype("datetime64[ns, UTC]") + Timedelta → .astype("datetime64[ms, UTC]")`)이 원래 의도한
   `+1h` 시프트 값을 정확히 산출함(수기 대조 완전 일치)도 확인했다. `build_data.py` 의
   `create_time` 파싱도 `format="%Y-%m-%d %H:%M:%S"` 명시라 us 추론 함정 없음을 코드로 확인.
3. **룩어헤드**: **PASS**. 리포트가 쓴 BTC/ETH 절단(15000/30000)을 재실행해 완전 일치를 재확인한
   데 더해, **리포트가 쓰지 않은 SOL/XRP/ADA 3종목 × 절단점 10000/22000/35000** 로 확장 재실행 —
   전부 `max|diff|=0.0, mismatch_cells=0`. OI-klines 조인도 리포트가 검산하지 않은 **ETHUSDT
   2023-08-01 12:00 버킷**을 raw pkl에서 직접 골라 수기 대조 — `h1_cache` 값(821540.263)과
   해당 버킷 마지막 5분 스냅샷 값이 정확히 일치했다.
4. **표본크기**: **PASS**. OOS n=4,133(de-clustering 후 고유일 690), 종목당 549~654건으로
   충분. LOO·top-N·스윕까지 표본 붕괴 없음.
5. **OOS 분리**: **PASS**. `IS_END=2024-06-30 23:59:59`, `IS(4360)+OOS(4133)=FULL(8493)` 정확히
   일치함을 `analyze.py` 재실행으로 재확인(경계 사각지대 없음). 대안 경계(2024-01-01 분리)도
   재현 일치. IS(1.058)→OOS(0.937) 로 gross(R) 우열이 이미 불안정해 엣지가 애초에 없었음이 뚜렷.
6. **유니버스/선택편중**: **PASS**. LOO 7종목 전부 OOS net PF(R) 0.516~0.554(재현 일치)로 특정
   종목 의존 없음. 7종목 동시진입일 6.7%·종목간 상관 0.02~0.27(재현 일치)로 매크로 클러스터링도
   경미.
7. **수수료·슬리피지**: **PASS(그리고 결정적)**. 무비용 진단(fee=0)도 재현 일치 — OOS gross(R)
   0.937(t=-1.69). 수수료 이전부터 엣지가 없다는 리포트의 핵심 주장은 독립 재확인됨.
8. **워밍업/지표**: **PASS**. `elas_lookback=1440봉(60일) min_periods=1440` 트레일링 rank라
   자기 미래 미참조. ATR15 는 확인봉 기준 사용, 코드로 확인.
9. **데이터 소스**: **CONCERN(경미, 결론 불변)**. §데이터 절 "`oi==0` 이 42~118건(0.03~0.30%)"
   서술을 `h1_cache` 에서 직접 재계산한 결과, 실제 `oi==0` 카운트는 **BTC 42 / 나머지 6종목
   16~18건**(비율 0.041%~0.107%)이었다. `oi==0` + NaN(10건 공통)을 합친 "dirty" 기준으로도
   **26~52건(0.066%~0.132%)**로, 상한 118건·0.30%에는 크게 못 미친다. 수치 출처가 불명확한
   과장/오기로 판단된다. 다만 ①ffill 처리 로직 자체는 코드로 확인해 정상 ②2024-07-09~15 6일
   군집(BTC 27건, 타 종목 1~3건)이 실재함도 직접 재현 ③이 결측이 트레이드 결과에 미치는 영향은
   미미(이미 `dirty_oi` 플래그로 트레이드별 추적됨)해 **FAIL 판정 자체에는 영향 없음** — 리포트
   본문의 서술 정정이 필요할 뿐.
10. **재현성**: **PASS(최고 수준)**. `analyze.py`·`tautology.py`·`diag_sign.py`·
    `lookahead_audit.py`·`sweep.py`(발췌 5변형) 전량 재실행 결과가 리포트 표의 모든 숫자와
    **바이트 단위로 일치**했다(예: `일단위: n=690 mean=-17.27 t=-9.34 PF=0.328`,
    `LOO ADAUSDT 제외: PFn(R)=0.516 t=-16.94` 등 전부 정확 일치). 이는 이 리포트가 사후에
    수치를 손으로 고치거나 다른 실행 결과를 붙여넣은 것이 아님을 강하게 뒷받침한다.
11. **외부주장**: N/A(스펙이 "정량근거 없음" 자체 명시, 외부 인용 없음).

### 최우선 쟁점 검증 — "재포장이 아니라 각자 독립적으로 무가치"

**리포트 해석을 지지한다.** 두 갈래로 독립 검증했다.

- **상관을 신호시점으로 더 좁혀 재계산**: 리포트의 `tautology.py`(burst=True 시점, |r|<0.024)에
  더해, **실제로 base 모드가 트레이드를 발화시키는 정확한 게이트 집합**(`burst & beta_pctile<=30`
  또는 `>=70`)으로 더 좁힌 독립 스크립트를 작성해 재계산했다 — corr(β,ΔOI%) 평균 **+0.0068**,
  종목별 |r| 최대 0.026(BNB)로 여전히 근접 0. 과거 라운드(밴드이탈 z-RSI 사례, 전 구간 상관은
  낮았으나 신호시점 상관이 0.928로 급등)와 같은 "구간을 좁히면 사실 같은 정보" 패턴이 이 스펙엔
  **재현되지 않음**을 확인했다.
- **4개 변형(base/gate_off/roc_gate/reverse)이 통계적으로 구분되지 않는다는 주장을 실제 검정**:
  리포트는 점추정치(0.937~0.973)를 나열만 했을 뿐 정식 검정은 하지 않았다. 리뷰어가 **일 단위
  블록 부트스트랩**(1,500회, 날짜를 복원추출해 재표집)으로 OOS gross(R) PF 의 95% 신뢰구간을
  산출:
  - base 0.937 [0.855, 1.028] / gate_off 0.972 [0.861, 1.084] / roc_gate 0.973 [0.888, 1.069] /
    reverse 0.962 [0.870, 1.062]
  - 쌍별 차이(base−gate_off, base−roc_gate, gate_off−roc_gate, base−reverse) 95% CI 가 전부
    0을 포함(P(diff>0) = 0.29~0.51) — **네 변형이 통계적으로 구분 불가하다는 리포트의 핵심 주장이
    독립 검정으로 확증됨**. "서로 다른 정보(상관≈0)인데 각자 독립적으로 무가치"라는, 리포트가
    스스로 "이 프로젝트에서 드문 결론"이라 표현한 해석은 타당하다.

### 대조군 공정성 + 표본 수 왜곡 확인

`strat.py`/`sweep.py` 코드 검토 결과 4개 변형이 **동일 `feat_cache`/`m15_cache`, 동일 확인·체결·
청산 엔진**을 쓰고 `gen_signals()` 의 게이트 조건 한 곳만 다름을 확인(공정한 대조군). 트레이드 수는
base 8,493 vs gate_off 11,721 vs roc_gate 10,697 vs reverse 8,492 로 최대 1.38배 차이(과거
124배 왜곡 사례와는 규모가 다름). **PF 비교가 전부 R-배수(위험 대비 수익률, `pnl/risk_amt`)
기준**이라 계좌 복리 경로에 의존하지 않는지 직접 점검했다 — 실제로 `gate_off`(BNB $4.95),
`roc_gate`(BTC $2.04, BNB $4.81) 등 **일부 종목의 경로별 계좌 잔액이 과거 Hikkake 사례와 유사한
수준까지 거의 소진**되는 구간이 있음을 발견했다(리포트에 서술되지 않은 부분). 다만 `r_multiple`은
매 트레이드 시점의 `risk_amt`(= 그 시점 equity × 1%)로 정규화된 값이라 계좌 소진 경로와 무관하게
등가중 비교가 성립하며, `common.py`를 거치지 않는 완전 독립 재계산(순수 파이썬 루프+scipy)으로
OOS PF_gross(R)=0.9373·PF_net(R)=0.5332·t=-16.96 을 그대로 재도출해 왜곡이 없음을 확인했다.

### 자체 발견 버그 처리

수정 전 코드(`+ pd.Timedelta` 직접 덧셈)를 되돌려 재현 → 즉시 `MergeError` 발생(주장과 일치).
수정본이 산출하는 시프트된 인덱스가 기대값(+1시간, ms 단위)과 정확히 일치함을 원소 단위로 대조
확인. 은폐형(조용히 신호가 줄어드는) 버그가 아니라 즉시 예외로 죽는 형태였다는 리포트 서술도
타당함을 확인했다(수정 전 코드로는애초에 `strat.py` 실행 자체가 불가능하므로 "조용한 오염 결과가
과거에 이미 나갔을 위험"은 없음).

### 리포트 서술 정정 (1건)

§데이터 절의 "`oi==0` 이 42~118건(전체 39,288~39,408 시간봉의 0.03~0.30%)"는 부정확하다.
`h1_cache`를 직접 재계산한 결과 **`oi==0` 실측은 BTC 42건, 나머지 6종목 16~18건**(0.041%~
0.107%)이고, NaN 10건을 더한 "dirty" 합계도 **26~52건(0.066%~0.132%)**로 상한(118건/0.30%)에
크게 못 미친다. 결측 처리(ffill) 로직 자체와 2024-07-09~15 군집 실재는 정상 확인됐고 이 정정이
FAIL 판정에 영향을 주지는 않으나, 리포트 본문의 수치는 위 실측값으로 교체돼야 한다.

### 직접 재확인한 것 요약

- `analyze.py`·`tautology.py`·`diag_sign.py`·`lookahead_audit.py`·`sweep.py`(5개 변형) 전량
  재실행 → 리포트 표의 모든 수치와 완전 일치(회계정합·LOO·top-N·무비용진단·대안IS/OOS·
  de-clustering·상관행렬·신호빈도 포함).
- `common.py` 미사용 완전 독립 경로(순수 루프 PF + scipy `ttest_1samp`)로 OOS PF_gross(R)/
  PF_net(R)/t 를 재도출 → 리포트값과 완전 일치.
- 신호시점(실제 게이트 통과 집합)으로 좁힌 β-ΔOI% 상관 신규 계산 → 여전히 |r|<0.03, 리포트의
  "재포장 아님" 결론 보강.
- 4개 핵심 대조군의 OOS gross(R) 에 대한 day-block 부트스트랩 신뢰구간·쌍별 차이 검정 신규 수행
  → "통계적으로 구분 불가"라는 리포트 주장을 정식 검정으로 확증.
- ms/us MergeError 버그를 원복해 재현, 수정본의 정확성을 값 단위로 대조.
- 룩어헤드 감사를 리포트 밖 3개 종목·3개 절단점으로 확장 재실행 → 전부 일치, mismatch 0.
- OI-klines 조인을 리포트가 확인하지 않은 제3의 시점(ETHUSDT 2023-08-01 12:00)에서 수기 검산.
- **한계**: `data.binance.vision` metrics 덤프의 재다운로드 시 bit 단위 재현 비보장(과거 확인된
  파이프라인 특성) — 이번엔 기존 캐시를 그대로 재사용해 확인했으므로 "이 캐시로 이 결과가
  나온다"는 확인이지 "덤프를 다시 받아도 완전히 같은 트레이드가 나온다"는 보장은 아니다(과거
  사례상 방향성·유의성 결론은 통상 불변이었음).
