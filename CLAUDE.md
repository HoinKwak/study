# CLAUDE.md — 행동 규약 & 프로젝트 가이드

이 파일은 세션·대화 압축(compaction)과 무관하게 **항상** 지켜야 하는 규약이다.
대화가 압축되어 맥락이 리셋되어도 이 파일의 규칙은 그대로 적용된다.

---

## 📌 공지 (세션 시작 시 항상 먼저 확인·안내)

> 이 블록은 세션 시작(또는 압축 후 이어받기) 때 **가장 먼저 읽고**, 내용이 있으면
> 사장님께 답변 상단에 짧게 안내한다. 처리되면 해당 줄을 지운다. 비어 있으면 안내 생략.

_(현재 공지 없음)_

## 🧭 진행상황 스냅샷 (Live — 압축돼도 잃지 않게 주기적으로 갱신)

> 큰 작업 시작 전·컨텍스트 70% 초과 시·상태가 바뀔 때 이 블록을 최신화해 커밋한다.
> 이어받을 때 여기부터 읽으면 "지금 무엇을 어디까지 했는지" 한눈에 파악된다.
> (마지막 갱신: 2026-09-05, 9/5 00:30Z 발굴 라운드 및 09:00Z 정기루틴까지 반영)

- **현재 모드**: 상시 세션에서 **정기 리서치 루틴 4종**을 발화받아 서브에이전트로 수행 → 검증 → 커밋·push 반복.
  - kol-watch(온체인 트렌딩 조기경보) 2h · futures-scout(선물 리서치) 2h · market-brief(시장 브리핑) 12h · strategy-scout→backtester→**backtest-reviewer**(전략 발굴+백테스트) 12h.
  - 매 사이클 공통 절차: `git pull` → `date -u`로 UTC 확보 → 서브에이전트에 그 UTC 명시(에이전트는 Bash 없어 날짜 환각 방지) → 산출물 스키마/ts/렌더 검증 → 파일경로별 `git add` → 한글 커밋(푸터는 현재 세션에 지정된 `Co-Authored-By`/`Claude-Session` 두 줄을 그대로 사용 — 모델명을 이 파일에 박아두면 세션이 바뀔 때 낡은 값을 쓰게 된다) → push.
  - 검증 포인트: watch.json `ts/tokens/notable` 스키마·ts 일치·대시보드 렌더 스모크테스트; futures brief 주식화토큰 제외 스캔; ETF flows 과거이력 prefix보존·신규거래일만 append; chartist_views 인물 6인 고정.
- ⚠️**컨테이너 재시작 사고(2026-09-03)**: 작업트리가 `c342fcf Initial commit`(README만)으로 리셋되고 `/tmp` 스크래치패드가 통째로 비워졌다. 저장소는 `git fetch origin <브랜치>` + `reset --hard FETCH_HEAD`로 완전 복구했고 **커밋된 산출물은 하나도 잃지 않았다**. 그러나 **정기루틴 파이프라인 스크립트 12종과 `prices_prev.json`·`kol_pools.json`이 영구 유실**됐다 — 저장소에 없고 스크래치패드에만 있었기 때문이다(`research/.gitignore`가 `impl/`을 제외해 백테스트 구현체가 재현 불가였던 것과 **같은 구조적 취약점**). → **재발 방지: 파이프라인 도구는 전부 `research/tools/`에 두고 커밋한다.** 현재 복구·커밋된 것: `fetch_all.sh`(선물 9개 피드 수집) · `build_digest.py`(선물 다이제스트) · `kol_pre.py`(온체인 계측) · `kol_digest.py`(온체인 다이제스트) · `kol_tokens.json`(42종 CA·기준선) · 검증기 3종(`kol_check.py`·`kol_verify.py`·`mb_check.py`·`futures_check.py`). 아직 미복구: 선물 `merge_patch.py`/`verify_merge.py`/`publish_brief.py`(현재는 부모가 직접 머지·발행).
- ⚠️**기준선을 산문에서 되읽지 않는다(9/3 확립)**: 유실된 `kol_digest.py`는 직전 thesis의 "유동성 $A→$B"에서 A(2회차 전 값)를 잡아 40종의 Δ가 누적되던 사고가 있었다. 재작성분은 **`kol_tokens.json`에 실측 기준선을 기계적으로 보관**하고 매 회차 실측으로 갱신한다 — 발행된 산문은 더 이상 입력이 아니다.
- ⚠️**선물 수집기 결함 자체발견(9/3)**: CoinGecko 티커에서 `BTCUSDT`·`BTCUSDC`·`BTCUSD_PERP`(COIN-M)가 **전부 심볼 `BTC`로 정규화돼 (심볼,벤뉴) 중복 행이 42~51건** 생겼다. 16:30Z 발행본이 그 상태로 나갔고(정식 USDT 값과 COIN-M 값이 공존), 발행본에서 저거래대금 중복 7행을 제거하고 `build_digest.py`에 **(벤뉴,심볼)별 최대 거래대금 계약만 남기는 중복 제거**를 넣었다. **신규 규칙: base로만 정규화하는 매칭은 분기물뿐 아니라 견적통화·COIN-M 중복도 만든다.**
- ⚠️**이월 서사가 chg24 숫자를 낡은 채로 나른다(9/3 18:30Z 적발)**: 에이전트가 다이제스트 `직전 why` 칼럼의 chg24를 이번 회차 값으로 인용해 **ZEC(-2.44% → 실제 +18.21%)·XPL(-4.04% → +16.13%)·BEAT(-8.89% → -6.57%)** 3건이 발행 직전까지 갔다. ZEC는 수치만이 아니라 "24h 창에 이전 급락분이 남아있다"는 **해석 자체가 반대**였다. 원인은 `build_digest.py`의 폐기 규칙이 **실측 부호만** 보고 chg24를 안 봤던 것 → 이월 서사가 chg24를 명시하면 이번 값과 대조해 폐기하도록 추가했고, **이번 회차 기준 59건이 추가로 걸렸다**(그동안 같은 유형이 상당수 지나갔을 가능성). ⚠️**부작용: 폐기가 늘면 빈 서사도 늘고 오버라이드 상한 25건으론 다 못 채운다 — 상한/문턱 조정은 사장님 결정 대기.**
- ⚠️**futures_check 방향 검사가 잘못된 기준을 쓰고 있었다(9/3)**: chg24로 판정해 **8건 전부 오탐**이었다(ZEC chg24 +18.6% vs 실측 +2.12%처럼 둘이 자주 어긋난다). 브리핑의 주 근거는 **실측%**이므로 `prices_prev/cur` 스냅샷에서 실측%를 직접 계산해 대조하고, 문장이 chg24를 명시할 때만 chg24를 쓴다. 그러고 나서야 ZEC 한 건이 진짜 오류로 남았다.
- ⚠️**OKX 수집 결함 2건(9/3)**: ①펀딩 조회 대상 60종목을 `volCcy24h`(**코인 수량**)로 정렬해 SATS·PEPE·SHIB가 상위를 채우고 **BTC·ETH가 빠져 있었다** → `last`를 곱한 달러 기준 정렬 ②OKX 행의 **OI가 항상 비어 있었다**(oi_map을 만들고 쓰지 않음) → `oiUsd` 병합. **거래대금 정렬 함정이 펀딩 선정에도 그대로 있었다는 점이 교훈.**
- ⚠️**XRP ETF flows 원천 불가 확인(9/3)**: SoSoValue openapi v2가 `us-xrp-spot`에 계속 **빈 배열**을 반환한다. 이름 변형 3종(`us-xrp-spot-etf`·`xrp-spot`·`us-xrp`)도 전부 빈 배열이고 **같은 호출로 BTC·SOL은 정상**이다. 이 소스로는 복구 불가 → 시장브리핑에서 '갱신 실패' 표기 유지.
- ⚠️**검증기 오탐 2건 즉시 수정(9/3)**: ①`futures_check`의 심볼 경계 `\b`가 **한글 조사에서 성립하지 않아**("BTC는"의 `C`와 `는`이 둘 다 `\w`) 방향 검사가 통째로 무력화돼 있었다 → `(?<![A-Za-z0-9])…(?![A-Za-z0-9])`로 교체 ②펀딩 대조의 절대허용오차 0.02가 **펀딩 값(0.01% 수준)보다 커서** 3배 왜곡을 통과시켰다 → 필드별 허용오차(펀딩 0.0005) 분리. 두 건 다 **주입 테스트로 확인**했다.
- ⚠️**수집 성공 판정은 파싱만으로 부족하다(9/3 20:30Z)**: CoinGecko가 Aster 피드에 **429 본문**을 반환했는데 그것도 유효한 JSON이라 수집기가 "OK"로 통과시켰고, **Aster 60종목이 통째로 빠진 채**(DEX 203→143행) 다이제스트가 만들어졌다. 하마터면 '벤뉴 이탈'이라는 시장 현상으로 보고될 뻔했다(원자료 먼저 확인 규칙이 잡았다). → `fetch_all.sh`의 `_get`에 **기대 필드 인자**를 추가해 응답에 실제 데이터가 있는지 확인하고 없으면 5·10·15·20초 백오프 재시도한다. ⚠️그 수정 중 **GET 호출에서 POST 데이터 자리에 기대 필드를 넣는 인자 실수**로 두 번째 실행이 dydx 파일을 손상시켰다 — 바로 발견해 전 피드 재수신했고 발행본 영향은 없었다.
- ⚠️**kol_pre 회차 카운터 결함(9/3 19:00Z)**: 기준선 복원 때 **회차수가 "미확인"이던 종목을 0으로 간주해 +1**하는 바람에 FLUSH·Truth Coin에 "2회차"라는 없는 이력이 생겼다. 에이전트가 시장 사실로 단정하지 않고 "카운터 재기준으로 보인다"고 정직히 기록해 잡혔다. 미확인은 미확인으로 유지하도록 고치고 기준선·발행본을 되돌렸다.
- ⚠️**검증기 오탐 3건 추가 수정(9/3)**: ①`kol_verify` 최상급 주어를 목록 라벨("뒷북 대형 토큰")로 잡아 오탐 → **표현 직전에 나온 토큰명**을 주어로 삼는 방식으로 교체했고, 그 과정에서 예전 방식("정답 토큰이 블록 어딘가에 있으면 통과")이 **진짜 오귀속을 놓치고 있었다**는 것도 드러나 함께 해소 ②`futures_check`의 "직전 …" 절이 **소수점에서 끊겨** 과거 서술의 방향어("직전 -11.10% 급락")가 살아남아 오탐 → 숫자 앞 점을 절 안에 포함 ③한 문장에 실측과 chg24가 함께 나오면 방향어는 실측에 붙는데 chg24로 판정 → **실측이 없을 때만** chg24 기준. 전부 양방향(정상 통과·오류 검출) 주입 테스트로 확인했다.
- **파이프라인 복구 완료(9/3)**: `research/tools/`에 `fetch_all.sh`·`build_digest.py`·`merge_patch.py`·`verify_merge.py`·`publish_brief.py`·`kol_pre.py`·`kol_digest.py`·`kol_tokens.json`·검증기 4종(`kol_check`·`kol_verify`·`futures_check`·`mb_check`)이 전부 커밋돼 있다. `merge_patch.py`는 **인자 순서가 뒤바뀌면 파일 내용으로 판별해 거부**한다(과거 market·themes가 직전 회차 값으로 덮여쓴 사고의 재발 방지).
- **9/5 00:30Z 발굴 라운드(스펙 5종 · 백테스트 2종 — 둘 다 FAIL)**: ⚠️**이 라운드 최대 사고는 내 것이다 — 스펙을 커밋하기 전에 워크트리를 띄웠다.** 백테스터는 저장소에서 스펙 파일을 찾고 없으면 발주 프롬프트를 근거로 **스펙을 사후 재작성**하는데, 두 건 다 그렇게 됐다. Abdi-Ranaldo는 재작성분이 원본보다 오히려 엄격해 라벨이 불변이었지만, **OI HHI는 재작성분이 원본과 근본적으로 다른 설계**였다(HHI 유니버스 동적 20종목→고정 7종목 · 정규화 365일 백분위→60일 z-score · 추세신호 BTC 4h EMA20/50 크로스→1d 종가/EMA50 · 집중레짐 전환 청산 50% 축소→전량 · 스톱 ATR(4h,20)×2.5→ATR(4h,14)×2.0+트레일×3.0). '같은 아이디어의 다른 파라미터'가 아니라 별도 설계라서 **원본이 사전 등록한 가설이 검증되지 않았고**, 리뷰어가 그 이유로 SUSPECT를 냈다 → 부모가 **원본 설계 초점 진단**(`oi-hhi-original-design-tautology-check.md`)을 따로 돌려 해소했다. **신규 규칙: 워크트리를 띄우기 전에 스펙을 반드시 커밋할 것.**
  - **OI 횡단면 허핀달 집중도 브레드스 + BTC 추세추종[스윙·1d/4h] — 판단불가→FAIL(리뷰어 SUSPECT, 초점 진단으로 해소)**: 파라미터 10개 조합 전부 OOS 6~17건으로 사전 등록 최소치 20건 미달이라 1차 라벨은 판단불가였으나 보조 증거가 전부 FAIL 방향이었다. ⭐**핵심은 `hhi ⊆ btcshare` 100% 부분집합** — HHI 게이트가 고른 24건이 훨씬 단순한 대조군②(BTC 자기 OI 점유율)의 완전한 부분집합이고 상관도 전체구간 0.8748·트리거시점 0.8935다. OOS t는 전 조합 <1.96(최대 1.630), 승률고정 대안검정 51.0 백분위, top-2 제거 시 1.240 붕괴. **초점 진단 결과 원본 설계(동적 20종목·365일 백분위)에서도 전체구간 r=0.8685·트리거시점 0.8232로 사전 폐기조건 (b)의 0.8을 넘겨** 원본 기준으로도 FAIL이 확정됐다. ⚠️**정직 표기: raw HHI 기준이면 0.7971/0.7197로 0.8 미만**이라 백분위/raw 중 무엇을 기준 삼느냐가 판정을 가른다 — 스펙 문언이 백분위 정규화를 명시하므로 백분위를 채택했다. **신규 규칙: BTC가 유니버스 명목가치를 압도하는 구조에서 '횡단면 집중도(HHI)'류 게이트는 정의상 BTC 자기 지표의 재서술이 되기 쉽다 — 착수 전 BTC 자기 지표와의 상관·부분집합 관계를 최우선 점검할 것.** 긍정 확인: 룩어헤드 없음(7종목 절단 817표본 완전 일치), 반전 대조군 버그 없음(zero_hold 4%), gross/net 갭 미미.
  - **Abdi-Ranaldo 고저-종가 유효스프레드 유동성 레짐[단타·15m/1h] — FAIL·리뷰어 VALID**: OOS net PF(R) 0.699이고 **gross 0.957로 비용 이전부터 무엣지**. 원본 스펙의 사전 폐기조건에 대입해도 (b) net<1.15 · (c) gross<1.08 둘 다 큰 여유로 충족한다. 대조군②(단순 ATR z게이트)·③(레짐반전)이 점추정상 기본안과 동등하거나 우수 → **AR 스프레드가 ATR%·RV·HL range·CS와 상관이 낮아 정보로는 독립적인데 그 독립 정보에 실전 가치가 없는 네 번째 사례**이고, 스펙이 주장한 '저스프레드=모멘텀 유리/고스프레드=평균회귀 유리' 방향 자체도 지지되지 않았다. 자체발견 버그: 대조군③의 조기청산 임계가 원신호 기준으로 고정돼 진입 직후 88.3% 즉시청산. ⚠️**리뷰어 정정 3건**: ①⭐**진입 수수료가 pnl에 미반영**돼 net의 실제 비용가정이 0.14%가 아니라 약 0.09%였다 — 수정 시 OOS 0.699→0.595(t=-9.187)로 **FAIL이 더 강해진다**. **직전 라운드(테이커매수 명목/OI)에 이어 두 라운드 연속 같은 버그**다 ②대조군③ 관련 서술을 '전제 기각'에서 '전제를 지지하는 증거가 없다'로 완화(Welch p=0.164 비유의이고 원본 스펙엔 이 대조군의 폐기 기준 자체가 없다) ③**구현 공식이 원본 스펙의 분산분해 방식과도 다른 제3의 단순화**(Roll식 계열공분산)라 검증 대상이 '진짜 AR 추정치'가 아님을 §3.9에 명시. ⚠️**한계: Abdi & Ranaldo(2017) 원문이 페이월이라 리뷰어도 접근 불가** — 공식의 진짜 정확성은 이 감사로도 확정할 수 없다(SSRN·ResearchGate·T&F 전부 403). ⭐리뷰어가 재현 스크립트 6종 전량을 캐시 없이 원본 CSV부터 재빌드해 bit 단위 일치시켰고, 자체발견 버그도 버그판을 되돌려 재현했다.
  - 신규 스펙 5종(OI 횡단면 허핀달 집중도 브레드스·Abdi-Ranaldo 유효스프레드 레짐·방향변화 내재시간 오버슛 모멘텀·실현변동성 시그니처플롯 미시구조노이즈 레짐·COIN-M/USDT-M 펀딩스프레드 지속성).
- **9/4 12:30Z 발굴 라운드(스펙 5종 · 백테스트 2종 — 둘 다 FAIL·리뷰어 VALID)**: OI AR(1) 반감기 레짐 브레이크아웃[단타·15m/1h](OOS net PF(R) 0.717·t=-10.341이고 **gross 1.048도 사전 등록 폐기조건 (d)의 임계 1.05에 미달**[t=+1.353 비유의]이라 비용 이전부터 무엣지. 스윕 17/17·LOO 7/7 수렴. **대조군② OI z-score 게이트와 대조군③ φ 낮은[반대 방향] 레짐이 둘 다 기본안을 점추정상 상회**해 스펙의 두 핵심 가설이 지지받지 못했다. φ는 OI 변화 stdev·ROC·z와 |r|<0.2로 **재포장이 아닌데도 무가치했던 세 번째 사례**. 백테스터 자체발견 버그 2건[반전 대조군이 방향의존 절대 중간선을 재사용해 zero_hold 99.97%·PF>1377 / `donchian_lb`가 캐시된 신호에 도달 못하는 데드 파라미터]. ⚠️**리뷰어 정정 4건**: ①"두 핵심 가설 **직접 반증**"은 과장 — 우열 차이가 전부 비유의하다[phi_hi vs oiz_hi Welch p=0.194, 중복 보정 0.063, phi_hi vs phi_lo p=0.577] → "우위를 보이지 못했다"로 완화 ②**`base ⊆ pool` 중복 99.08%**를 진단 스크립트가 이미 계산해뒀는데 본문에 없었다 → 잔여표본 독립 Welch[t=+0.848, p=0.399]로 (b) 결론 보강 ③top-N 기준값 0.712→baseline 0.717 ④⭐**"§8 스윕에서 설계판단을 점검했다"가 사실과 달랐다** — 스윕은 EMA 임계값만 다뤘고 **φ 해상도[15m vs 1h] 대안은 구현조차 안 됐다**. 리뷰어가 문자 그대로의 대안[1h φ → causal 15m 매핑]을 직접 실행하니 net 0.743·t=-9.354로 불변이나 **gross 1.087로 (d) 임계를 넘어** (d) 트리거가 이 미검증 설계판단에 민감함이 드러났다. **신규 규칙: "설계판단을 스윕에서 점검했다"고 쓸 땐 그 스윕이 실제로 그 판단의 대안을 돌렸는지 확인할 것 — 인접 파라미터 스윕은 해석 대안의 검증이 아니다.** ⭐리뷰어가 캐시 없이 BTC 신호를 원본부터 재빌드해 OOS 1,270건 bit 단위 일치시켰고 버그 2건도 버그판을 되돌려 재현, 리포트 미실시였던 BNB·SOL·ADA 절단검증 추가) · 테이커매수 명목/OI 비율 소진 페이드[단타·15m/1h](OOS net PF(R) 0.341·t=-29.05, **gross 0.830으로 사전 등록 폐기조건 (d) 'gross PF<1.10 즉시 폐기'에 정면 해당**. ⭐**동어반복이 핵심 사인** — z_buy가 **트리거 시점 캔들 거래량 z와 0.935~0.957**이라 사실상 같은 정보이고 정작 `taker_buy_frac`과는 트리거 시점 -0.08~0.03으로 무상관이라, '매수 유량 소진'이라는 방향 서사가 데이터로 뒷받침되지 않는다[거래대금 급증 봉을 잡고 있을 뿐]. 스윕 26/26·LOO 7/7·종목별 7/7·캘린더일 de-clustering[net t=-18.586, gross t=-3.128] 전부 수렴. 스카우트 최우선 의심점[결합조건 배타성]은 실측/독립기대 0.69~0.79로 기각됐으나 대신 **신호빈도가 정규근사의 18~20배**라는 다른 형태의 정의 취약점이 드러났다. 자체발견: 진입 수수료가 pnl에 미반영[수정 후 결과가 더 나빠짐]. ⚠️**리뷰어 정정 1건 — 삼백병 건과 같은 함정의 재발**: 부호 무작위화 나이브 50/50 셔플이 **승률 50%를 암묵적 귀무가설로 깔아** 실제 승률 35.5%/37.6%인 이 전략에선 t검정과 동어반복이다. **승률 고정 대안 검정에서 net은 0백분위[승률을 고정해도 실제 페어링이 무작위보다 유의하게 나쁨 — 리포트보다 더 강한 신규 발견]인데 gross는 100백분위로 반전**된다 → "양방향 모두 무정보"가 아니라 **"비용 이전엔 미약한 방향 정보가 실재하나 비용·청산구조와 결합하며 net에서 역전"**이 정확하다. FAIL은 (d)의 절대 수치로 이미 확정이라 불변. ⭐리뷰어가 재현 스크립트 10종 전량 bit 단위 재현[재현성 최상위권]하고 진입수수료 버그판도 되돌려 OOS 0.463·t=-22.18 정확 재현). 신규 스펙 5종(앵커드VWAP 기울기 레짐 게이트·OI AR(1) 반감기·프리미엄인덱스 정산후 재수렴 반감기·BTC나스닥 상관 불안정 게이트·테이커매수 명목/OI 비율). ⚠️**공통 교훈: 두 건 다 gross부터 무엣지이고, 리뷰어 정정 5건 중 3건이 "리포트가 자기 검증 범위를 실제보다 넓게 서술한 것"이었다** — 검증했다고 쓴 것이 정말 그 대상을 검증했는지 리포트 단계에서 대조할 필요가 있다.
- **9/4 00:15Z 발굴 라운드(스펙 5종 · 백테스트 2종 — 둘 다 FAIL·리뷰어 VALID)**: OI 변동성 대 가격 실현변동성 비율 극단 페이드[단타·15m/1h](OOS net PF(R) 0.687·t=-13.7이고 **gross(비용 0)부터 무엣지** — 페이드·모멘텀 양방향 모두 0.974~1.010·|t|<1이라 비용 잠식이 아니라 애초에 방향 정보가 없다. 스윕 17변형·LOO 7/7·종목별 7/7·캘린더일 de-clustering[net t=-7.475 / gross t=-0.127] 전부 수렴. 백테스터 자체발견: **1h klines[ms]와 metrics OI[us]를 join하면 인덱스가 조용히 us로 승격**돼 z-회귀 청산이 구조적으로 발화 못하던 버그 → ns 명시로 수정. ⚠️**리포트 정정**: 그 버그의 영향을 "OOS PF 0.606→0.687로 소폭 이동"이라 적었으나 리뷰어가 **ns 통일 3곳을 전부 되돌린 버그판으로 처음부터 재실행**하니 **0.687 → 0.687로 사실상 동일**했다[z_reversion 청산 BTC 0→10건은 정확히 재현]. `0.606`은 디버깅 중간 상태 값을 잘못 인용한 것으로 보여 철회. ⭐리뷰어가 재현 스크립트 **전량을 bit 단위로 일치 재현**했고 리포트 미실시였던 **ETH·XRP·DOGE 3종목 추가 절단검증**으로 룩어헤드가 BTC 한정이 아님을 독립 확인) · 알트 OI성장률-BTC가격 베타 잔차 다이버전스 로테이션[스윙·4h/1d](OOS n=222 PF(R) 1.223·t=+1.173[p=0.242]로 통과선 미달, gross부터 비유의[t=+1.548]. **3~5일 롤링 de-clustering에서 t가 +1.17 → -0.18~+0.05로 소멸**. ⭐**스펙의 핵심 차별점인 회귀 베타 단계가 무가치함이 실증** — 잔차 대신 원 OI성장률 z를 쓴 대조군과 구분 불가[부트스트랩 55.9 백분위]. 스카우트가 **스펙 단계에서 자인한 최대 의심점**[BTC-ETH OI성장률 상관 0.129로 약해 잔차가 노이즈일 위험]이 그대로 실증된 첫 사례다. ⚠️**리뷰어 보강 3건**: ①이 대조군 비교에 **`base ⊆ pool`이 실재**[222건 중 199건=89.6% 진입시각 일치, z_resid와 z_oi_raw 상관 0.952~0.984 — 베타가 거의 0이라 잔차≈원계열] → 중복 없는 독립 Welch[t=-0.288, p=0.775]로도 결론 불변·오히려 강화 ②**리포트가 놓친 매크로 클러스터 극단 집중을 신규 규명**: 5일 롤링 클러스터 47개 중 **상위 3개[26건=11.7%]가 OOS 순 R의 103.9%**를 차지하고 제외 시 나머지 196건이 PF 0.991·t=-0.053으로 완전한 잡음. 최상위 2건이 **2025-07-10~14 BTC $108K→$120K 돌파**[요일조건부 ΔOI% 감사에서 독립 기록된 것과 동일 사건]와 **2025-01-12~15 $94.5K→$100K 돌파**라 "저빈도 로테이션 엣지"가 아니라 **BTC 매크로 랠리 2건이 6개 상관 알트에 복제된 결과** ③리포트가 gross는 캘린더일까지만 계산한 것을 **gross 3~5일 롤링까지 추가**[t=+0.404/+0.237/+0.125]해 동일 붕괴 확인. ⚠️**정정**: §4.4의 "방향에 어느 정도 정보가 있다[반전 p=0.064 경계적]"는 **제3의 대안 실행**[z_reversion 청산 완전 비활성]에서 **p 0.064→0.327로 약화**돼 청산조건 정의에 민감함이 드러나 철회. ⚠️**재현성 공백**: "55.9 백분위"가 저장된 스크립트에 해당 호출이 없어 재현 스크립트만으로는 재현 불가였고 리뷰어가 함수를 직접 재구성해 55.92를 얻어 검증했다). ⚠️**인프라**: 격리 워크트리가 README만 있는 빈 브랜치로 시작되는 문제가 또 재현됐다[백테스터가 `git fetch`+`reset --hard`로 복구, 로컬 전용 브랜치라 오염 없음].
- **미션 #28 현황(9/5 09:00Z 갱신)**: 스펙 **550개** · 백테스트 **248개**(초점 진단 1건 포함) · **PASS 여전히 0건**이고 `scalp15m`이 유일한 라이브 전략이다. 8/27 라운드에서 스펙 5종을 추가하고(음력설 유동성공백·반월급여일·서머타임 전환 불일치·실현변동성 기간구조·펀딩 횡단면 상대순위) 백테스트 2종을 돌렸는데 **둘 다 FAIL·리뷰어 VALID**였다 — 1시간 모원봉 15분 되돌림[단타·1h/15m] (OOS n=2,077 net 0.632·t=-9.52, **월별 24/24개월 순손실**) · 마크가격 레인지 합의괴리[단타·1h] (OOS n=1,294 gross 0.850/net 0.584, 폐기조건 (a)(c) 충족). ⚠️**신규 규칙 2건**: ①**방향성 조건으로 선택된 진입봉**에서는 반전 대조군의 스톱을 "그 방향의 자연스러운 극값"으로 재계산하면 안 된다 — 진입 확인봉이 원신호 방향으로 편향돼 있어 반전 쪽 스톱이 저절로 타이트해진다(반전 64.3%가 1봉 내 청산 vs 정방향 20.6%). 리스크 거리를 원신호 기준 1회 계산해 대칭 재배치할 것 ②**gross PF의 t값을 폐기조건 근거로 인용하면 net에 적용한 것과 동일한 de-clustering을 gross에도 병행**할 것(마크가격 건에서 gross t=-2.61이 de-clustering 후 -0.74~-0.98로 유의성이 소멸했다). ⭐한편 `base ⊆ pool` 검정력 쟁점은 백테스터가 이미 올바르게 처리하고 있었고(0건 중첩 잔여분만 비교), 제3의 대안 실행도 이번엔 결론을 뒤집지 않고 견고성을 보강했다.
- **9/5 12:06Z 발굴 라운드(스펙 5종 추가, 총 555개)**: 백테스트 없이 스펙만 발굴. COIN-M 인버스계약 방향별 OI-가격 탄력성 비대칭(컨벡시티 헤징 흔적)[스윙·1d/4h] · OI 변화 부호전환빈도(Zero-Crossing Rate) 확신도 게이트+Donchian 브레이크아웃[단타·15m/1h] · 체결건수-거래대금 로그탄력성(스케일링 지수) 이탈 게이트[단타·15m/1h] · 실현변동성의 변동성(Vol-of-Vol) 레짐 스위치[스윙·1d/4h] · 가격-OI 부호일치율(Co-Sign Agreement Rate) 게이트[스윙·4h/1d]. ⚠️**스카우트가 사전에 자인한 최우선 의심점(백테스트 착수 시 최우선 확인 필요)**: ①zero-crossing rate 건은 브레이크아웃 봉과 낮은 zcr이 독립이 아니라 같은 근본원인(방향성 거래량 급증)의 두 증상일 위험(체결건수 흡수 스퀴즈 건과 동일 함정) ②스케일링 지수 β는 평균체결규모 지표군과 트리거 시점 한정 상관이 높을 위험을 자인(사전 폐기조건 |r|>0.85) ③부호일치율(agree)은 기존 다수 피어슨 상관 게이트 스펙의 재포장일 위험을 자인(사전 폐기조건 |r|>0.85) ④Vol-of-Vol은 RV 레벨 자체와 상관이 높아 '고변동성 레짐'의 재포장일 위험 ⑤컨벡시티 건은 이론적 효과 크기가 노이즈보다 작을 위험. 5종 모두 결합확률·전제 일관성 실측을 스펙 단계에서 완료 못해 "백테스트 착수 시 최우선 확인"으로 정직히 명시함(일부만 검증 표기 원칙 준수).
- **미션 #28(진행중)**: **15m/1h VALID 전략 발굴**. 지금까지 발굴·백테스트한 다수 전략 **전부 FAIL**(backtest-reviewer로 재검증해 거짓 FAIL 아님 확인). **8/24 00시 라운드(2종 백테스트 — 전부 FAIL·리뷰어 VALID)**: 시간대 조건부 OI 가속 z-score[단타·1h/15m](OOS n=915 net PF(R) 0.669·t=-5.182. **gross(fee=0·slip=0)부터 전 구간 PF 1.04~1.05·|t|<1.0**이라 비용 잠식이 아니라 애초에 무엣지. LOO 7/7·top-N 5/5·캘린더일 de-clustering[450일, t=-4.415]·스윕 22/22 전부 수렴. ⭐**스펙 핵심 주장인 '시간대 조건화'가 24h 풀링 대조군과 통계적으로 구분 불가**[부트스트랩 p=0.659, 풀링이 근소 우세]. 리뷰어가 diag1~7 전량을 원본 캐시로 독립 재실행해 **bit 단위 일치**[재현성 최상위권 평가]하고, 나아가 **hod·pool 표본이 67.4% 겹침을 직접 계산**해 비중첩 표본만의 완전독립 Welch[p=0.4765]와 겹침분 페어드 t[p=0.225]를 신규 수행 — 둘 다 부가가치 없음 재확인. 리뷰어 추가검증: 룩어헤드 4종목 확장[7종목 전부 Δz=0], de-clustering **대칭점검**[최대손실 10/20일 제외해도 0.737/0.793], 승률고정 순열검정 독립 재구현[백분위 100.0]. ⚠️**정정 2건**: ①폐기조건 (a) 평가가 **스펙이 명시한 '캘린더일 de-clustering 기준' 대신 거래단위 수치를 인용**[스펙대로면 0.606·t=-4.415로 오히려 더 강하게 FAIL 지지] ②**zero_hold 68.2%는 `research/.gitignore`의 `impl/` 제외로 원본 버그코드가 미보존이라 정확한 재현 불가**[정성적 패턴만 재구성 확인]. ⭐**규칙 경계 확정: SL은 리스크관리 메커니즘이라 직전 이중VWAP 건의 '서사적 테제 무효화 → 원신호 기준' 예외에 해당하지 않고 표준 대칭 재배치가 적용된다**) · 요일 조건부 ΔOI% 서프라이즈 브레이크아웃[스윙·1d/4h](OOS n=105 net PF(R) 1.216이나 **t=0.867[p=0.388] 비유의**로 사전 등록 폐기조건 (a)의 OR 절 충족. top-5[4.8%] 제거만으로 1.015 손익분기 붕괴. **5일 롤링 클러스터 3개[16.2%]가 순 R의 85.5%**, 특히 **2025-07-10~14 BTC 신고가 돌파가 BTC/XRP/ADA/BNB에 복제돼 85.7% 단독 설명** — 두 라운드 전 확립한 '3~5일 롤링 재검사' 규칙이 실제 작동해 백테스터가 자체 포착. 스윕 14변형 전부 t<1.96. 자체발견·수정: 1d 신호를 4h에 naive merge_asof 매핑해 빠른 손절 시 하루 최대 3회 재발화[BTC 25→20건, 원신호 수와 정확 일치 — 리뷰어가 버그판을 직접 되돌려 25건 **bit 단위 재현**]. ⭐⭐**이 프로젝트에서 '제3의 대안 실행'이 결론을 실제로 바꾼 첫 사례** — 리포트가 긍정근거 ②로 든 '반전 대조군 유의 열세[0.580, t=-2.13]→방향에 실재 정보'가 **청산구조 아티팩트로 판명**됐다. 반전모드 청산의 88%[199/226]가 `breakout_invalidation`이고 **그중 91%가 진입 1봉 내 즉시청산**[정방향은 이 조건 **0건**으로 사실상 죽은 코드]. 리뷰어가 해당 청산 완전 비활성판을 구현·실행하니 **t=-2.13 → t=-0.180으로 소멸**[PF 0.964]. 코드 버그가 아니라 **반전+브레이크아웃+무효화의 상호작용이 만드는 구조적 아티팩트**. → 본문 3곳 철회·격하 정정. ⚠️**신규 규칙: `zero_hold_frac`이 이 엔진에선 구조상 항상 `holding_bars>=1`이라 트리비얼 지표이고, 이 아티팩트는 zero_hold_frac 점검으로 절대 드러나지 않는다 — 브레이크아웃 계열 반전 대조군은 청산사유·보유기간 분포를 별도로 볼 것**. ⭐반대로 긍정근거 ①[요일 조건화가 풀링 대조군 상회, 폐기조건 (d) 미충족]은 **리뷰어 독립 검증에서 강화** — 매크로 클러스터 제외 후에도 우위 유지·확대[1.031 vs 0.815, 3개 전부 제외 시 1.036 vs 0.758]라 단일 사건 인공물이 아님[단 클러스터 제외 시 wd의 **절대** 성과는 손익분기 PF≈1.03이라 '더 낫다'와 '그 자체로 수익성 있다'는 별개]. 리뷰어 신규 검증: **손실 클러스터 대칭점검**[최악 3개 제외해도 t=1.638로 미달], **꼬리 상관 정량화**[평시 0.060 → BTC 절대수익률 상위 5% 위기국면 0.116으로 2배 — 직전 OI펀딩 건 규칙과 방향 일관하나 완만], overlap 75.2% + Welch 독립 재계산, z_wd를 함수 미사용 순수 pandas로 5지점 손계산해 소수점 6자리 일치, **캐시 재사용 타당성**[직전 OI펀딩 라운드 캐시 재사용 — 기간·유니버스·필드 대조, 11,921개 파일 카운트 일치], de-clustering 3종[캘린더일·5일블록·주단위] 독립 재검정 전부 비유의. **축 평가: 실패 원인이 '요일 조건화 축이 무가치'가 아니라 'OI 필드 자체의 절대 엣지가 단일 사건 의존적'**). 신규 스펙 총 **432개**. ⚠️**인프라**: 두 백테스터가 scratchpad 디렉터리명(`wdoi`)이 우연히 겹쳐 한쪽이 '동일 스펙 중복 작업'으로 오인 보고했으나 실제로는 다른 스펙[모듈명만 `wdoi_common`/`wdoi_engine`]이었다 — 격리 조치 자체는 옳았음. **8/23 12시 라운드(2종 백테스트 — 전부 FAIL·리뷰어 VALID)**: 삼백병/삼흑아 3연속 추세지속[스윙·4h](OOS n=418 gross 0.700/net 0.627·t=-3.91로 비용 이전부터 무엣지. 16변형 중 15개 OOS<1, LOO 7/7, de-clustering 양방향[최고·최악 클러스터 제거] 전부 PF≪1. ⚠️**스펙의 사전 결합확률 실측이 "부분적으로만" 이뤄진 첫 사례** — 스펙이 BTC 4h 실측으로 모닝스타류 과잉제약을 배제했다고 주장했고 (a)~(d) 조건에 한해 실제로 맞았으나[리뷰어가 SMA ATR로 독립 재구현하니 14/7로 스펙 주장 15/8과 일치, Wilder로는 11/4], **꼬리필터 (e)를 포함하면 빈도가 88.4% 붕괴**[813→94건]해 모닝스타와 같은 규모의 과잉제약이 그대로 있었다. → **신규 규칙: 스펙 단계의 결합확률 실측은 최종 진입조건 전체를 대상으로 할 것, 일부만 검증했으면 그 사실을 명시**. ⭐리뷰어가 (e) 포함판을 직접 실행하니 OOS net 0.345로 제외판[0.627]보다 **오히려 더 나빠** 설계판단 편향 없음이 실증됐고, 스펙 각주에 "(e) 포함/제외 둘 다 돌려 붕괴 시 제외판 채택"이 명시돼 있어 스펙 위반도 아님을 확인. ⚠️**리포트 정정: 부호 무작위화 "0 백분위 = 방향 선택이 무작위보다 나쁨"은 과장** — 이 극단값은 **승률 34.4%<50%에서 오는 산술적 귀결**[부호 50/50 무작위화는 승률 50%를 암묵적 귀무가설로 깖]이고, **승률 고정 대안 검정에서는 92.9 백분위**로 특이점 없음. 리뷰어가 재현 커맨드 10개 전량 재실행해 소수점까지 일치시키고 "이 프로젝트 관측 재현성 중 최상위" 평가) · 거래대금가중 VWAP vs 체결건수가중 VWAP 괴리[단타·1h/4h](OOS n=2,162 gross 1.017/net 0.872·t=-2.87. 스윕 14변형 전부 net<1. **동어반복 점검 통과** — spread_z와 평균체결규모 상관 |r|<0.05, ATR·실현변동성 |r|<0.08이고 ⭐**리뷰어가 트리거 시점 한정 상관까지 독립 재계산**[프리미엄인덱스 건에서 전체구간 0.246이 트리거 시점만 보면 0.928이던 함정 경계] 7종목 전부 양쪽 |r|<0.13 → **'독립 정보였는데도 가치 없었던' 두 번째 사례**[GK/RS에 이어]. ⚠️**리포트 핵심 서술 정정: "방향정보는 실재하나 비용을 못 이긴다"가 의사반복 아티팩트** — 트레이드 단위 t=+2.798[p=0.0052]이 **캘린더일 단위 de-clustering에서 t=-0.933[p=0.351]로 소멸하고 부호까지 반전**[페어드 -0.676, 주단위 +1.642 비유의, gross 일단위 -1.670]. 8h 펀딩주기 건[t=2.86→0.55~1.71]과 동일 패턴. ⭐**누락 원인: 리포트가 3~5일 롤링 퇴화를 확인하고 캘린더일 단위를 건너뛴 채 곧바로 월별 분해로 넘어갔음**[OOS 고유일 637일이라 일 단위는 퇴화하지 않음] → **신규 규칙: 고빈도 전략에서 3~5일 롤링 de-clustering이 퇴화하면 월별로 가기 전에 캘린더일 단위를 반드시 시도할 것**. ⚠️**기존 규칙의 예외 확인**: "스프레드 무효화" 조기청산을 최종방향 기준으로 두니 반전 대조군 83.96%가 즉시청산 퇴화 → 이 청산은 SL/TP류가 아니라 **'테제 무효화'류라 원신호 기준이 맞음**[리뷰어가 수정 전 코드로 83.96% bit 단위 재현하고 **제3의 대안**[반전모드에서 해당 청산 완전 비활성]도 실행해 net 0.735/gross 0.855로 리포트 수정판 0.733/0.853과 사실상 동일 → 어느 해석에도 결론 불변]. ⭐리뷰어가 **신호빈도 2배 초과의 정량 원인을 신규 규명**: spread_z 실측 std가 **1.35~1.38**로 정규가정 1.0과 달라 등가 임계 1.10에서 확률이 13.3~13.9%[스펙 나이브 근사 6.7%의 정확히 2배]. 자기상관 탓에 60봉 트레일링 std가 분산 변화를 후행하는 구조적 특성). 신규 스펙 총 **427개**(삼백병삼흑아·크로스섹셔널 볼린저밴드폭 스퀴즈 랭크·크로스섹셔널 체결건수 서지 랭크·펀딩비 평균회귀 반감기 AR(1) 게이트·이중VWAP 괴리). ⚠️**시장브리핑 루틴 신규 함정**: 에이전트가 **flows.json에 "XRP 키가 없고 XRP가 SOL 키에 잘못 누적됐다"는 구조적 결함을 보고했으나 부모가 파싱해 확인하니 사실이 아니었다**[BTC·ETH·SOL·XRP 4키 정상, SOL 8/21=10.07·XRP 8/21=18.38로 note 기재와 일치]. 32KB source 필드가 든 대용량 파일을 부분 열람하다 두 키를 뒤바꿔 읽은 것으로 판단. 다만 **불확실할 때 append를 보류한 판단 자체는 규약에 부합**했고 파일 무변경이라 손상 없음 → **신규 규칙: 대용량 JSON의 구조 이상을 주장하기 전에 파싱 결과로 교차 확인할 것**. ⚠️**인프라**: 격리 워크트리가 README만 있는 빈 브랜치로 시작되는 문제가 **2건 연속 재현**[백테스터들이 `git checkout origin/<브랜치> -- .` 또는 `reset --hard`로 복원해 작업]. 리뷰어 2명이 각각 확인한 결과 해당 워크트리 브랜치는 **원격에 없는 로컬 전용**이라 공유 이력 오염은 없었다. **8/22~8/23 라운드(4종 백테스트 — 3 FAIL·1 판단불가, 전부 리뷰어 VALID)**: 변동성 브레드스 동시확장 브레이크아웃[단타](FAIL. ⚠️**리뷰어 핵심발견: 청산 구조가 라이브 `scalp15m`과 근본적으로 다름** — 트레일링 활성화 시점·100% vs 50% 러너·1h/4h vs 15m/1h 전부 상이 → **이 결과는 라이브 전략 검증에 어떤 함의도 없고**, '라이브에 게이트를 추가하면 개선되는가'는 이 실험으로 테스트되지 않았다. ⚠️**신규 규칙: "라이브 전략과 동일 계열"이라 주장하려면 청산 구조까지 코드로 대조할 것**[`strategy/scalp.py`·`portfolio/worker.py`]) · 8h 펀딩주기 네이티브캔들 바디 모멘텀[스윙](3변형 전부 OOS net PF<1.3. ⚠️리뷰어가 '무비용 gross t=2.86'이 **의사반복**임을 규명[de-clustering 후 t=0.55~1.71]. **8h 네이티브캔들 open_time이 정확히 [0,8,16] UTC라 펀딩 정산과 정렬**됨은 실측 확인[SOL 2022년 2시간 펀딩주기 예외 있음]) · GK/RS 드리프트-분산 추세순도 레짐 게이트+EMA20/50[스윙·4h](**'재포장이 아닌데도 가치가 없었던' 사례** — GK-RS와 ADX·Choppiness·EMA기울기·실현변동성 상관이 7종목 전부 **|r|<0.42**로 진짜 독립 정보였는데 그 독립 정보에 실전 가치가 없었다. OOS n=232 gross 0.936/net 0.852·t=-0.946, IS도 gross 0.809로 비용 이전부터 무엣지. 대조군 4종 부트스트랩 전부 비유의[p>0.4], LOO 7/7·스윕 13/13 수렴, 반전도 <1. ⭐**리뷰어가 '1d 확인TF 미사용' 설계판단의 편향 여부를 대안 실행으로 검증**: 스펙이 '1d 레짐 확인'을 언급하나 진입/청산/파라미터 어디에도 사용처가 없어 4h 단독 구현한 것인데, 리뷰어가 1d purity+causal merge_asof 게이트를 직접 구현하니 OOS net PF **1.198로 컷오프 상회**하지만 t=+0.600 비유의라 여전히 FAIL이고, **IS 0.876 손실·XRP 단일종목 의존[XRP 순익 2,259 > 전체 순익 1,731이라 나머지 6종목 순손실]·top-1 제거만으로 1.005 붕괴**로 4h 단독판보다 **더 취약** → 설계판단이 결과를 유리하게 만들지 않았음이 실증. 리뷰어가 리포트 미실시 BNB/SOL/DOGE 절단 재실행으로 7종목 전부 룩어헤드 부재 확증하고, **손실클러스터 제거 대칭점검**[3일창 -549.1·5일창 -638.5 제외해도 net 0.903/0.912]으로 FAIL이 특정 클러스터 탓에 과하게 매겨진 것이 아님도 확인. 정정 2건: ①붕괴청산이 '원신호 방향 기준'이라는 인과 설명이 부정확 — 해당 분기는 **방향 변수를 아예 참조하지 않는 스칼라 조건**이라 퇴화가 구조적으로 불가능한 것 ②셔플 80백분위에 단측 p≈0.20 병기. ⚠️**리뷰어 본인이 1d 검증 스크립트에서 `.index + pd.Timedelta(days=1)`의 ms→us 업캐스트 함정에 다시 걸림** — 1d↔4h 정렬을 새로 쓰는 코드는 여전히 사정권) · 불마켓 서포트밴드 20주SMA+21주EMA[스윙](**판단불가** — 7종목 합산 FULL n=9[BTC 3·ETH 3·BNB 1·SOL 1·ADA 1·XRP 0·DOGE 0], OOS n=3으로 사전등록 폐기조건 [c] 15건 미달. 스펙 예상[종목당 15~35건]의 4~9%. 원인은 데이터 결손이 아니라 **정의 과잉제약**: 레짐확정[SMA20w·EMA21w 각각 4주 상승 동시 요구]이 전체 봉의 3~18%뿐. ⭐**리뷰어가 '해석이 과도하게 엄격했나'를 완화판 3종 직접 실행으로 검증**: 스펙 원문에 '둘 다 상승 중[4주 전 대비 증가]'이 그대로 있어 문자 그대로의 구현이 맞았고, OR 결합 OOS 5건·2주 기준 2건·상승조건 완전제거 11건으로 **어떤 완화에도 15건 미달** → '완화하면 새 스펙'이라며 보류한 판단이 정당. 단 **가장 느슨한 해석에서 처음으로 손익 부호가 양전환**[net PF 1.954, t=0.463 비유의]이라 재설계 시 '레짐 게이트를 빼는 방향'이 후보. ⚠️**리포트 정정: '레짐확정 65회 → 9건, 86% 소멸'의 65는 고유 터치 이벤트가 아니라 조건 만족 일수[중복 계상]** — 상태기계 기준 고유 이벤트는 43건이고 실질 전환율 약 21%. **신규 규칙: 병목 진단에서 bar-day와 event를 섞지 말 것**. ⭐리뷰어 신규발견: OOS 3건 전패가 각각 **2024-08 엔캐리 청산발 급락 직전·2025-02 Bybit 해킹발 급락 초입·2025-10 청산 캐스케이드**와 시점이 겹쳐 '급변장 진입 직후 휩소'라는 동일 패턴 → 버그 배제 논거 보강 + 구조적 취약점 시사. 주봉 데이터: **네이티브 `1w` monthly 덤프가 2024-01분까지만 존재[2024-02부터 404]** → 1d→1w 리샘플[W-MON,label=left,closed=left] 전 구간 사용, 네이티브와 145/146 일치하고 **유일 불일치[2021-07-26]는 open/high/low는 같고 close만 다른 거래소 원본 이상치**로 규명). 신규 스펙 총 **422개**. ⚠️**정기루틴 인프라 신규**: **futures brief가 78KB로 커져 에이전트가 64k 출력토큰 상한에 걸림** → **2단계 파이프라인 전환**(부모가 6개 벤뉴 원자료로 기계적 필드 전량 생성+`why`/`tag`는 git HEAD에서 이월, 에이전트는 `narrative_patch.json`[market·themes·overrides≤25]과 md만 산출, 부모가 `merge_patch.py`로 머지). ⚠️**그 머지 도구에서 자체 회귀 적발**: override 부분일치 폴백이 Binance venue 문자열에 든 "OKX" 때문에 OKX 오버라이드를 Binance 항목에도 적용해 25건→39건 적용·14개 오염 → **접두 일치 우선 + 0건일 때만 부분일치**로 수정하고 venue 문자열에서 "OKX값"→"OKX 병기"로 원천 차단. **8/21~8/22 라운드(2종 백테스트 — 전부 FAIL·리뷰어 VALID)**: OI변화율-펀딩변화 롤링상관 레짐 게이트+EMA 추세추종[스윙](**통과선을 넘은 표면수치가 사전 등록 폐기조건에 걸려 FAIL된 두 번째 사례** — OOS n=133 PF 1.423으로 통과선 1.3 상회했으나 **t=+1.565[p=0.120] 비유의**라 스펙 (b) 'OOS PF(R)<1.15 **또는** t 비유의'의 OR 절 충족. IS n=130 PF 0.973으로 손익분기. 리뷰어가 스펙 39행 원문을 직접 대조해 OR 결합이 글자 그대로 일치함을 확인하고, t≥1.96 관행도 20개 이상 리포트에서 일관됨을 grep으로 확인해 **사후 기준 신설 아님**을 입증. ⭐**리뷰어가 리포트가 놓친 더 결정적인 취약점을 신규 규명: 다일간 매크로 클러스터** — 리포트는 '같은 캘린더일' 동시진입만 검사해 '전면적 매크로 클러스터 아님'으로 결론냈으나, **종목별 EMA 눌림목 타이밍이 며칠씩 어긋나 같은 매크로 이벤트라도 같은 날엔 절대 겹치지 않음**. 5일 롤링 윈도우로 재검사하니 **OOS 순익의 71.6%가 2026-01-19~22 진입·02-06~08 청산 단일 이벤트[5/7종목·6건]**에서 발생하고 제외 시 t=+0.526으로 붕괴. 실제 사건(BTC 01-17 $95K→02-05 $62.9K 34% 폭락 후 02-06 하루 +12% 반등)임을 1d klines로 확인했고, **BTC-금 로테이션[SUSPECT]에서 독립 기록된 '2025Q4~2026Q1 붕괴장'과 동일 국면**임이 교차 확인. **결정적: 게이트없음 대조군의 OOS 엣지 전체[+1,182.8]도 이 사건 하나에서 나오고 제외 시 순손실[-653.0]**. 'SOL 54.6% 의존'도 부분 오귀인[SOL 순익의 29%가 이 공유 이벤트분]. ⚠️**신규 규칙 2건: ①종목 간 신호 상관은 연간 평균만이 아니라 위기 국면[꼬리] 상관까지 별도 점검할 것**[이 건은 평균 상관이 +0.04~0.07로 낮았는데도 위기 국면에서 클러스터가 터짐 — '평시엔 낮고 위기엔 급등하는 상관'이 핵심 함정] **②de-clustering은 같은 캘린더일 검사만으로 부족하고, 종목별 진입 타이밍이 어긋나는 전략은 3~5일 롤링 윈도우로 재검사할 것**. `analyze.py` 재실행이 로그와 **바이트 단위 일치**, `metrics.create_time`[문자열]은 애초에 미파싱[파일명+행순서]이라 ms/us 함정 구조적 미노출) · 모닝스타/이브닝스타 3캔들 반전+거래량·RSI[단타·1h](**캔들패턴이 '느슨해서'가 아니라 '너무 엄격해서' 죽은 첫 사례** — 신호빈도가 스펙 예상[연 40~120건/종목]의 **약 1/10**[연 4.2건]이고 원인은 캔들1·캔들3이 **정확히 2시간 간격으로 반대 방향 body≥1.0ATR**을 동시 요구하는 결합조건[개별 7~9% → 결합 0.04%, BTC 41,614봉 중 18봉]. OOS n=78 **gross PF 0.698로 비용 이전부터 무엣지**, net 0.602, t=-1.88. 스윕 13/13·LOO 7/7 전부 OOS PF<1. **부호 무작위화 100회에서 실제 mean(R)이 3 백분위**로 방향 선택이 무작위보다 나쁨. 갭 포함 변형은 연속선물에 실질 갭이 없어 n=3 붕괴. 리뷰어가 10개 커맨드 전량 재실행해 소수점까지 일치시키고 조건별 발생률표를 `detect_events()` 없이 독립 재구현해 재현 — **'이 프로젝트에서 감사한 백테스트 중 재현성 최고 축'** 평가. 표본 n=78이 얇아 판단불가가 맞는지 물었으나 **FAIL 확정이 타당**하다고 답[판단불가 선례 n<20 크게 상회 + gross<1·13/13·7/7·de-clustering 전부 같은 방향 수렴]. ⚠️**리뷰어 신규 발견 3건**: ①갭 불일치 원인 오진 — '부동소수 오차'가 아니라 **거래소 최소 틱사이즈 단위의 실제 1틱 차이**[BTC diff 정확히 ±0.01] ②**'크립토는 갭이 사실상 없다'는 일반화 과장** — 의미있는 갭 비율이 BTC 0.16%·ETH 0.42%·BNB 2.24% vs **SOL 15.17%·XRP 30.20%·DOGE 24.25%·ADA 43.90%**로 편차 극단적이고, 저가 알트는 가격 대비 최소호가단위 비율이 커서 0.01% 임계값이 1틱 변동을 갭으로 오분류하는 **정의 아티팩트**[ADA 틱/가격=0.0219%] → **메이저 3종목 한정 결론** ③'RSI 단독 대비 부가가치 없음'이 과한 표현 — 이벤트 중복률 9건[11.5%]뿐이라 **base ⊆ pool 미해당**이고 중복 제거 독립 Welch에서 base 평균 R[-0.153]이 RSI단독[-0.319]보다 **근소하게 유의하게 덜 나쁨**[t=2.00, p=0.049], 다만 양쪽 다 PF<1). 신규 스펙 총 **412개**(모닝스타/이브닝스타·세션별 수익률분해·OI펀딩 상관게이트·3대 롱숏비율 합의·상위트레이더 리드래그). ⚠️**정기루틴 데이터 함정 신규**: **CoinGecko `binance_futures` 티커에 분기물이 섞여 있어 base로만 매칭하면 BTC/ETH가 오염된다**[BTC $79,346·vol24 $33.6M, ETH OI $0.0M 같은 값이 나옴 → `contract_type=='perpetual'` 필터 필수]. ⚠️**온체인 워치 규칙 정정**: **다수 풀 토큰은 '최대 유동성 풀'만 기계적으로 고르면 안 되고 거래량을 함께 봐야 한다** — PANTS 페어 10개 중 최대유동성 풀[meteora $123.6K]은 회전율 1.01배로 거래가 거의 없고 실제 거래는 pumpswap[$105.5K, 회전율 33.7배]에서 발생. ⚠️**심볼 개명 회귀 2건 적발·원복**: dex `kPEPE`→`kPEPE(1000PEPE)`, cex OKX `1000PEPE`→`1000PEPE(kPEPE)`[git 이력으로 7회차 연속 원표기였음 확인. 검증 절차에 **cex·dex 양쪽 심볼 diff**를 넣어야 함 — 첫 회차엔 dex만 보고 cex를 놓쳤음]. **8/19~8/20 라운드(4종 백테스트 — 3 FAIL·1 판단불가, 전부 리뷰어 VALID)**: 12h 네이티브캔들 레인지확장+평균체결규모 브레이크아웃[스윙](**통과선을 넘은 표면수치가 사전 등록 폐기조건에 걸려 FAIL된 사례** — OOS PF 1.782·t=+2.39로 통과선 상회했으나 스펙이 **사전 등록한** 폐기조건 (b)에 해당. ⚠️**신규 규칙: 대조군 풀이 채택안을 포함하면(base ⊆ pool) 부트스트랩 검정력이 떨어진다** — 리뷰어가 채택안이 풀의 84.4%를 차지함을 밝히고, 중복 없는 독립 검정[Welch t=0.36, p=0.72]으로 더 강하게 결론을 확증) · COIN-M/USDT-M 베이시스 변동성 레짐 게이트+추세추종[스윙](**판단불가** — OOS n=11로 사전 등록한 최소표본 15건 미달. ⚠️**신규 규칙: `zero_hold_frac=0`을 '버그 없음' 근거로 삼기 전에 그 지표가 해당 엔진에서 유효한지 먼저 확인할 것** — 리뷰어가 이 엔진에서는 구조상 항상 0이라 아무것도 검증하지 못하는 트리비얼 지표임을 규명) · COIN-M/USDT-M 평균체결규모 다이버전스[단타](FULL n=44,204로 표본 충분한 FAIL. 리뷰어가 **COIN-M 계약 액면가가 고정[BTC=$100, 그 외 $10]이라 z-score가 불변**임을 확인해 구현결함이 아님을 입증. ⚠️**신규 규칙: 계좌소진이 의심되는 고빈도 전략은 LOO·top-N·de-clustering까지 전부 R-배수로 계산할 것** — 리포트가 '병기한다'고 서술했으나 실제로는 달러 PF만 쓰여 세 진단이 전부 소진 아티팩트에 오염돼 있었음) · 펀딩 부호전환 브레드스 모멘텀[단타](FAIL. ⚠️**리뷰어가 폐기근거의 강약을 재배치** — gross PF 0.994의 CI가 [0.768,1.278]이고 P(≥1.0)=47.9%라 근거 (d)는 박약한 반면, net PF CI [0.53,0.89]·P<0.2%라 net 기준 FAIL이 훨씬 견고. '모멘텀이 컨트래리언보다 뚜렷이 낫다'도 페어드 부트스트랩에서 비유의로 하향. BNB 펀딩률 53.9%가 진짜 `0E-8`임도 확인). 신규 스펙 총 **397개**(스카우트가 **`1M`[월봉]은 data.binance.vision에서 404**임을 실측 확인 — 미사용 캘린더 해상도는 주봉이 사실상 마지막). ⚠️**정기루틴 규칙 신규**: **가격·chg24는 거래소 직접 API가 1차 근거이고 WebSearch는 보조** (⭐이번 창 최대 오류 — 에이전트가 메이저 chg24 +6~10%를 CoinGecko 오염으로 판정해 3회차 동안 옛 값을 이월했는데, 부모가 OKX·크라켄·비트스탬프·코인베이스를 직접 조회하니 4곳 전부 BTC $68.6K·+6.34%로 일치 → **랠리가 진짜였고 WebSearch가 낡았던 것**. 둘이 어긋나면 서로 독립인 거래소 API 여러 곳의 일치 여부로 판정하고, WebSearch를 근거로 API 값을 기각하지 않는다) · **시계열 비교는 같은 벤뉴의 직전 브리핑 값 기준**(중간 스팟값·다른 벤뉴 값과 섞지 말 것 — '재가속' 오판 3건의 원인) · **심볼 필드는 티커만 쓰고 구분·라벨은 `why`/`thesis`에**(개명 시 시계열 단절) · **CSV는 전 행 정확히 8열·쉼표 포함 값은 예외 없이 큰따옴표** · **md에 활성으로 쓴 종목은 json `tokens`·csv에도 반드시 존재해야 하므로 저장 후 세 파일 종목 목록을 대조** · **EVM 토큰은 DexScreener 페어 API 2차 조회 필요**·**GT의 풀 생성시각 표기는 단독으로 믿지 말 것**(DS 교차검증 필수) · **HL 공식 API는 POST 전용이라 GET만 되는 에이전트가 조회 불가** → 부모가 `curl -X POST`로 받아 프롬프트에 명시(dYdX는 공식 indexer, OKX는 직접 ticker). **8/17~8/19 라운드(8종 백테스트 — 7 FAIL·1 HOLD, 전부 리뷰어 VALID)**: 테이커매도량-OI 방향 사분면 반전[4h] · 거래대금 시간대집중 HHI 레짐[1d] · 알트-BTC 거래량상관 디커플링 고유모멘텀[1d] · 30분 네이티브캔들 거래량가속 지속[30m] · **평균체결규모 z-score 전용 재검증[1d](⭐이 프로젝트에서 통과선을 넘은 유일한 관측 — OOS PF 1.337·t=+3.258. 전용 재검증으로 엔진 아티팩트[4변형, P=0.46]·동어반복[체결건수 셔플에서 100 백분위]·룩어헤드·회계결함을 전부 배제하고 남은 것은 분기 집중뿐. ⚠️리뷰어가 라벨을 뒤집음 — 스펙의 **사전 등록된** 폐기조건은 '단일 분기 ≥50%'(실측 41.4%로 미충족)인데 리포트가 **사후에 만든** 'top-2 분기 75.4%' 기준으로 FAIL을 매겼음 → 부모 세션이 **FAIL→HOLD로 정정**. 라이브 미반영은 유지)** · COIN-M/USDT-M 펀딩률 괴리 컨트래리언[8h](**신규 데이터소스 COIN-M 덤프[`futures/cm/`] 개통 확인** — 7개 퍼프 심볼 2022-07~2026-06, 2022-01~06은 전량 404) · 인덱스-퍼프 스윙구조 다이버전스[1d/4h](⚠️**신규 규칙: 블록셔플은 수익률 단위로 해야 하고 가격 레벨[절대 USD]에는 부적합** — 리뷰어가 레벨 셔플이 +584% 인공 갭을 만들어냄을 규명) · 프리미엄인덱스 정산 스퀴즈 브레이크아웃[15m](⚠️리뷰어가 '게이트가 대조군보다 나쁘다'를 다시 표본수 맞춘 부트스트랩으로 기각[p≈0.058~0.13]. **신규 재현성 결함: `dropna`가 잔여 NaN을 못 걸러 `np.corrcoef`가 오염 → 상관계수는 `pandas.corr()`[pairwise]를 쓸 것**). 신규 스펙 총 **380개**. ⚠️**스펙 요건 신규 추가**: 비용 감당 산술(예상 엣지×빈도 vs 빈도×0.14%) · 정규화 게이트 논증 · 수익 인과 경로 · 라이브 실행 가능성 · 셔플 대조군 100회 권고(20회는 p 하한 0.048) · 사전 폐기조건은 처음부터 신중히(**사후 변경 금지** — HOLD 정정의 직접 원인). ⚠️**신규 파이프라인 규칙**: ①방향별 비대칭 청산 시 조건제거형 대조군의 청산구조 매핑을 코드로 검증 ②레짐 게이트 혼합 풀은 서브모드별로 분리 후 비교 ③무비용 트레이드 단위 t가 비유의면 gross까지 de-clustering 마친 뒤 '실재하는 신호' 표현 판단 ④롤링 자기 백분위 게이트는 정보와 무관하게 일정 비율 발화 ⑤'최대 기여 분기 제거 시 미달' 단독으로 추세추종 자동 FAIL 금지. ⚠️**정기루틴 데이터 함정 2건 신규**: **CoinGecko `derivatives/exchanges/*`의 `funding_rate`는 퍼센트라 /100 필요**(거래소 직접 API는 이미 fraction) · **OKX SWAP `volCcy24h`는 코인 수량이라 `last`를 곱해야 달러**. **8/14~8/17 라운드(8종 백테스트, 전부 FAIL·리뷰어 VALID)**: Qstick 캔들바디 모멘텀 눌림목[4h](**동어반복 점검이 실제로 전략을 무너뜨린 첫 사례** — Qstick 신호가 ROC와 **93.6~99.9% 일치**해 사실상 같은 지표이고, 동일 엔진의 ROC 대조군이 채택안보다 우수) · Weis Wave 노력-결과 다이버전스[4h](**정보원 무력화 대조군이 원본을 이긴 사례** — 거래량을 랜덤 셔플한 대조군 20개 중 기본안이 **0/20 백분위**[p≈0.048]로 최하위 → 거래량 정보가 도움이 아니라 해가 됨) · 볼린저 스퀴즈 재발간격 과숙 브레이크아웃[15m](**'스펙 재해석하면 살아난다'는 문을 닫은 사례** — 대안 해석판이 스펙 예상 빈도[연 119건]를 정확히 맞췄는데도 t=-13.16으로 더 크게 실패. ⚠️리뷰어가 '게이트가 대조군보다 뚜렷이 나쁘다'는 주장을 **표본수 맞춘 부트스트랩으로 기각**[p=0.115]) · 인덱스-퍼프 수익률 상관붕괴 리커플링[15m](⚠️**신규 버그 클래스: 계좌소진 아티팩트** — `no_corr_gate` 대조군이 채택안보다 **124배 많이 거래**해 종목별 $10,000 계좌가 소진돼 OOS 달러손익 합계가 $11.92[78.6%가 DOGE]로 붕괴 → '대조군이 우수[0.720>0.614]'가 R-배수 재계산시 **'동등[0.614 vs 0.614]'**으로 정정. **교훈: 빈도가 크게 다른 대조군은 반드시 R-배수로 비교한다**[전 백테스트 적용]) · 수익률 첨도 레짐 게이트+추세추종[1d/4h](대조군 우열 주장이 다시 부트스트랩에서 비유의[p≈10.5~10.8%]) · OI-가격 탄력성 레짐[15m](ΔOI%·Kyle λ와 **|r|<0.02**로 기존 지표의 재포장이 아님이 확인됐는데도 4개 변형 전부 일블록 부트스트랩 95% CI가 1.0을 포함 → **'재포장이 아니라 독립적으로 무가치'**) · 마크가격 청산지연 연속[15m](**스펙이 스스로 적은 최대 의심지점에 정확히 걸린 사례** — dev와 gap의 신호시점 상관 **0.91~0.95**로 사실상 동일 정보. 신규 데이터소스 `markPriceKlines` 개통 확인[2022 zip은 헤더 없음·2024+는 헤더 있음, `volume=0`·`count=60` placeholder]) · 거래대금 유니버스 진입임계 모멘텀[1d](**유니버스 정의가 가설 검증 자체를 차단한 사례** — '첫 진입' 이벤트가 `UNIVERSE_85`에 SUI/APT/WLD 등 신흥토큰이 없어 **구조적으로 2022세대 레거시 알트에서만 발화** → 스펙의 원래 동기는 검증도 반증도 되지 못함). 신규 스펙 총 **360개**(8/13 320 → 라운드마다 5종씩 추가). ⚠️**스펙 요건 2건 신규 추가**: ①빈도 불일치 대조군은 R-배수 비교 ②대조군 우열 주장 전 표본수 맞춘 부트스트랩 검정. **8/13 00시 라운드(2종 백테스트)**: 유니버스 OI 브레드스 레짐 게이트+BTC EMA50 추세추종[1d/4h](**게이트가 대조군 3종을 처음으로 능가한 사례이나 표본부족으로 판단불가** — IS n=19/OOS n=16으로 검증가능 임계(20건) 미만이고 t가 전 구간 |t|<0.5. 명목 PF는 IS/OOS/FULL 1.35~1.57로 통과선 근방이지만 **top-2 트레이드 제거시 OOS 1.363→0.764로 반전**, 최대기여 분기(2025Q1 3건, 순익이 총 OOS의 107.3%) 제거시 0.992. `oi_mean`→`oi_last` 전환만으로 IS 1.355→0.944, lookback 14봉에서 OOS 0.389로 파라미터 민감도 극단. **metrics `sum_open_interest`는 글로벌 필드라 안정적**이라는 스펙 주장이 실측 확인[11,613개 일별 zip 전량 200 OK·7종목 결측 0건, `count_toptrader`가 완전 결측이던 2022-06~12 구간도 `oi_mean`은 고유값 365/365]. ⚠️**리뷰어가 게이트 우월성의 성격을 약화**: `self_oi` 대조군도 **OOS 톱-3 승리 중 2건을 거의 동일하게 포착**해, 열등한 주된 이유가 '다른 정보를 본다'가 아니라 **신호 수가 많아(22 vs 16) 잡음손실이 누적된 선택성 차이**. 세 대조군 전부 OOS t=-1.17~-1.49, 브레드스 자체도 t=+0.50으로 **통계적으로 구분 불가**. 리뷰어는 라벨을 **FAIL 쪽 권고**[체결건수 극단페이드 선례와 일관], 라이브 미반영엔 이견 없음. 룩어헤드는 리뷰어가 `strat.py`를 안 쓰고 raw CSV에서 6개 신호일 breadth를 손계산해 전부 일치 확인) **판단불가·VALID(리뷰어는 FAIL 권고)** · 펀딩 명목흐름(펀딩률×OI명목) 극단 컨트래리언[8h/4h](**스펙 자신의 사전 폐기조건에 정확히 해당한 사례** — 핵심 대조군 `funding_only`(OI 가중 없음) 대비 부가가치 없음. notional IS n=93 gross 0.677/net 0.622인데 OOS n=61 gross 1.234/net 1.129, funding_only는 IS gross 1.066·OOS 0.900으로 **IS↔OOS 우열이 역전**돼 노이즈 범위. 스윕 12변형 전부 IS net<1. 두 variant가 `zcol` 한 줄만 다르고 같은 merged 이벤트 집합을 공유함을 리뷰어가 코드로 확인해 비교 공정성 입증. ⚠️**리뷰어가 0-fill 처리 타당성을 대조 실행으로 반증**: 0-fill을 그대로 둔 버전이 OOS net 1.246인데 **필터 적용판이 1.129로 오히려 불리** → 유리하게 조작한 것이 아님이 실증. ⚠️**리뷰어가 ms/us 함정의 원인을 한 단계 더 일반화**: `metrics`의 `create_time`이 **날짜 문자열**이라 unit 미지정 `to_datetime`이 pandas 3.0.5에서 `us`로 추론하는 반면 klines·funding은 정수 epoch를 `unit="ms"` 명시 파싱 → **재발 조건 = "문자열 타임스탬프에 unit 미지정 파싱" + "정수 epoch 컬럼 병존"**. 직전 라운드의 `Timedelta` 업캐스트와는 **별개 원인**) **FAIL·VALID**. 신규 스펙 5종 추가(캔들VWAP 위치 2종·인덱스가격 레인지서지·OI브레드스 게이트·펀딩명목흐름, 총 320). **8/12 12시 라운드(2종 백테스트, 전부 FAIL·VALID)**: ETH/BTC 펀딩레이트 스프레드 z-score 컨트래리언 로테이션[4h](**신호원 교체 대조군으로 "펀딩이 가격 대비 부가가치 없음"을 보인 사례** — 채택안 OOS n=85 gross 0.834/net 0.756인데 **동일 엔진·동일 게이트에 신호원만 ETHBTC 가격비율 z로 바꾼 대조군도 OOS gross 0.838**로 거의 동일하게 무엣지 → 펀딩 정보의 부가가치가 없을 뿐 아니라 **컨트래리언 로테이션 구조 자체가 신호원 무관하게 성립하지 않음**. 스윕 11변형·게이트 제거판(n=195)·무비용 진단까지 전부 OOS gross<1. `|z|>=1.5` 실측 13.54% vs 정규분포 명목 13.36%로 **데미닝 결함 없음**이 확인돼 과거 반복된 정의결함류와 구분. ⚠️백테스터 자체발견: `Timedelta(hours=4)` ms→us 업캐스트가 **직전 라운드와 동일 함정으로 재발**해 `merge_asof` 에러 → ns 명시 통일. 리뷰어가 그 MergeError를 실제로 재현하고, `merge_asof`와 무관한 독립 알고리즘으로 15개 임의시점 `funding_z`를 재계산해 mismatch 0/15. 반전 3모드 비교로 **forward 경로는 반전 버그와 무관한 코드 경로**임을 확증. 리뷰어 서술 정정 2건: 'gross<1이 전 구간 일관'은 **OOS만 해당**[IS·FULL은 종종 >1], '6가지 독립 경로'는 동일 OOS 구간 중첩관찰이라 통계적 독립 아님) **FAIL·VALID** · 수익률-거래량 롤링상관 게이트+Donchian 브레이크아웃[15m](**의사반복(pseudo-replication)이 t값을 부풀린 것을 리뷰어가 반증한 사례** — 백테스터는 "무비용에서 유의한 양의 엣지 실재[OOS PF(R) 1.052, **t=+2.22**]하나 슬리피지 하나에 즉시 반전"이라 서술했으나, ⚠️**리뷰어가 동일 de-clustering을 무비용 데이터에 적용하니 일단위 730일 t=+1.40·주단위 105주 t=+1.43으로 비유의**. 종목별 개별 t도 7종목 중 DOGE(+2.32) 하나만 명목 유의하고 XRP는 음수(-0.87) → **"엣지는 있으나 비용에 잠식"이 아니라 "애초에 유의한 엣지 없음"**으로 본문 3곳 정정. **진입일 이진 상관(0.063)은 '언제 진입하는가'의 독립성이고 R-배수 상관(0.259)은 '얼마를 버는가'의 독립성으로 서로 다른 지표**임도 규명. 반면 슬리피지 단독 반전은 de-clustering에도 견고(일단위 0.662, t=-4.17). 신호빈도가 스펙 예상의 12~30배인 원인은 **브레이크아웃 봉이 이미 강한 방향성 거래량을 동반**해 게이트 통과율이 조건부 63.4% vs 무조건부 22~34%인 것으로 규명. 게이트가 게이트없음 대비 소폭 개선은 있어 rvasym의 '게이트가 해롭다'와는 다른 양상이나 통과선엔 한참 미달. **IS_END를 23:59:59로 명시**해 경계 사각지대를 원천 차단한 첫 라운드[`IS+OOS==FULL=37,526` 실측 확인]) **FAIL·VALID**. 신규 스펙 5종 추가(ETH/BTC 펀딩스프레드 2종·수익률거래량 상관게이트·엘리엇5파·메가폰, 총 315). **8/12 00시 라운드(2종 백테스트, 전부 FAIL·VALID)**: 실현변동성 비대칭(세미배리언스) 게이트+EMA50 크로스[1d/4h](**게이트 대조군으로 부가가치 부재를 직접 증명한 첫 사례** — SR 게이트 OOS gross 0.756/net 0.668인데 **게이트 없는 순수 EMA50 대조군이 0.806/0.719로 오히려 우수**하고 단순 모멘텀부호 게이트(0.786/0.711)와도 구분 불가. 세 변형 전부 gross<1이라 수수료 이전부터 무엣지. LOO 7종목·top-N 4단계 전부 OOS net 0.55~0.71, 스윕 10변형 전부 IS PF<1. **직전 라운드 최대 실패원인이던 매크로 클러스터는 이번엔 해당 없음** — 7종목 동시진입일이 4.5년간 **단 1일**, 종목간 신호 상관 |r|<0.111로 스펙의 독립성 주장이 실측 지지됨 → 실패 원인이 착시가 아니라 **각 종목에서 독립 반복되는 진짜 무엣지**. ⚠️백테스터 자체발견 버그 2건(①스톱 이중반전으로 zero_hold 100% ②SR부호반전 청산이 최종방향 참조시 퇴화). ⚠️**리뷰어가 최우선 쟁점[반전 대조군 처리가 지침 회피인가]을 3모드 재구현으로 해소**: 지침대로 최종방향 참조시 **zero_hold 99.4%로 퇴화함을 독립 재현**했고 그 원인이 게이트가 진입조건을 원신호 부호와 결부시킨 **구조적 필연**임을 규명, 리뷰어가 추가한 제3의 대안[반전모드에서 해당 청산 완전 비활성]이 채택안과 거의 동일(0.731/0.650 vs 0.738/0.655)해 **퇴화 모드만 빼면 어떤 해석에도 결론 불변**임을 실증. **교훈: 게이트와 결합된 서사적 청산조건은 가격 기반 스톱과 달리 최종방향 참조 원칙의 예외가 될 수 있고, 그 판단은 대안 실행으로 검증해야 한다.** `analyze.py` 재실행이 리포트 로그와 **바이트 단위 일치**[역대 최고 재현성], BNB·DOGE 추가 절단과 `merge_asof` 수기검산으로 1d게이트의 미래일봉 참조 없음 확인) **FAIL·VALID** · 펀딩정산 앵커드 VWAP 되돌림[5m](**앵커 이동 대조군으로 스펙 핵심 가설을 기각한 두 번째 사례** — 정산시각(00/08/16 UTC)을 임의시각(02/10/18, 04/12/20)으로 옮겨도 gross PF(R) 0.756~0.770에 밀집해 구분 불가. 무비용 진단(fee=0·slippage=0)에서도 net PF(R) 0.937~0.953[t=-6.4~-11.2]로 구조적 음의 기댓값. 신호빈도가 예상(200~500건/년)의 **19.7배**(6,907건/년)인 원인이 `band_std`가 앵커 블록 초반 **소표본 6~12개**로 계산되는 불안정 추정치라 명목 2σ가 실제로는 전체 봉의 12~13%를 돌파하는 것임을 규명. OOS 101,350건으로 표본 충분해 확정적 FAIL. ⚠️백테스터 자체발견: **pandas 3.0.5에서 `datetime64[ms]` 인덱스 + `pd.Timedelta`가 `datetime64[us]`로 조용히 업캐스트**돼 15m RSI 매핑이 전부 NaN이 되던 치명 버그(ns/ms 함정의 새 변종) → ns 명시 통일로 수정. 리뷰어가 `asi8` 1000배를 직접 재현하고 독립 방식(불리언 마스크) 10개 표본점 대조로 새 오정렬 없음 확인. **⚠️파이프라인 경계: 리샘플 인덱스+Timedelta 덧셈 패턴을 쓰는 다른 15m/1h 확인TF 백테스트도 잠재 노출 — 이번엔 신호 0건이라 즉시 발각됐지만 부분적으로만 어긋나면 조용히 신호가 줄어든다.** ⚠️**리뷰어 신규발견: 리포트가 오히려 과소평가** — §2 상관계수(BTC 0.246)는 `min_bars` 미적용값이고 **실제 트리거된 신호만으로 좁히면 상관 0.928**로 밴드이탈 z와 RSI가 사실상 동일 정보. 리포트 표의 zero_hold_frac 오기재 1건 정정[52.94%→79.97 → 54.04%]. IS/OOS 경계 사각지대는 이 코드엔 없음[IS_END가 23:59:59]. 다만 FULL 집계가 OOS_END 이후 1개월 3,558건을 포함해 IS+OOS와 불일치) **FAIL·VALID**. 신규 스펙 5종 추가(펀딩정산 앵커드VWAP·탑트레이더 비율속도 다이버전스·실현변동성 비대칭 게이트·펀딩 변동성스퀴즈·펀딩x변동성 사분면, 총 310). ⚠️**리뷰어 신규발견[파이프라인 전체 해당]: `IS_END`(2024-06-30 00:00)와 `OOS_START`(2024-07-01 00:00)가 둘 다 자정인 코드에서는 6/30 장중 진입 트레이드가 IS·OOS 어디에도 안 잡히는 경계 사각지대 발생**(실현변동성 건에서 SOL 1건 누락, IS+OOS≠FULL). 같은 정의가 복사됐을 수 있어 경계. **8/11 12시 라운드(2종 백테스트)**: 스팟/선물 거래대금 비율 참여주체 역추세[1h](gross PF가 FULL/IS/OOS 전부 1 미만[0.781~0.877], 스윕 13변형 전부 미달[최대 OOS gross 1.047이나 같은 변형 IS 0.762로 사후선택 패턴]. 스펙 최우선 요구였던 **스팟-선물 거래대금 상관이 실측 +0.716~+0.894로 강함**을 확인. 다만 신호빈도 붕괴 대신 **조건 비대칭**이 드러남 — 숏 조건[선물우세+신고가]은 독립기대의 1.89배인데 롱 조건[스팟우세+신저가]은 0.28배로 억제(패닉 급락시 선물 거래량 급증) → 스펙 롱 가설의 절반이 구조적으로 약화. 백테스터 자체발견: Donchian 신고가 조건이 신호봉 자신을 포함해 `close>=high` 자기참조로 사실상 항상 거짓[BTC 54개월 트리거 3건] → 자기 제외로 수정. ⚠️**리뷰어 핵심발견: 스펙의 "SOL/XRP/DOGE/ADA는 스팟 시장이 없다"는 유니버스 배제 근거가 사실과 다름**[4종목 스팟 덤프 전부 존재, HEAD 200 확인] — 리뷰어가 직접 4종목을 받아 **7종목으로 확장 재실행**하니 OOS gross 0.870/net 0.701[t=-3.89, n=1,567]로 3종목보다 오히려 나빠 결론 불변. **교훈: 스펙이 "데이터 부재"를 근거로 유니버스를 좁힐 때 검증 없이 수용 금지.** ms/us 조인은 원본 zip 재수신으로 결함 없음 확인) **FAIL·VALID** · BTC-금 상대모멘텀 로테이션[1d](**이 프로젝트 첫 PASS(조건부) 후보였으나 리뷰어가 SUSPECT로 뒤집은 사례** — 표면 수치는 OOS n=134 net PF 1.761/gross 1.808, 7종목 중 6종목 OOS net>=1.3, LOO 7시나리오 전부 1.59~2.18, 스윕 10변형 중 9개 통과, 반전 대조군 0.895로 뚜렷이 열세였다. ⚠️**리뷰어가 매크로 클러스터 분해로 무너뜨림: OOS 134건 중 24건[18%]만 제거하면 나머지 110건이 net 0.935/gross 0.961[t=-0.16]**, 단일 대형승리 2건까지 빼면 108건이 **net 0.469/gross 0.484[t=-3.20]로 유의한 손실**. IS는 더 극단적으로 **19건[8%] 제거시 223건이 gross 0.595[t=-2.90]**. R-배수 재계산으로 복리 사이징 인공물 가능성도 배제. 해석: 순이익은 **BTC 매크로 추세 3건[2023Q4~2024 반감기전 랠리·2024Q4 ETF 후속 랠리·2025Q4~2026Q1 붕괴장]을 맞춘 것이 7개 상관 알트에 6~19회씩 복제된 결과**이며, "LOO 전부 통과"는 종목 다각화가 아니라 **한 매크로 콜이 여러 상관종목에 동시 발화하는 착시**. 실질 독립 베팅이 3건 안팎이라 명목 t=+1.59의 유의성 해석 자체가 무의미. **교훈: LOO·종목 확장은 종목간 신호 상관이 낮을 때만 통계력이 되고, 그렇지 않으면 착시 표본을 만든다 — 앞으로 스펙 단계에서 종목간 신호 상관을 사전 명시할 것.** 금 데이터[Yahoo GC=F]의 주말·휴장 forward-fill은 리뷰어가 ET 00:00 라벨이 신호시점보다 항상 선행함을 확인해 룩어헤드 없음, Quantpedia 원문도 curl로 fetch해 인용 실재 확인. **라이브 미반영, 소액 반영 옵션도 리뷰어가 비권고** — 대안으로 매크로 레짐 필터 보조 활용 재설계 또는 추가 사이클 관측 권고) **SUSPECT·라이브 미반영**. 신규 스펙 5종 추가(스팟선물 거래량비율·BTC금 듀얼모멘텀·김치프리미엄·OI/유통공급 침투율·난이도 재조정, 총 305). **8/11 00시 라운드(3종 백테스트)**: 포지션-테이커 플로우 다이버전스[1h](**전제 내적일관성 요건이 처음으로 통과된 사례** — 스펙이 우려한 두 필드 상관이 실측 기각[7종목 전부 |상관|<0.03]돼 최근 2라운드의 전제 모순 패턴 미재현. 다만 gross PF가 IS·OOS 모두 1.029~1.030으로 사실상 손익분기라 수수료 이전부터 무엣지, net IS 0.858/OOS 0.866이고 7종목 전부 OOS net<1.0. 스윕 16변형 통과 0건. `flow_z_entry=0.5`가 시간의 30%를 포착해 신호빈도가 예상의 3.5~11배. ⚠️백테스터 자체발견: 스퀴즈 무효화 조기청산이 z 노이즈만으로 작동해 invalidation 청산의 70%[3,199/4,573건]가 진입 즉시 발생 → 절대 임계값으로 수정. ⚠️**리뷰어 핵심발견: `invalidation` 청산 분기가 `direction_long`이 아닌 원신호 플래그[is_short_raw/is_long_raw]를 참조해 반전 모드에서 안 뒤집히는 오류** — toptrader 건과 동일 코드 패턴이라 직접 패치·전체 재실행했으나 결론 불변[패치 후 반전 0.782~0.868<정방향]. **교훈: zero_hold_frac이 정상[28.78%/29.69%]이어도 이 오류는 안 드러남 — 모든 청산 분기가 원신호가 아닌 최종 방향 변수를 참조하는지까지 점검 필요**) **FAIL·VALID** · 앤드류스 피치포크 미디언라인[4h](터치모드 OOS gross 1.31/net 1.15이나 t=0.99 비유의, **IS는 스윕 11변형 중 9건 손실**[0.63~0.96], LOO에서 **XRP 제외시 OOS 0.999 붕괴**·top-10 제거시 0.898. 브레이크아웃 모드는 gross<1[t=-3.6~-3.8]로 명확 실패. ⚠️**리뷰어가 분기별 분해로 과적합 확증**: OOS 순익 +2,005.7 중 **2024Q4 단독이 +1,779.2[약 89%]**로 2024년 4분기 광범위 랠리와 시기 일치 → '레짐 변화'보다 '특정 국면의 우연한 롱 편향'. ⚠️**리뷰어가 스펙 라벨링 모호성 처리의 편향을 실증**: 백테스터가 임의로 P2선 통일한 것 대신 문자 그대로의 대안[하락 피치포크 숏=P1선]을 직접 재실행하니 OOS 0.956/IS 0.467로 **채택안보다 더 나쁨** — 설계 판단이 결과를 유리하게 만들었음이 실증[결론은 불변]) **FAIL·VALID** · 1-2-3 리버설 패턴[4h](**gross PF가 FULL/IS/OOS 전부 1 미만**[0.86/0.87/0.86], net 0.80~0.81, t=-2.4~-3.6. 스윕 12변형 전부 0.79~0.86 평탄대, **LOO 모든 단일종목 제외에도 OOS<1**이라 구조적. 반전 대조군도<1이라 트리거에 방향정보 자체 없음. 스카우트가 우려한 p3_proximity 필터 과잉제약은 실측 **제거해도 표본 22~27%만 감소**해 과거 회전율 레짐의 1/500 붕괴와 다른 양상) **FAIL·VALID**. ⚠️**리뷰어 최우선 검증[ZigZag 룩어헤드] 결함 없음 확인**: zigzag.py가 `confirmed_at` 이후에만 피벗 편입하고 precompute_channels가 매 bar `confirmed_at<=i`만 사용해 구조적으로 미확정 피벗 참조 불가. BTC 50봉 외에 **ETH[cut=77]·ADA[cut=133]·XRP[cut=20] 3종목과 1-2-3 고유 신호함수까지 별도 절단 재실행**해 전부 bit 단위 일치. ZigZag 피벗 빈도 3~10배 초과도 정의 결함이 아니라 4h 해상도의 자연스러운 결과로 판별. 신규 스펙 5종 추가(분기물만기수렴·피치포크·포지션테이커괴리·1-2-3리버설·연준유동성, 총 300). ⚠️**인프라 이슈**: 이번 라운드 백테스터 2건 모두 **격리 워크트리에 저장소 파일이 체크아웃되지 않아**[HEAD에 README만] Write/Edit 실패·Bash 우회 필요했고 한 건은 워크트리 브랜치를 origin으로 reset --hard함[빈 브랜치라 손실 없음] — 워크트리 격리가 최근 정상 동작하지 않음. **8/10 12시 라운드**: ETH/BTC 상대강도 추세 브레이크아웃(기존 btc-eth-zscore-spread[평균회귀]의 정반대 가설 대조군 — FULL 80건 net 1.287[t=+0.78], **IS 51건이 gross부터 무엣지**[1.115, t=+0.12], OOS 29건 net 1.765이나 t=+1.12 비유의이고 **top-3 승리 제거시 0.856 순손실**[top-5시 0.436]. 스윕 17변형 전부 IS 0.70~1.22 평탄·OOS t가 +1.58 미만. 반전 대조군이 IS·FULL에서 정방향과 대등·우수. ⚠️**백테스터가 버그 2건 자체 발견·수정**: ①비율 청산 비교가 ETH-롱·BTC-롱에 같은 부등호를 써서 BTC 트레이드 대부분이 진입 1봉 뒤 거짓청산 ②**반전 대조군이 원래 방향의 절대 Donchian 채널 재사용해 zero_hold_frac 100%**[126건 중 119건 즉시청산] — 최근 반복된 바로 그 버그 클래스. **리뷰어가 버그 2건을 코드로 되돌려 직접 재현**해 수정 전 수치 일치 확인하고, 수정본이 진입가 대칭 재배치+청산조건 방향분기를 둘 다 만족함을 대조해 **해당 버그 클래스의 구조적 해소를 처음 확인**[프롬프트에 명시+zero_hold_frac 기재 요구가 실제 작동한 첫 사례]. impl 5개 스크립트 전량 재실행해 소수점까지 일치, OOS_END·회계정합·룩어헤드 재확인. 리뷰어 의견: OOS t 미달 단독이 아니라 4가지 독립증거 수렴이라 FAIL 타당. 부수 결론: 평균회귀·추세추종 양방향 모두 엣지 없어 ETH/BTC 관계에 활용 가능한 방향성 정보 희박) **FAIL·VALID** · 체결건수 흡수 스퀴즈 브레이크아웃(**스펙 전제의 내적 일관성이 실측 반박된 두 번째 사례** — 스펙이 사전 검토에서 '체결건수 극단과 레인지 스퀴즈는 상호배타적이지 않다'고 결론냈으나 7종목 전부 range_pct와 count_z의 **상관 +0.51~+0.61**로 강한 양의 상관. 결합조건이 독립 기대치의 **약 1/525**[BTC 실제 8봉]만 발생해 신호빈도가 예상[연 150~400건]의 1/60~1/160. 전체 78건[IS 14/OOS 64] gross 0.988[t=-0.04]·OOS gross 1.063[t=0.17]로 수수료 이전부터 무엣지, net 0.304[t=-4.06]·승률 21.8%. LOO·top-N·스윕 13변형 전부 엣지 부재 수렴. 반전 대조군 zero_hold_frac 21.8~29.5%로 버그 재현 없음. **리뷰어가 BTC 개별조건 발생률을 직접 계산**[squeeze 27.81%·count_ok 5.63%·vol_ok 57.46%]해 독립기대 4,250봉 vs 실제 8봉[1/531]으로 전제 반박을 확증, ADA 300봉 교란으로 룩어헤드 부재 독립 확인. 리뷰어 신규발견: IS 14건/OOS 64건 비대칭은 **데미닝 결함이 아니라** 흡수봉이 2025년에 쏠린 결과[상관이 2025년 0.41~0.47로 약화되며 희귀 꼬리 이벤트의 결합확률이 비선형 증가, 시장 미시구조의 시간가변성]) **FAIL·VALID**. 신규 스펙 5종 추가(금리커브 10Y-3M 레짐·CME거래량서지·체결건수흡수·ETHBTC상대강도·펀딩분산붕괴, 총 295). 스펙 요건에 **전제의 내적 일관성 자문**을 추가했는데 그 라운드에 바로 두 번째 결함이 실측 적발됨 — 스펙 단계 사전 검토만으로는 부족하고 **실측 발생빈도 대조가 필수**임이 재확인. **8/10 00시 라운드**: 탑트레이더 내부 괴리[계좌수비율 vs 포지션가치비율](OOS net 1.147[gross 1.208, t=+1.89]로 통과선 미달, 스윕 16변형 전부 OOS net<1.3[최대 1.253], IS는 사실상 손익분기[1.018, t=+0.22]. OOS를 반기 분할하면 4구간 중 3구간이 0.91~1.15이고 최근 구간[2026상반기 n=273]만 PF 1.638[t=+3.24], 제외시 나머지 OOS[n=885]가 1.022[t=+0.27]로 IS와 동일한 무엣지 회귀. DOGE 의존[LOO 제외시 t 1.89→1.08]. 데이터 발견: count/sum_toptrader 필드가 2022-01~05 거의 전부 NaN[연간 87.3% 결측]이라 실질 시작이 2023-01-13. ⚠️**리뷰어가 완화요인 2건을 철회**: ①**반전 대조군의 명백한 열세[0.66~0.69, t≈-9]가 청산로직 버그의 인공물** — `div_revert` 청산조건이 반전모드에 맞게 조정되지 않아 **반전거래 99.5%가 진입 봉에서 즉시청산**되고 피한 0.5%는 오히려 net 3.83. '방향에 진짜 정보' 주장 철회. **교훈: 반전 대조군은 SL/TP 진입가 대칭 재배치만이 아니라 청산조건도 방향에 맞게 뒤집어야 함**[첫 발견, 전 백테스트 적용] ②'최근 1분기만 강함' 서술 과장[대안 분할로는 단절 아닌 요동, 결론은 불변]. 리뷰어 신규발견: 2022 결측 서술 축소 — 원본 CSV curl로 **2022-06-25~12-14[172일, 7종목 전부]도 거의 완전 결측** 확인, 단 상위트레이더 전용 필드 국한이고 글로벌 계정비율·OI는 정상. '|whale_div|>=1.5가 32~45%'는 데미닝 버그가 아니라 롤링 자기정규화의 산술적 결과[z 실현 std 1.17~1.34]로 판별) **FAIL·VALID** · 거래대금/OI 회전율 극단 레짐(**스펙 전제의 구조적 자기모순을 규명한 사례** — 레짐A[회전율 극저→브레이크아웃] 트리거가 4.83년×7종목 합산 **단 1건**[예상의 1/170~1/500]으로, 브레이크아웃이 통상 거래량 급증=회전율 상승을 동반해 '극단적 저회전율' 전제와 상호배타적. 실질 검증은 레짐B[페이드] 단독. gross부터 무엣지[IS 0.524 t=-2.89, FULL 0.670 t=-2.61], OOS gross 0.973/net 0.943[t=-0.13~-0.27]로 1과 구분 불가하고 BNB 단일 의존[LOO 0.943→0.813]. 스윕 23변형 전부 0.87~1.11[|t|<=0.65] 평탄. 반전[추세추종] 대조군이 FULL에서 유의하게 우수[1.375, t=+2.13]. 리뷰어가 **직전 건에서 발견된 반전 청산조건 버그의 재발 여부를 최우선 점검**해 이 코드엔 구조적으로 없음을 확인[strat.py가 매 봉 trade.direction에서 is_long 재독출, 캐시 없어 klines·metrics 전량 재다운로드 후 holding_bars 진단 zero_hold_frac=0.0%, reverse 재실행 1.372 t=+2.11로 일치] → **반전 우세는 진짜 결과**. 레짐A 1건·데미닝·룩어헤드·OOS_END·7종목 동시진입일 3개 전부 독립 재현) **FAIL·VALID**. ⚠️**리뷰어 파이프라인 발견**: `data.binance.vision` metrics 일별 덤프가 **재요청 시점에 따라 bit 단위 재현을 보장하지 않음**[재다운로드시 548/12,348건 404, ETH·DOGE 트레이드수 소폭 차이] — 방향성·유의성 결론은 동일 재현되나 같은 덤프 쓰는 리포트 재검증시 '수치가 소수점까지 안 맞으면 즉시 버그'로 속단 금지. 신규 스펙 5종 추가(크로스거래소 OI괴리·고래집중도·VIX레짐·회전율속도·Deribit스큐, 총 290). 스카우트가 OKX 펀딩 히스토리를 실측했다 3~6개월뿐이라 자체 폐기하고, Deribit 스큐는 **무료 과거 히스토리 아카이브 부재를 확인해 백테스트 불가임을 사전 명시**. **8/9 12시 라운드**: 인덱스가격 즉시괴리 스냅백(**스펙 정의 결함을 백테스터가 규명한 첫 사례** — gross가 모든 컷에서<1[FULL 0.629 n=138,837, OOS 0.755 n=72,738], R-배수 등가중 재계산 OOS R_gross 0.683[t=-41.71]. 7종목 전원 gross<1, LOO 0.754~0.755로 거의 불변, 스윕 5축 20변형 전부 0.59~0.79, top-N 제거시 오히려 악화[0.755→0.663], 반전 대조군 0.675. **신호빈도가 예상의 15~200배**[종목당 연 3,038~6,098건]인 원인이 스펙의 `dev_mult = dev / rolling_std(dev,60)`에 **데미닝 누락**이라는 정의 결함임을 규명 — dev가 전 기간 연평균 음수[-0.044%~-0.049%]이고 그 크기가 60분 롤링 std[~0.013%]와 맞먹어 3σ가 아니라 상시 준-정상상태를 포착[BTC 전체 시간의 66.05%가 이미 |dev_mult|>=3]. 데미닝 보정시 0.93%로 정상화되나 **보정판도 gross<1**[FULL 0.587/OOS 0.663]이라 가설 자체 미재현. **리뷰어가 원본 parquet에서 핵심 수치를 전부 독립 재실행**해 소수점 3자리까지 일치 확인[66.05%·연도별 음수편향·데미닝 후 0.93% 전부 재현], indexPriceKlines 덤프 curl 검증[HTTP 200, volume=0·count=60 상수 placeholder]. **리뷰어 신규발견**: 서술만 있던 클러스터링을 일 단위로 de-clustering 재집계[OOS 고유 729일, 7종목 동시진입일 93.7%]해 gross 0.486[t=-3.39]로 여전히 유의하게<1임을 확인해 표본 부풀림 우려 해소. **파생 함의: dev의 구조적 음수 편향이 실측 확인돼 기존 premium-index 계열 스펙에도 함의**) **FAIL·VALID** · BTC 월별 계절성 업토버(**첫 판단불가(Indeterminate) 사례** — 선물 1d 덤프가 2020-01부터만 존재해 종목당 **n=6**, 스팟 확장해도 n=9로 원문[코인데스크] n=13과는 2013~2016 4개년이 **원천 결측**[바이낸스 2017년 개설]. 명목 n=39는 같은 해 종목 간 상관 0.39로 부풀려져, 연도 단위 독립표본 재검정시 t=+2.14[p=0.039]→**t=+1.52[p=0.19]로 유의성 소멸**. 12개월 다중비교에서 10월이 1위지만 7월이 근소한 2위[2.39 vs 2.91]로 본페로니 임계값[4.98/3.96]에 크게 미달. 원문 11월 중앙값 +8.81% vs 재현 **-7.10%로 부호 반대**, 원문 13년 10승3패도 이항검정 p=0.092로 5% 미달. gross-net 갭 미미[2.725→2.693]로 스펙의 수수료 가정은 실측 확인, 반전 대조군 열세[0.49]·10월 중앙값/승률이 원문과 근접[11.00% vs 10.8%, 77.8% vs 76.9%]이라 FAIL로도 단정 안 함. 연 1회 이벤트라 표본이 매년 +1씩만 증가해 **재백테스트로 해결 불가**. 리뷰어가 8개 커맨드 전부 재실행·scipy로 p값 독립 재계산하고 **코인데스크 원문 두 기사를 직접 curl로 fetch해 인용문 확인**[인용 환각 아님, 11월 부호 반대는 실제 데이터 불일치]. 판단불가 라벨도 지지) **판단불가·VALID**. ⚠️**리뷰어 핵심 정정**: 백테스터가 자체 발견 보고한 klines 덤프 ms→us 단위변경 함정이 **날짜·범위 모두 과장**임을 fresh curl로 규명 — 실제 changeover는 2025-09가 아니라 **2025-01**이고 **스팟 전용**, **선물(um)은 2020-01~2026-01 전 구간 13자리 ms 불변**. research/impl 7개 중 스팟 쓰는 건 이 리포트뿐이고 고정 unit=ms인 quote_volume_rank_migration.py는 선물만 써서 노출 없음 → 파이프라인 영향 사실상 없음. 신규 스펙 5종 추가(인덱스가격 스냅백·DVOL 변동성리스크프리미엄·CME베이시스·월별계절성·DXY, 총 285). 발굴 방향: 소진된 축을 배제하고 **바이낸스 밖 신규 데이터 소스**[Deribit DVOL·Yahoo Finance CME/DXY]와 **미사용 캘린더 해상도**[월 단위]로 전환. 과거 폐기된 CME 갭 스펙의 24/7 전환을 역이용해 베이시스 스펙 설계. **8/9 00시 라운드**: Hikkake 인사이드바 페이크아웃 반전(**표본 24,310건으로 역대 최대인데도 gross부터 무엣지** — 신호빈도가 스카우트 예상[월 수~수십건]의 20~100배인 종목당 연 640~900건. FULL gross 0.855/net 0.681[t=-13.80], **OOS gross 0.938/net 0.756**[t=-8.48], OOS 종목별 gross 7종목 중 6종목<1. 스윕 14변형 전부 net<=0.80, LOO 0.743~0.767 고정, top-20 제거해도 0.756→0.733으로 거의 불변해 구조적 손실. 반전 대조군 0.726도 <1. **리뷰어가 최대 쟁점[정의가 느슨해 노이즈를 잡은 것 아닌가]을 해소** — Oxford Strat 원문을 직접 fetch해 수식과 코드를 줄 단위 대조해 완전 일치 확인, BTC 15m 전 구간 독립 재계산으로 인사이드바 발생률 16.6%·페이크아웃 조건부확률 74.6% 재현. 빈도 과다는 일봉 기준 원문 정의를 15m에 그대로 적용한 자연스러운 결과. **리뷰어 신규발견**: IS+OOS가 하나의 연속 equity라 IS에서 계좌가 거의 전소된 뒤[BTC $10,000→$2.25] OOS를 이어가는 구조 — equity 독립적 **R-배수 등가중 재계산**으로 OOS gross PF[R]=0.918[t=-4.17] 확인해 결론 불변[ETH '손익분기 근접'은 가중치 아티팩트]. CONCERN: gap_ok 게이트가 셋업 형성만 보호하고 confirm/보유 루프는 미보호라 오염 2건[SOL·XRP 2022-04-03] 실재, 24,310건 중 0.008%·전부 IS라 판정 불변이나 '원천 차단' 표현은 과장)·프리미엄 인덱스 모멘텀 추세추종(기존 z-score 평균회귀 스펙의 **정반대 가설 대조군** — FULL gross 0.805/net 0.551, **OOS gross 0.859/net 0.572**[t=-12.64]. 무비용[fee=0·slippage=0] 진단 OOS PF 1.041[t=+0.92 비유의], 반전 대조군 0.806도 <1이라 방향 자체에 정보 없음. 스펙이 경고한 매크로 클러스터링 실측 확인[명목 10,061→고유 1,410일, **7종목 전부 동시진입일 188일=13.3%**]. **리뷰어가 이벤트 단위로 재집계**해 OOS 고유 637일 기준 gross 0.723[t=-2.33]·net 0.320[t=-8.52]로 극단적 de-clustering에도 gross조차 유의하게<1임을 확인해 t 부풀림 우려 해소. premiumIndexKlines 덤프를 curl로 직접 확인[HTTP 200, 값 0.1~0.26%로 정상 베이시스]. ⚠️**리뷰어 구조적 지적**: `research/.gitignore`가 `impl/` 전체를 제외해 `research/impl/*.py`가 애초에 git에 안 남아 두 리포트 로더 diff가 원천 불가 — 이번엔 워크트리가 살아 재현했으나 **워크트리 소멸 시 재현 불가능한 구조적 취약점**[파이프라인 전체 해당]. '두 번째 독립 확인' 표현은 유니버스 차이[기존 BTC/ETH 2종목 vs 이번 7종목] 감안하면 다소 수사적) 2종 **FAIL·VALID**. 신규 스펙 5종 추가(Hikkake·트위저 탑바텀·프리미엄인덱스 모멘텀·탑트레이더 vs 글로벌 스프레드·홀리그레일 ADX, 총 280). 스카우트가 트위저의 'TrendSpider 승률 55~60%'를 WebFetch 재확인 실패로 미인용하고, 홀리그레일은 원문에 **백테스트 통계 전무·사례 2건뿐**임을 정직 인정해 후순위 채택. **8/8 12시 라운드**: 위클리 시가(월 00:00 UTC) 괴리 평균회귀(**표본 충분한데도 무엣지인 첫 사례** — FULL n=435/OOS n=207, OOS **gross 0.996으로 정확히 손익분기**·net 0.957·t=-0.28. 고유 독립 91주는 아니고 **121주**[IS 68/OOS 53], 명목:고유 0.28로 클러스터링 실재[2022-11-07 FTX주 7종목 동시]. 파라미터 13변형 전부 OOS net<1.1·통과선 0건, LOO 7종목 전부<1.1. **방향반전 대조군이 과거와 다른 패턴** — 정방향 gross 0.996 vs 반전 1.029로 둘 다 1.0 부근·양쪽 |t|<0.3라 '반대가 이긴다'가 아니라 **'어느 방향으로도 정보 없음'**[스펙이 밝힌 'gross 방향 미확정'이 실측 확인]. 리뷰어가 **순수 무비용[fee=0·slippage=0] 시뮬을 직접 실행해 OOS net 1.008(t=+0.05)** 확인해 FAIL 강화, 종목 불일치[ETH/BNB/XRP PF>1.3 vs BTC/SOL/DOGE/ADA 0.70~0.80]는 **7종목 중 사후 최적 2종목 선택이라 다중비교 보정시 유의성 소멸**하는 전형적 사후 선택편향으로 판정. 리뷰어 신규발견: **OOS 필터가 `OOS_END` 상한 미적용**으로 2026-07-01~04 진입 6건 혼입[문서-코드 불일치, 제외시 0.957→0.985로 완화되나 여전히<1] — 같은 코드 패턴이 다른 백테스트에 복사됐을 수 있어 경계)·거래대금 순위이동 모멘텀(**유니버스 정의 문제부터 드러난 사례** — 지정 7종목이 전부 major_bases라 85종목 내 순위가 1~3위 고정돼 `rank_jump≥20`이 4.5년간 **DOGE 단 1회[거래 2건]**만 충족[스펙 본문의 'BTC·ETH는 신호 대상 제외'와 일치], 그래서 **85종목 알트 유니버스로 확장**해 검증. FULL n=1,386 **gross 0.646**/net 0.617·t=-6.87, OOS n=173 gross 0.818/net 0.790 — gross부터 전 구간<1. 클러스터링 명목 1,386→고유 91건[동시진입 최대 11종목/일]이고 **이벤트 단위로도 net 0.144·t=-4.86**. 반전 대조군도 1 미만[FULL 0.838]이라 트리거가 방향 무관 무정보. 스윕 36조합·LOO 전부<1, top-N 제거시 오히려 악화. 백테스터가 **진입수수료가 pnl에 미반영되던 버그 자체 발견·수정**, GRTUSDT 절단 재실행으로 룩어헤드 부재 실증. **리뷰어 신규발견[CONCERN]**: `UNIVERSE_85`에 SUI/APT/TIA/SEI/WLD/PYTH/JUP/ONDO/RENDER/FET/INJ/BONK/WIF/PEPE 등 **2023~2025 주요 내러티브 알트가 전부 부재** — 리포트의 '초단명 밈코인 누락'보다 배제범위가 훨씬 넓고, eligible[≥$10M] 심볼이 2022년 ~75~80종목→2025~26년 ~32~42종목으로 **절반 감소**해 OOS 표본이 IS 대비 7배 얇은 이유가 '알트시즌 편중'이 아니라 **고정 유니버스의 구조적 노후화**임을 정량 규명. 다만 신흥토큰 고변동성상 포함해도 개선 근거 약해 FAIL 유지, 적용범위만 '2022세대 레거시 알트'로 좁혀짐) 2종 **FAIL·VALID**. 신규 스펙 5종 추가(펀딩 부호 스트릭 지속·위클리 시가 괴리·거래대금 순위이동·평균 체결규모 축적·상위트레이더 극단 지속, 총 275). 기존 270개 전수확인 결과 고전지표·OI/펀딩조합·캔들패턴·세션효과·크로스섹셔널 등 표준축이 대부분 소진돼 **아직 안 쓰인 데이터 구성**(순위 이동·단순 앵커·스트릭 지속기간·체결규모 비율)으로 발굴. **인프라 신규 발견**: `data.binance.vision`에 `metrics` 덤프(OI·상위트레이더/글로벌 롱숏비율, 5분 간격, 2020-09~현재) 존재 확인 — 기존 여러 OI/롱숏비율 스펙이 'REST 30일 제한'으로 지적한 표본 제약을 해소 가능. **8/8 00시 라운드**: 위클리 아웃사이드 리버설 바(주봉 키리버설 — FULL n=22 net PF1.19, IS n=9 net 2.80인데 **OOS n=13 net 0.63·gross 0.64로 방향이 정반대**, 파라미터 스윕 12변형 중 11개 OOS<1, top-1 제거만으로 0.63→0.40, **방향반전 대조군이 OOS에서 우세[2.28]**. **스펙이 경고한 매크로 클러스터링은 실측 결과 문제 아니었음**[명목 22→고유 15, 3종목↑ 동시 1건뿐·7종목 0건] — 진짜 문제는 절대 표본부족. 선정근거 '주봉이라 수수료 부담 작음'은 gross-net 차 0.01~0.04로 실측 확인. 리뷰어가 5개 커맨드 전부 재실행·OOS 13건을 손으로 합산 재도출하고, **BNB 3건 전패[-299.43]를 빼면 나머지 6종목은 +83.52 흑자**임을 밝혀 리포트의 '5가지 독립 증거 수렴' 표현이 과장[대부분 같은 소수 트레이드 파생]이라 지적. 신규발견: `compute_signals`의 `np.roll`[위치기반]이 결측주 존재시 2~3주 떨어진 행을 '직전 주'로 오참조하는 **구조적 결함** — SOL/XRP 15m 결측[2022-02-21·02-28·03-28, 과거 위클리피벗 감사와 동일 위치]에서 발현 가능하나 base 22건은 전부 오염구간 밖이라 판정 불변)·BTC 거래량-시간 인트라데이 모멘텀(Gao-Han-Li-Zhou 2018 JFE의 크립토 확장, 1h 캔들 근사 — n=**11,433**로 표본 충분한데 **gross PF 1.029·t=+0.56~1.06으로 비유의**, net 0.666[t=-11~-14]. **앵커 이동 대조군**[day 경계 0/6/12/18 UTC]에서 net 0.51~0.69로 전부<1이라 특정시각·구조적 효과 모두 미재현, 방향반전 0.648로 정방향 0.637과 거의 동일해 `r_first` 부호에 방향정보 없음. 리뷰어가 '수수료가 gross의 595%' 수치를 **서로 다른 두 시뮬 교차비교**임을 규명하고 fee/slippage 분리 실행으로 **슬리피지[왕복 0.04%] 단독으로도 raw_pnl이 -21,977로 뒤집힘**을 보여 사이징·수수료 버그가 아니라 gross 엣지 자체가 희박함을 확증. CONCERN: 7종목 동시진입으로 t의 독립표본 가정 과대추정 가능[net t=-11~-18라 결론 불변], 30m 덤프가 실존[HTTP 200]하나 1h 배수만 스윕해 30분 해상도 정밀재현 미수행) 2종 **FAIL·VALID**. 신규 스펙 5종 추가(거래량-시간 모멘텀·위클리 아웃사이드바·OI 증가율 횡단면 로테이션·런던NY오버랩 거래량서지·오프닝 되돌림 리테스트, 총 270). 스카우트가 오프닝 리테스트 유튜브 원문에서 **제목의 '1000회 백테스트' 주장이 본문에 근거 전무함을 확인해 미인용**하고 원 화자가 크립토 비거래 주식/선물 트레이더임도 기록. **8/7 12시 라운드**: OI 캐피출레이션 멀티데이 디레버리징 리셋(3일 OI-15%↑ 급감 후 반전 — 셋업 186건→체결 53건[IS 37/**OOS 16**], IS gross PF **0.628**로 수수료 이전부터 무엣지, OOS gross 1.006/net 0.980·t=-0.03. **스펙 단계에서 미리 경고한 '명목표본 부풀림'이 실측 확인**: 5일 클러스터링시 고유이벤트 51개 중 3종목↑ 동시발생 17개·7종목 전부 동시 3개[2022-11 FTX·2024-04·2025-10]뿐. 방향반전 대조군이 오히려 우세[OOS net 1.506]해 'OI 소진 후 반전' 가설 미지지. OOS top-5 제거시 0.980→**0.000**, XRP 제외시 0.409. 리뷰어가 **0-fill 보정을 끈 버전을 직접 재실행**해 플래그 188→186으로 2024-07-09~15 스퓨리어스 2건만 제거됨을 정량 재현[원 리포트는 서술만 있던 것], **OOS 4승/12패를 금액분해해 XRP 2건이 승리손익의 64%임을 입증**, 반전 대조군이 `stop = o ± dist` 진입가 기준 대칭 재배치임을 코드로 확증[허위PF 버그 재발 아님]. 리뷰어 지적: 스윕표 `atr_trail=2.0` 행 플래그수 186→실제 191[상태기계 경로의존성, 결론 불변])·일봉 체결건수(count) 극단 페이드(전체 n=**25**[IS 7]로 극단적 표본부족, IS gross 0.531·OOS gross 1.399/net 1.358이나 **t 전 구간 ≤0.65**, 방향반전 1.065와 통계적 구분 불가, top-1 제거만으로 1.122→0.962, 파라미터 스윕 단조성 없이 0.32~1.12 요동. 종목×기간 14조합 중 5개 트리거 0건. `data.binance.vision` 덤프에 `count`가 9번째 컬럼으로 실존함을 선결 확인. 백테스터가 **EMA20 복귀 익절 채널이 반전 방향과 논리 충돌해 전 트레이드 1일 손실청산되던 버그** 자체 발견·수정. 리뷰어가 count 컬럼을 원본 CSV 값범위로 직접 검증[taker_buy_base_volume 오인 배제]. 리뷰어 의견: 통계력만 보면 '판단불가'가 더 정밀하나 표본 하나에 의존않고 독립근거 3가지로 FAIL을 확정해 도출방식 건전) 2종 **FAIL·VALID**. 신규 스펙 5종 추가(헤드앤숄더 넥라인·쐐기형 추세선수렴·OI캐피출레이션·삼백병/삼흑아·체결건수 극단페이드, 총 265). 발굴 기준 추가 진화: 직전 HOLD 2건이 표본부족(16건·7건)으로 막힌 점을 반영해 **스펙 단계에서 예상 표본빈도를 사전 명시**하도록 지시 — 실제로 OI캐피출레이션은 그 경고대로 무너졌다. 스카우트가 삼백병 스펙에서 원문의 '크립토 일봉은 표본부족' 경고를 직접 발견해 1h·넓은 유니버스로 설계 변경, altFINS '성공률 82~84%'·TradingView '82%'는 방법론 비공개라 정량근거로 인용 안 함. 8/7 00시 라운드: **처음으로 FAIL이 아닌 HOLD 2건**. 토큰 언락 공급쇼크 페이드 숏(표면 OOS net PF **2.035**·전체 2.955로 통과선 상회하나 표본 16건[IS 3건]·유일 반복표본 SpaceID n=9가 PF0.796 손실·플라시보 대조군 PF1.11로 알트 구조적 하락편향과 미분리·Keyrock 원문['팀 언락 평균 -25%'·'90% 부정적'] 미재현[팀 평균 +1.94%·음수비율 56.2%]. **리뷰어 신규발견이 HOLD를 보강**: 반복표본 SpaceID의 'Seed Round'가 클리프가 아니라 5년 분기별 1% 균등 선형 베스팅임을 vest_raw 직접확인으로 규명 — `min_unlock_pct_supply≥1%` 필터가 배치크기만 보고 불연속성 미검사하는 설계공백. 제외시 '진짜 클리프' 7건[전부 n=1]으로 더 얇아짐. OOS t=1.15 비유의)·현물BTC ETF 유출스트릭 컨트래리언(2.57년간 **거래 7건**뿐, 전체 net PF1.526이나 절반분할시 0.705 반전·파라미터 한칸에 0.24~1.61 요동·top1~2 제거시 1.53→0.87→0.31 붕괴·t=0.46. 구조적 발견: 7건 전량이 '순유입 전환 첫날 청산'으로 끝나 TP/SL/최대보유15일 미발동[평균보유 2.0일] — 스펙의 '1~3주 보유 +8~12%' 전제가 스펙 자체 청산규칙과 충돌. `bool Series.shift(1).fillna(False)`의 object-dtype 버그로 58건→7건 자체수정, **리뷰어가 버그 재구성해 58건 재현 후 7건이 옳음 확인**. 리뷰어가 `flows.json`의 **git 커밋이력을 분석해 각 flow 값의 파일 최초등장 시점을 실측**[대부분 D+1, 간헐 D+2~4]해 D+1 공표지연 가정이 낙관적이지 않음을 독립검증) 2종 **HOLD·VALID**(둘 다 라이브 미반영). 신규 스펙 5종 추가(전부 스윙 — 토큰언락·ETF유출스트릭·Deribit 옵션만기핀닝·BTC 200주MA·CME COT, 총 260). 발굴 기준 진화: '엣지 크기보다 **엣지 대비 거래빈도**가 생존을 가름'(OI컨퍼메이션 gross1.395 t=7.06 유의였으나 연~200건 빈도로 net 0.688 잠식 vs 위클리피벗은 수수료 부담 작았으나 신호 무엣지)을 근거로 저빈도·고엣지·논리적 근거 뚜렷한 이벤트드리븐 계열만 탐색. 스카우트가 COT 스펙에서 WebSearch 요약의 '승률 60~70%'를 원문 WebFetch로 재확인했다 존재하지 않아 폐기·'정량근거 없음' 표기(인용 환각 자체 차단). ⚠️규약 위반 1건 발견·정정: OI 리포트 말미에 모델 식별자 포함 Co-Authored-By 줄이 들어가 있어 제거, 전 산출물 재스캔 잔존 0건. 8/6 12시 라운드: OI-가격 컨퍼메이션 vs 다이버전스 **정면비교**(기존에 스펙만 있고 미구현이던 다이버전스를 이번에 처음 구현해 동일엔진 비교 — 컨퍼메이션 OOS gross PF1.395/net 0.688로 작은 gross 엣지가 수수료에 잠식[gross t=7.06 유의], 다이버전스 n=226으로 gross t=-1.11 비유의라 '표본부족으로 판단불가'가 정확하나 net t=-2.91로 FAIL 유지. 방향반전 대조군 net 1.073은 **리뷰어가 코드추적으로 절대레벨 재사용 없음 확인**해 과거 2건의 허위PF 버그 재발 아님이나 t=1.40 비유의로 채택 안 함. 리뷰어 신규발견: OI가 봉 시가 시각에 정렬[보수적 어긋남, 룩어헤드 아님]·7종목 동일날짜 0-fill 결측[2024-07-09~15 6일연속은 OOS 시작 직후]이 isna() 체크를 우회했으나 오염거래 제외해도 PF 변화 미미)·위클리 클래식 피벗포인트(브레이크아웃 OOS net PF1.18로 미달+IS는 0.93 손실로 방향 뒤집힘+top10 제거시 1.04 붕괴, 되돌림은 gross 0.90으로 수수료 이전부터 무엣지·청산 64.6%가 손절. **선정근거였던 '위클리라 수수료 부담 작음'은 실측 확인**[gross-net 차 0.05~0.12p]되어 실패 원인이 신호 자체임이 분명. 리뷰어가 resample 비의존 수동슬라이스로 BTC 233개 완전주 전수대조해 룩어헤드 0건 확증, 범위 밖 파라미터까지 확장스윕해 필터 제거시 OOS 1.34여도 IS 0.94로 '살아날 여지 없음' 확인) 2종 **FAIL·VALID**. 신규 스펙 5종 추가(OI컨퍼메이션·크로스섹셔널 초단기리버설·위클리피벗·Pi Cycle·스테이블코인 디페그, 총 255). 발굴 방향 전환: 최근 FAIL 2종이 모두 gross부터 무엣지였던 점을 근거로 '지표 정규화·조합' 계열을 의도적으로 배제하고 구조적으로 다른 엣지 원천(자산 간 관계·미사용 사분면·매크로 사이클·캘린더 레벨)을 탐색. 스카우트가 'CME 갭 채우기'를 발굴했다가 원문검증에서 2026-05-29부로 CME 24/7 전환돼 갭 미생성임을 확인하고 자체 폐기. 8/6 00시 라운드: MACD-V 모멘텀 생명주기 스캘프(IS PF0.76·OOS PF0.65, 7종목 전부 PF<1 — 리뷰어가 무비용진단을 7종목·IS까지 확장 재계산해 **풀링 gross PF OOS 0.981/IS 1.024로 애초에 gross 엣지 없음** 확정. 구현 중 단위사고(`asi8` ms vs `asm8` ns → 거래 0건)·부분익절 수수료 이중차감 버그 2건을 백테스터가 자체 발견·수정, 리뷰어가 재구성 검증)·더블 볼린저(20/1σ+50/1σ)+RSI50(연 ~2,900회 극단 고빈도, **OOS gross PF 0.88·net 0.60으로 수수료 이전부터 무엣지**, 방향반전은 더 나쁜 0.35, σ 넓혀 빈도 낮춰도 개선 없음. 리뷰어가 슬리피지까지 0으로 강제한 순수 무비용에서도 OOS PF1.028 t=1.11로 무의미 확인, 계좌 소진은 회계버그 아닌 음의기댓값 복리사이징의 정상 붕괴로 트레이스 검증) 2종 **FAIL·VALID**. 신규 스펙 5종 추가(츠바이크 브레드스 스러스트·RRG 사분면 로테이션·MACD-V·GARCH(1,1) 변동성예측·더블볼린저+RSI50, 총 250). ⚠️리뷰어 신규 지적(파이프라인 전체 해당, 미조치): 라이브는 증거금 기준(`build_plan_by_margin`, `.env POSITION_MARGIN_PCT=10.0`) 사이징인데 `research/impl` 약 20개 백테스트는 전부 리스크 기준(`build_plan_with_stop`)만 사용 — 이 전략만의 문제 아니고 PF 결론은 불변이나 구조적 괴리로 기록. 8/5 12시 라운드: Gator Oscillator 4국면 스퀴즈-확장(IS PF0.84 순손실·OOS PF1.18 미달, confirm_bars=2만 근접통과했으나 종목간 불일치+top20제거 붕괴로 기각 — 리뷰어가 SATED부분익절 dead code 신규발견해 기각근거 강화)·WRB되돌림 눌림목 스캘프(7종목 OOS 전부 PF<0.44, 방향반전 구현버그 발견·수정 후에도 역방향 PF0.36 무엣지) 2종 **FAIL·VALID**. 신규 스펙 5종 추가(Gator Oscillator·펀딩브레드스·WRB되돌림·월말계절성·USDTM-COINM베이시스, 총 245). 8/5 00시 라운드: 볼륨프로파일세션POC+테이프스피드(3종목 전체 PF 0.49~0.66, gross부터<1)·15분봉 턴오브더캔들효과(원문효과 미재현 p=0.70/0.20, net PF 0.003~0.054로 계좌 소진) 2종 FAIL·VALID. 8/4 12시: 합성청산클러스터·상위트레이더 포지션비율 2종 FAIL·VALID. 8/4 새벽: BTC 디커플링·펀딩플립+OI가속·AlphaTrend 3종 FAIL·VALID. 8/3 오후: 딥스매수·코랄트렌드+ADX·앵커드VWAP 3종 FAIL·VALID. 8/3 오전: QuantPedia MACD·TSMOM 월간 FAIL·VALID. 8/2: z-score 계열 4종 최종 FAIL 확정, CUSUM·아시아세션·RSI DCA 래더 FAIL·VALID. **scalp15m이 여전히 유일 라이브 전략**. 미검증 대기: 극값 스트릭·볼린저 반대밴드·HMM 국면·울프웨이브·ATR그리드·VA마이그레이션·주말모멘텀·아미후드비유동성·펀딩브레드스·월말계절성·USDTM-COINM베이시스·츠바이크 브레드스 스러스트(표본 희소)·RRG 사분면 로테이션(원저자 공식 비공개)·GARCH(1,1)(구현 복잡·룩어헤드 위험)·크로스섹셔널 초단기리버설(포트폴리오 엔진 필요, 착수 시 수수료0 진단 선행 권고)·트위저 탑바텀(정량근거 없음)·탑트레이더 vs 글로벌 스프레드(metrics 덤프 7종목 확인됐으나 독립성 위험 큼)·홀리그레일 ADX+SMA20(원문 백테스트 통계 전무·표본부족 우려 사전 명시)·펀딩 부호 스트릭 지속·평균 체결규모 축적(count 필드 쓴 직전 스펙이 FAIL이라 후순위)·상위트레이더 극단 지속(기존 롱숏비율 스펙과 데이터 중복 위험)·헤드앤숄더 넥라인·쐐기형 추세선수렴·삼백병/삼흑아(3종 모두 패턴탐지 로직 복잡·정량근거 약해 후순위)·OI 증가율 횡단면 로테이션(포트폴리오 엔진 필요)·런던NY오버랩 거래량서지·오프닝 되돌림 리테스트(크립토엔 '진짜 개장'이 없어 세션 앵커 이식 자체가 미검증)·Deribit 옵션만기 핀닝(원문도 '핀닝 최근 자주 실종' 회의적)·BTC 200주MA(터치 4회로 검정 불가)·CME COT(정량근거 없음·데이터소스 신규구축 필요)·Pi Cycle·스테이블코인 디페그(둘 다 표본 수년에 1~2회라 통계검증 원천적 곤란, 모니터링 용도)·VPIN·ICT 브레이커·52주 신고가/돈치안 앙상블/캐리 합성/왜도(포트폴리오 엔진 필요)·코인베이스 프리미엄·SSR(외부 데이터 필요)·분기물 만기수렴 모멘텀(예상 표본 연 4~8건·인과 취약을 스펙이 자인)·연준 대차대조표(WALCL) 유동성 레짐(레짐 전환 드물어 판단불가 위험 사전 명시)·미국 10Y-3M 금리커브 레짐(레짐 전환이 6.9년에 1~2회뿐이라 판단불가 가능성 사전 명시)·CME BTC선물 거래량 서지(Yahoo 비공식 API·월물 롤오버 아티팩트 위험)·펀딩비 횡단면 분산 붕괴(기존 breadth 스펙의 대조군)·크로스거래소 OI 성장률 괴리(OKX 커넥터 신규구축 필요·히스토리 2.6년으로 짧음)·VIX 극단 레짐(유니버스가 사실상 BTC뿐)·Deribit 25델타 스큐(무료 과거 히스토리 부재로 현재 백테스트 원천 불가)·DVOL 변동성리스크프리미엄(Deribit 커넥터 신규구축 필요·BTC/ETH 2종목뿐)·CME-바이낸스 베이시스(Yahoo Finance 비공식 API 의존)·DXY 브레이크아웃(정량근거 없음·외부 데이터 필요)·`long-short-account-ratio-contrarian-swing`(정식 재검증 후보, 2022년 5%결측 구간 포함 IS 재설정 권고) 등. 참고: pandas 3.0.3은 `to_datetime(unit="ms")`가 `datetime64[ms]` 유지(ns 자동승격 아님) — `Timestamp.value`(항상 ns)와 섞으면 단위사고 재발 가능, 경계.
- **기타(8/1)**: 주간 API 한도 도달로 7/31 18:03Z~8/1 14:00Z 약 22h 루틴 중단 → 리셋 후 루틴별 1회 캐치업으로 복구(중복 재생 안 함). 사장님이 LIT(Lighter) 수동 트레이딩 중 — 차트 해석·미시구조 Q&A 진행(정보 제공만, 매매 판단 개입 없음). LIT 심층 리서치 노트 `research/notes/lit-research-2026-07-30.md` 커밋됨(토큰 정체·RH 파트너십 사실검증·HYPE 비교 밸류·12월 클리프 리스크). market-brief 검증 중 ETH ETF 과거이력 임의수정 발견→백업본 복원 사례 있음(append-only 검증 계속 철저히).
- **미조치 대기**: #45 Lighter 리더보드 커넥터, #46 폴리마켓 롱샷편향 실측 — 요청 없으면 착수 안 함.
- **사장님 결정 대기**: `research/etf/flows.json`의 `source` 필드가 **32,845자**로 실용 한계에 도달. 선택지 ①출처 이력을 별도 파일(`research/etf/flows_sources.md`)로 분리 ②최근 N개월만 남기고 나머지는 git 이력에 위임. **임의로 줄이지 않고 그대로 유지 중**(append-only 규약 준수).
- **정기 루틴 인프라 상태(8/22)**: OrangeX API 전면 중단 **64회차 이상**(8/22 기준. ⚠️**8/22부터 응답 형태가 바뀌어 HTTP 200에 `result` 빈 배열**을 반환 — 그 전까지는 코드1000 "No service found"였다. **원인 미규명**이며 어느 쪽이든 실질 조회는 계속 불가. 16개 항목 전부 `chg24: None` 표기. 대체도메인 `www.orangex.com`은 ACE는 반환하나 **펀딩이 없고 OI 단위가 불명[5배 과대 가능]**이라 아직 미채택) · 바이낸스 CoinGecko 티커 절단은 **8/22 회복** — 13종목 → **4종목**(AEON·APEX·CASHCAT·CORE)만 미수신. ⚠️**같은 API에 분기물이 섞여 있어 base로만 매칭하면 BTC/ETH가 오염되므로 `contract_type=='perpetual'` 필터 필수** · **HL HIP-3 빌더 DEX는 `{"type":"perpDexs"}`로 목록을 받고 `{"type":"metaAndAssetCtxs","dex":"hyna"}`로 조회**(HYNA:* 심볼은 삭제된 게 아니라 별도 네임스페이스. 휴면 마켓[vol·OI 0]과 상품 perp[GOLD/SILVER]는 제외) · dYdX는 CoinGecko 404라 공식 indexer(`https://indexer.dydx.trade/v4/perpetualMarkets`)로 대체 · Hyperliquid는 공식 API가 POST 전용이라 부모 세션이 `curl -X POST -d '{"type":"metaAndAssetCtxs"}'`로 받아 프롬프트에 명시 · Aster 직접 API 403 지속(CoinGecko id `aster`로 대체, 구 `aster_dex`는 404) · Hyperliquid CoinGecko 요약값이 5종목 거래량을 1000배 부풀린 사례 있어 **raw 필드 직접 확인 필수** · OKX는 ctVal 반영 + open-interest 엔드포인트 개별 재조회 필요 · BTW는 티커 충돌(Bitway/Banana Tape Wall/BitWhite)로 Bitway 기준 집계 · BANK '언락 D-N' 프레임은 CryptoRank 정적캐시 오류 반복으로 **폐기**(일정 미확인 표기 고정). ⚠️**선물 브리핑 fetch 절차 고정(8/24 사고 대응)**: fetch가 `build_skeleton.py`가 기대하는 파일명과 다르게 저장돼(okx_tickers vs `okx_raw`, binance_raw vs `cg_binance_futures` 등) 빌더가 **직전 회차 파일을 그대로 읽어** OKX·Binance·Bybit chg24가 전부 동결됐고, 에이전트가 이를 'CEX 데이터 갱신 정지'라는 시장 현상으로 보고했다(원자료는 정상 — OKX 응답 ts 02:32:26Z, 58종목 전부 상이). → ①원자료 수신은 **`$SP/fetch_all.sh`** 하나로만 수행(빌더 기대 파일명으로 직접 저장·검증·재시도·신선도 출력) ②`build_skeleton.py`에 **chg24 신선도 가드**를 넣어 변동 0건인 벤뉴가 있으면 경고. **신규 규칙: 벤뉴 단위로 값이 전혀 안 바뀌면 시장 현상으로 단정하기 전에 원자료 파일 수정시각·응답 ts를 먼저 확인할 것.** ⚠️같은 회차에 `merge_patch.py` 인자 순서를 반대로 넘겨(`merge(patch, brief)`가 정순) `narrative_patch.json`의 market·themes가 직전 회차 값으로 덮여쓴 2차 사고도 발생 — '오버라이드 0건 적용인데 미매칭도 0건'이라는 모순 출력이 단서였고, 에이전트 트랜스크립트의 Write 내용을 파싱해 전량 복구했다. **머지 후 `market`/`themes`가 직전 회차와 다른지 반드시 대조할 것.** ⚠️**모순 보고(중복 완료보고) 3회차 연속 발생(8/24 14:30Z·15:00Z·16:30Z)**: 동일 task-id에서 완료보고가 두 번 오는데 **수치가 서로 모순**된다(예: BTC 실측 부호가 반대, ZRO +2.29% vs 실제 +0.15%, CASHCAT -4.06% vs 실제 +5.20%). 세 번 모두 **나중 보고가 유효**했고 저장된 파일도 그쪽이었다. → **판정 절차: ①산출물 파일 수정시각 확인 ②디제스트/원자료로 인용 수치 대조 ③유효 보고 확정 후 폐기분 수치가 md에 혼입되지 않았는지 검사**. 보고를 그대로 믿고 커밋했다면 3회 전부 오염됐을 것이다. 에이전트 프롬프트에 '작업을 끝까지 마친 뒤 한 번만 보고'를 넣었으나 16:30Z에도 재발했다. ⚠️**kol-watch 에이전트 조기 중단 2회 발생(8/24 23:00Z·8/25 03:00Z)**: 두 번 다 **실행 시작 약 1분 뒤 출력이 멈추고** 완료 통지도 산출물 저장도 없었다(watch.json이 직전 회차 상태 그대로). 부모가 `tasks/<id>.output` 최종기록 시각과 `watch.json` 수정시각을 대조해 중단을 판정하고, **선례대로 중복 재생 없이 다음 회차로 이어받아 4시간 간격을 md에 정직 표기**했다. → **판정 절차: 완료 통지가 안 오면 ①`ls -l tasks/<id>.output`로 출력이 멈춘 시각 확인 ②산출물 수정시각이 직전 회차 그대로인지 확인 ③둘 다 해당하면 유실로 처리하고 다음 회차에서 이어받되 간격을 산출물에 표기**. 원인은 미규명(두 번 다 같은 에이전트 타입).
- **최근 유실/간격 이슈(8/21~22)**: 컨테이너 재시작으로 **KOL 21:00Z 회차가 유실**(watch.json이 19:00Z에 머묾, 선물 20:30Z는 재시작 전 커밋돼 무사). 선례대로 **중복 재생 없이 다음 회차에서 이어받음**(00:00Z 회차 md에 5시간 간격을 정직 표기). 이후 22:57Z 트리거를 00:09Z에 늦게 처리해 타임스탬프가 밀린 탓에 **00:57Z 트리거의 01:00Z 회차는 직전과 1시간 간격**이 됨(md에 압축 사실 정직 표기, 변화가 없으면 없다고 그대로 쓰도록 지시).
- **ETF flows 현황(8/27)**: BTC 672건 · ETH 534건 · SOL 208건(셋 다 **2026-08-26**까지) · XRP 190건(**2026-08-21에서 정지**). 8/27 12:15Z 라운드에서 부모가 SoSoValue openapi v2(POST)로 8/26분을 직접 확보·append했다(BTC +232.12 · ETH +192.35 · SOL +9.14). 겹치는 최근 5거래일을 원본과 대조해 일치 확인했고 `source` 32,845자는 바이트 단위 보존했다. ⚠️**XRP는 `us-xrp-spot`이 빈 배열을 반환해 계속 실패**(8/24~26 3거래일 미확보). ⚠️**이 파일은 부모가 직접 처리한다** — `source` 필드가 32,845자라 서브에이전트의 Read 한도를 넘어 **읽는 것 자체가 불가능**하고, 8/23에 에이전트가 메타데이터를 무단 절삭한 전례가 있다. 신규 규칙: 에이전트 완료보고의 "보존했다" 주장은 실제 바이트수 대조로 검증할 것.
- **검증기 4종 신설(8/27, `$SP/`에 상주)**: 산출물 검증을 눈으로 하던 것을 기계 검사로 옮겼다. `kol_check.py`(3파일 종목 수·빈 토큰명·이름 집합·CSV 8열) · `kol_verify.py`(유동성 A→B와 Δ% 재계산·h1/h6/h24 계측 대조, **매칭은 토큰명이 아니라 CA 기준** — 토큰명이 빈 문자열로 지워진 전례가 있다) · `futures_check.py`(**표 헤더를 읽어 칼럼별로** digest와 대조 + 발행본 방향 모순 감사) · `mb_check.py`(flows append-only·차티스트 6인 고정·brief.json 스키마·notable 미포함). ⚠️**이 검증기들이 실제로 잡은 것**: CSV 9열(쉼표 따옴표 누락)·7열(단계 열 누락, 2회) · LIT 실측 부호 반대(이월 칼럼에서 인용) · PRINTER h24 순서 뒤집힘(악화를 개선으로 서술) · 유동성Δ 40종 오염. **검증기 자체의 오탐도 두 번 고쳤다** — 키워드만 보는 방향 판정이 정확한 서술을 오탐한 건, "전회Δ→이번Δ" 두 값 표기를 첫 값만 읽은 건. **검증 도구가 틀린 경보를 내면 진짜 오류가 묻히므로 오탐도 즉시 고칠 것.**
- **선물 파이프라인 신규 함정(8/27)**: ①**이월 `직전 why` 칼럼에서 수치를 가져오는 오류가 반복**된다(ENA·CRV·CASHCAT·MON 4건, 이후 LIT 1건). 프롬프트에 그 사고를 구체적으로 명시하면 다음 회차에 재발하지 않았다 · ②**방향 모순 이월 서사 자동 폐기 가드**를 `mkdigest.py`에 넣었다(시장이 방향을 바꾼 회차에 이월 서사 39건이 정반대를 서술하고 있었고, 오버라이드 상한 25건이라 에이전트가 다 못 고친다). 가드는 **서사가 실측%를 명시하면 그 숫자의 부호로 판단하고 숫자가 없을 때만 키워드로 폴백**한다 · ③**폐기 문구에 수치를 넣으면 다음 회차에 그 숫자가 다시 낡은 주장이 되는 자기 증식**이 발생한다 → 폐기 문구에는 수치를 넣지 않는다 · ④**벤뉴 간 chg24는 비교 금지**(3회차 연속 확인 — CHIP Gate +12.00% vs MEXC -10.38%, ENA Gate +23.55% vs MEXC +3.53%. 가격 수준은 일치하므로 24h 창 기준 차이다) · ⑤**dYdX 펀딩은 CEX 8h 규약과 기간이 달라 순위·극단 논의에서 제외**(BTC 기준 자릿수가 100배 차이).
- **온체인 파이프라인 신규 함정(8/27)**: ①`kol_digest.py`의 기준선 추출이 직전 thesis "유동성$A→$B"에서 A(2회차 전 값)를 잡아 **42종 중 40종의 Δ가 2회차 누적**이었다(부호 반전 10건). → A→B면 **B**를 쓰고, 없으면 4자리 이상 마지막 매치, "금회$X" 폴백도 추가 · ②**먼지 풀이 허황된 값을 낸다**(LIZARD 2위 풀 $75가 h24 +96,246%, PEPECOIN 2위 풀 $486이 h1 -100%) → 인용 금지하되 관측 사실은 이상 징후로 기록 · ③**CYBERLEEK의 meteora 풀에서 -99.9%대 이상치가 반복**된다(17:00Z 발생 → 19:00Z 정상화 → 21:00Z 다른 풀에서 재발). 일회성 아닌 반복 아티팩트 · ④**h1 결측은 대개 거래 부재**다(PEE 24h 체결 54건, FLUSH 최근 1시간 체결 정확히 0건) — 수신 오류로 단정하기 전에 체결 건수를 확인할 것 · ⑤**가격 급등인데 24h 체결이 매도 우위**인 조합이 반복 관측된다(MAPLE 3회차 연속, PEPECOIN·Truth Coin) → 경계 신호로 표기.
- ⚠️**검증기 오탐 1건 추가 수정(9/4 06:30Z)**: `futures_check`의 `직전 …` 절이 **가운뎃점에서 끊겨** 과거 촉매 서술의 방향어가 살아남았다(DASH "직전 DashCon·shielded베타 촉매발 급등 이후 되돌림"에서 절이 DashCon에서 끊겨 '급등'이 이번 회차 주장으로 오탐). 소수점 건과 같은 부류다 — **절 경계 문자를 좁게 잡으면 고유명사·복합어에서 끊긴다**. 다만 "직전 -3.2% 하락·이번 +5.1% 상승"처럼 가운뎃점이 **다음 주장을 여는** 경우까지 삼키면 진짜 오류를 놓치므로 ①뒤에 부호·숫자가 오는 점 ②'이번' 표지 두 곳에서는 계속 끊는다. 양방향 주입 테스트(정상 통과 + 역방향 2건 검출)로 확인했다.
- ⚠️**내 인용 수치 오류 자체정정(9/4)**: 04:30Z 회차에 BICO를 "OKX 453종목 중 91위·60위 컷오프 $17.12M"이라 보고했는데, 그건 **주식화 토큰을 걸러내기 전 전체 USDT-SWAP 기준**이라 브리핑 유니버스와 맞지 않았다. 필터 적용 후 기준은 **60종목 중 59위(vol24 $8.46M, 컷오프 $8.37M)**다. 결론('컷오프 경계')은 두 기준 모두 같지만 수치가 틀렸고 06:30Z 발행본에 정정을 실었다. **신규 규칙: 순위·컷오프를 인용할 때 그 순위가 어떤 유니버스에서 나온 것인지 명시할 것.**
- **HFT 데이터 정체는 심볼 국한 확인(9/4)**: 직전 회차 brief.json과 대조해 **Binance 공통 60종목 중 값이 동일한 것은 HFT 1건**임을 확인했다(8/24 벤뉴 동결 사고 이후 규칙대로 시장 현상으로 단정하기 전에 대조를 먼저 함). 벤뉴 단위 수집 정체가 아니다.
- ⚠️**kol_pre 인자 자리 실수(9/4 07:00Z)**: `kol_pre.py`는 **첫 인자가 곧 출력 경로**인데 `--out <경로>` 꼴로 불러 저장소 루트에 `--out` 파일이 생겼고, 부모가 읽은 `kol_raw.json`은 **직전 회차의 낡은 파일**이었다. 그 결과 다이제스트가 **42/42 종목 vol24·h24·유동성 전부 baseline과 소수점까지 동일**하게 나와 '데이터 동결'로 보였다. 규칙대로 시장 현상으로 단정하지 않고 **DexScreener를 직접 조회해 대조**하니 원자료는 달라(PEPECOIN 실제 h24 -19.09% vs 파일 -10.42%) 즉시 판별했다. 9/3 `fetch_all.sh` 인자 자리 실수와 같은 부류다. → `kol_pre.py`에 **첫 인자가 `-`로 시작하면 사용법 출력 후 종료코드 2**, `--promote`에 경로가 없어도 트레이스백 대신 안내하는 가드를 넣었다. 발행본 영향 없음(산출물 작성 전에 잡음). **신규 규칙: 전 종목이 동시에 동결로 보이면 소스 장애보다 먼저 내가 넘긴 인자와 읽고 있는 파일의 수정시각을 의심할 것.**
- ⚠️**kol_verify 오탐 1건 수정(9/4 07:00Z)**: 매도우위 플래그 주장 검사가 **요약 줄이 빠진 종목을 함께 짚는 정확한 서술**("LIZARD·PROLOGUE·HOOKR·TOAD 4종이다. **FLUSH는 이번 회차 명시 플래그가 사라졌다**")에서 FLUSH를 '포함 주장'으로 세어 오탐을 냈다. 1차 수정(토큰명 뒤 60자에 부정어가 있으면 제외)은 **다음 문장의 부정어를 끌어와** 정상 나열의 TOAD를 누락으로 오판했다 → **판정 단위를 문장으로** 바꿔 해소. 나아가 '제외 주장'을 직접 대조하는 검사를 추가해 **HOOKR 유형(실제 플래그인데 해제됐다고 서술)을 종목수 불일치가 아니라 근거까지 짚어** 잡도록 했다(같은 종목이 구간 다른 문장에도 나오면 누락 검사만으로는 안 걸린다). 양방향 주입 테스트 3종으로 확인.
- ⚠️**내 단위 혼동 자체정정(9/4)**: 이번 watch.md를 "43,092자 → 28,167자로 35% 감소"로 보고 내용 유실을 의심했으나, 앞은 **바이트**(`ls -l`)이고 뒤는 **문자 수**(검증기)였다. 실제로는 26,816자 → 28,167자로 **+5% 증가**했다. `kol_check`가 과거 같은 이유로 고쳐진 이력이 있는데 내가 같은 혼동을 반복했다. **바이트와 문자 수를 같은 줄에서 비교하지 말 것.**
- ⚠️**futures_check 결함 3건 수정 — 주입 테스트가 아니었으면 못 찾았을 것들(9/4 08:30Z)**: 오탐 1건을 고치다 그 수정이 **진짜 오류까지 못 잡게 만든 것**을 주입 테스트로 발견하고 근본 원인 2건을 더 찾았다. ①**발행본은 줄바꿈으로 감싸져 있어 "**CHIP**…chg24…" 다음 줄에 "…로 뚜렷한 하락 전환입니다"가 온다 — 줄 단위 순회라 심볼 있는 줄엔 방향어가 없고 방향어 있는 줄엔 심볼이 없어 방향 검사가 통째로 건너뛰어지고 있었다**(언제부터인지 불명. 목록 항목+이어지는 들여쓰기 줄을 한 논리 줄로 병합해 해소) ②병합하니 문장이 길어져 **고정 60자 창이 방향어에 못 닿음** → 문장 끝까지 보되 **다음 종목명이 나오면 거기서 끊어** 남의 방향어를 이 종목 주장으로 읽지 않게 함 ③**두 지표를 대비하는 서술**("chg24는 +4.67%로 플러스인데 실측(2h)은 -4.95%로 하락")에서 chg24에 붙은 '플러스'까지 실측 기준으로 판정해 오탐 → **방향어마다 바로 앞에 나온 지표로 기준을 정하고 기준별로 각각 대조**(chg24 기준 방향어를 버리면 그쪽 오류를 아예 안 보게 된다는 것도 주입으로 확인). 3종 주입(실측 반대·chg24 반대·타 종목 실측 반대) 전부 올바른 기준을 짚어 검출하고 정상 발행본은 통과. **신규 규칙: 검증기 오탐을 고쳤으면 반드시 반대 방향 주입으로 '진짜 오류를 여전히 잡는지' 확인할 것 — 오탐 제거가 검사 무력화로 이어진 사례가 실제로 나왔다.**
- ⚠️**플래그 이탈 사유를 잘못 짚은 에이전트 서술 정정(9/4 09:00Z)**: '가격급등-체결매도우위' 플래그에서 HOOKR·TOAD가 빠진 것을 에이전트가 **"h1 체결이 매수우위로 전환돼서"**라고 md 5곳·json 2종목에 적었다. 그러나 **플래그 조건은 `h24 상승폭>20% AND h24 체결 매도우위`이고 h1은 조건에 없다** — 실제 이탈 사유는 h24가 문턱 아래로 내려간 것(HOOKR +18.89%·TOAD +17.78%)이며 **h24 체결 매도우위는 그대로다**(3710/3790·2312/2388). h1 매수우위 전환은 사실이지만 인과가 아니다. 위험이 해소된 것처럼 읽히므로 원인과 잔존 위험을 명시해 정정했다. **신규 규칙: 플래그·게이트가 빠졌다고 쓸 땐 어떤 조건이 풀렸는지 조건식과 대조할 것 — 같은 회차에 함께 변한 다른 지표를 원인으로 착각하기 쉽다.**
- ⚠️**kol_verify 오탐 재수정(9/4 09:00Z)**: 07:00Z에 넣은 '문장 단위 부정어 판정'이 **한 문장이 정상 나열과 제외 나열을 함께 담는 경우**를 처리 못했다("LIZARD·PROLOGUE 2종이다(직전 4종에서 HOOKR·TOAD가 빠졌다)" → 네 종목이 통째로 제외로 분류돼 3건 오탐). → 부정어의 **주어를 절(괄호·마침표 경계) 안 바로 앞의 토큰 나열**로 좁혔다. 양방향 주입 2종(실제 플래그를 해제됐다고 서술·미플래그를 플래그라 주장)으로 검출력 재확인. **같은 함정이 이틀 새 세 번 형태를 바꿔 나타났다 — 판정 범위를 '문장'처럼 큰 단위로 잡으면 반드시 반례가 나온다.**
- **HFT 동결 해소 확인(9/4 10:30Z)**: 06:30Z·08:30Z 2회차 연속 동결돼 '소스 문제 근거가 쌓였다'고 적었던 HFT가 **정상 갱신**됐다(Binance 공통 57종목 중 직전과 chg24 동일 0건, OKX·Bybit도 0건) → **일시적 문제로 판명**. 발행본에 그렇게 갱신했다. **교훈: 2회차 연속이라고 구조적 문제로 굳히지 말 것 — 관측이 바뀌면 이전 회차의 잠정 판단도 함께 갱신한다.**
- ⚠️**내 섹션 대조 오판 자체정정(9/4 10:30Z)**: md가 12,339→8,619자로 30% 줄어 섹션 구조를 대조했는데, 「핵심 변화」 제목에 **회차가 들어가는 것**(`핵심 변화 (06:30Z → 08:30Z)` → `(08:30Z → 10:30Z)`)을 놓치고 제목 전체로 매칭해 **'섹션 통째 누락'으로 오판**했다. 이름 기준으로 다시 보니 **누락 0건**이고 시장 변화가 적어 서술이 짧아진 것이었다. `mb_check`에서 "섹션 식별은 구분자 앞 이름으로"라고 이미 고쳤던 것과 **같은 부류를 내가 손으로 반복**했다. → 그 김에 `futures_check`에 **섹션 존재 검사**를 넣었다(섹션이 빠지면 그 안의 표가 없어 표·방향 검사는 그냥 통과해버린다). 괄호 앞 이름으로 대조하고 양방향 주입(섹션 제거 검출 · 회차 표기 변경 무탐지)으로 확인.
- ⚠️**BARRON 풀수 이월 오류 적발(9/4 11:00Z)**: 이번 실측 **6풀**인데 md·json에 **직전 회차 값 7풀**이 그대로 남아 있었다 — `kol_verify`의 풀수 대조가 잡았다. 수치가 아니라 **구조 필드도 이월된다**는 첫 사례다(그동안은 가격·유동성 이월만 경계했다).
- ⚠️**검증기 오탐 4건 수정 + 그 수정이 검출력을 죽인 것을 다시 주입으로 발견(9/4 11:00Z)**: ①`kol_check`가 md 헤더의 **표시 형식**(`2026-09-04 11:00 UTC`)을 못 읽어 정상 발행본을 오탐 — `mb_check`에서 같은 이유로 이미 고쳤던 부류를 다른 도구에서 반복 ②**"42종중 최대 변동"은 수준이 아니라 델타 최대**인데 유동성 수준으로 판정(TOAD +26.3%) → 최상급 뒤에 변동/증가/유입 등이 오면 dliq 최대와 대조 ③매도우위 주장 구간이 **200자에서 잘려** "…미충족이다"가 밖에 남아 오탐 → 400자로 확대 ④**seg 전체에서 토큰을 모으고 부정분만 빼던 방식**은 긍정·부정 나열에 **함께 등장한 종목**을 조용히 제외해 오류를 놓쳤다 → 절 단위로 긍정/부정을 나눠 판정. ⚠️**이때 괄호를 절 구분자로 쓰면 "…조건(설명) 미충족"의 부정어가 다음 조각으로 넘어가 긍정으로 오분류**되므로 괄호는 **지우고** 마침표로만 나눈다. **주입 ②③이 처음엔 미검출이었고 그 사실을 주입 테스트가 잡아냈다 — 어제 세운 '오탐 수정 뒤 반대 주입' 규칙이 이틀 연속 실제로 작동했다.** 최종 4종 주입(플래그를 미충족이라 서술·미플래그를 플래그라 주장·변동 최상급 오귀속·수준 최상급 오귀속) 전부 검출하고 정상 발행본은 통과.
- ⭐**에이전트가 자기 사고를 정직히 보고한 사례(9/4 11:00Z)**: 첫 Write에서 `watch.json`의 `notable` 배열을 절삭해 **JSON이 깨진 것을 스스로 발견·복구**하고 완료보고에 그 경위를 남겼다. 부모가 검증하니 **notable 80건이 직전과 바이트 단위로 동일**해 복구가 정확했다. 8/23에 에이전트가 메타데이터를 무단 절삭하고도 말하지 않았던 것과 대비된다 — **자체 보고가 있으면 그 지점을 우선 검증하면 되므로 훨씬 안전하다.**
- ⚠️**내 프롬프트 누락으로 LIT 섹션이 통째로 빠졌다(9/4 12:15Z)**: 시장브리핑 프롬프트에 산출물 3종만 적고 **`brief.md`의 LIT 섹션 요건을 명시하지 않아** 에이전트가 그 섹션을 아예 만들지 않았다 — `mb_check`의 LIT 필수 검사가 잡았다. LIT은 사장님 수동 트레이딩 종목이라 **수치·사실만 기재(해석·전망·매매 판단 없음)**하는 섹션이므로, 부모가 OKX·CoinGecko를 직접 조회해 채워 넣었다(두 소스 일치 확인: OKX $4.6452/chg24 +17.42%, CG $4.63/+17.47%). **신규 규칙: 시장브리핑 프롬프트에는 산출물 파일만이 아니라 `brief.md`의 필수 섹션(특히 LIT)을 매번 명시할 것.**
- **ETF flows 9/3분 확보(9/4 12:15Z)**: BTC **+730.87** · ETH **+141.39** · SOL **+6.40**(백만 USD). append 전 **겹치는 최근 5거래일을 원본과 대조해 전부 일치** 확인했고, append 후 prefix 보존·`source` 32,845자 바이트 동일·중복 없음을 검증했다. BTC 678 · ETH 540 · SOL 214건으로 모두 2026-09-03까지. ⚠️**XRP는 여전히 빈 배열**(2026-08-21 정지, 같은 호출로 나머지 3종은 정상이라 소스 측 문제).
- ⭐**에이전트가 자기 파싱 오류를 정직 보고한 두 번째 사례(9/4 12:15Z)**: 첫 조회에서 OKX 다종목 티커 응답을 잘못 파싱해 **ETH $3,511·SOL $234.56**이라는 오염값을 얻었으나 종목별 단일 조회로 스스로 재확인·정정했고, 그 경위를 발행본 본문에 남겼다. 부모가 거래소를 직접 조회해 대조하니 **발행 수치엔 오염값이 반영되지 않았다**(BTC $81,157·ETH $2,522·SOL $103.98 — 시차 범위 내 일치).
- ⚠️**내가 미검증 주장을 그대로 옮긴 사고(9/5) — BTW '수신 실패'**: 에이전트 보고를 두 회차(14:30Z·16:30Z) 커밋에 그대로 실었는데, 원자료를 보니 BTW는 **Binance $31.3M · Bybit $9.6M · Aster $0.48M로 정상 수신**돼 있었다. 실제로는 **벤뉴별 상위 60종목 거래대금 컷오프에서 탈락**한 것이다(Binance 컷오프 $38.2M · Bybit $12.9M). md 3곳을 정정했다. **신규 규칙: '수신 실패'·'데이터 없음' 주장은 커밋 전에 원자료 파일에서 해당 심볼을 직접 grep해 확인할 것 — 컷오프 탈락과 수집 실패는 산출물에서 똑같이 보인다.**
- ⚠️**검증기 보강 6건(9/5)**: `kol_verify` — ①풀수 표기를 화살표 체인(`N풀→M풀`·`N풀에서 M풀`)까지 읽도록 확장 ②**md 구조 필드 대조를 신설**해 42/42 전 종목의 CA 기준 풀수를 raw와 대조(`단일풀`=1풀 처리 포함) — 기존엔 thesis에 풀수를 쓴 종목만 걸렸다 ③최상급 판정에 **과거 회차 문구 제외**('직전…'으로 시작하되 뒤에 '이번'이 없으면 과거)·**기준 지표 해소**(지표어가 없으면 직전 $금액을 liq/vol24와 대조)·**델타 방향 분기**(증가/유입/급증→최대 양수, 유출/감소/급감→최소, 그 외 절대값 최대)를 추가 ④유동성Δ 대조에서 **항목별 회차 라벨**(`+8.22%,2회차전`)이 붙은 값을 건너뛰도록 수정. `futures_check` — ⑤심볼 경계에 **숫자 문맥 배제**를 추가(Binance에 심볼이 `4`인 토큰이 실재해 소수점·부호 옆의 `4`를 심볼로 오인했다) + 허용오차 0.02→0.05(다이제스트가 vol24·OI를 `%.1f`로 렌더해 표시 반올림이 그 폭이다) ⑥**크기 명사 배제**('상승폭'·'하락률'의 '폭'·'률'이 뒤따르면 방향어로 세지 않음) — 이건 오탐 수정이 아니라 **검출 공백을 메운 것**으로, '상승폭'이 진짜 하락 오류를 중화시키고 있었다.
- ⚠️**`build_digest.py` 주식화 토큰 제외 목록에 금 연동 크립토 추가(9/5)**: `XAU`·`GOLD` 같은 상품 티커만 막아뒀더니 **PAXG·XAUT(금 담보 토큰)가 brief.json에 통과**했다. `XAU XAG XAUUSD XAGUSD GOLD SILVER OIL WTI PAXG XAUT`로 확장했다. **교훈: 제외 규칙을 '기초자산 티커'로만 세우면 그 기초자산을 담은 크립토 토큰이 빠져나간다.**
- ⚠️**주입 테스트 절차 자체의 함정(9/5)**: 검증기 수정을 확인하려고 넣은 주입 문자열이 **실제 파일과 안 맞아 적용되지 않았는데**, 하마터면 '검출 실패'로 오독할 뻔했다. **신규 규칙: 주입 테스트는 '검출됐는가'를 보기 전에 '주입이 실제로 적용됐는가'(diff 또는 grep)를 먼저 확인할 것.**
- 📋**파이프라인 도구 인자 순서표(9/4 신설 — 하루에 두 번 헷갈려 만듦)**: 관례가 도구마다 달라서 외우지 말고 **매번 여기를 보고 부를 것**. `$SP`는 스크래치패드.
  - `bash fetch_all.sh $SP` — 9개 피드 수집·검증·재시도·신선도 출력
  - `build_digest.py $SP <ts> <직전ts>` — ⚠️**SP가 먼저**(다른 도구 대부분과 반대)
  - `kol_pre.py <출력경로>` / `kol_pre.py --promote <kol_raw.json>` — ⚠️**첫 인자가 곧 출력 경로**(`--out` 붙이면 안 된다, 가드 있음)
  - `kol_digest.py <ts> <kol_raw.json> > <출력.md>` — ts 먼저이고 ⚠️**결과를 stdout으로 뱉는다**(파일을 안 쓴다). 리다이렉트를 빼먹으면 `kol_digest.md`가 직전 회차 파일 그대로 남고, 그걸 읽으면 헤더 ts까지 낡은 값이라 '동결'로 오독하기 쉽다(9/5 11:00Z에 실제로 겪음 — `ls -l`로 수정시각을 보고 잡았다)
  - `merge_patch.py <patch> <staging> <out>` — 순서 뒤바뀌면 내용으로 판별해 거부함
  - `verify_merge.py <merged> [ts]` · `publish_brief.py <merged> <ts>`
  - 검증기: `kol_check.py <ts> [종목수=42]` · `kol_verify.py <kol_raw.json>` · `futures_check.py <ts> <staging> [prices_prev] [prices_cur]` · `mb_check.py <ts>`
- **다음 할 일**: 도착하는 정기 루틴 프롬프트를 계속 위 절차대로 처리. 매 회차 산출물은 위 검증기 4종으로 기계 검사한 뒤 커밋한다. 새 지시가 없으면 라이브 전략·리스크 파라미터는 임의로 바꾸지 않는다.
- **관찰 중인 이상 현상(8/27)**: ①**BICO 촉매가 13회차 넘게 미확인**이다. 실측이 회차마다 뒤집히고(-8.72%→+4.14%→-1.91%→-1.36%→-3.39%→+1.77%→+4.35%), 펀딩은 -0.7490%에서 6회차 연속 완화돼 -0.1240%까지 갔다가 22:30Z에 -0.2420%로 다시 심화됐다. **가격이 최대폭으로 오르는데 숏 쏠림이 깊어지는** 조합이다 · ②**XMR은 펀딩이 5회차 연속 최고인데 가격은 자주 하락**한다(롱 쏠림만 남은 형태) · ③**CYBERLEEK**은 h24 -72%까지 붕괴했다가 4회차에 걸쳐 -34%까지 회복했는데, vol24는 $14.5M→$8.2M로 계속 줄었다(거래 과열이 식으면서 가격이 회복되는 형태).

### 🔁 봇 재시작 코드 (항상 여기 비치)
재시작은 **해당 창에서 Ctrl+C로 멈추고 → 재실행**. 어느 프로세스를 재시작할지는 §1 「재구동 안내 규약」의 변경위치 판정표 참조.

**공통 — `git pull` (한 번, 아무 창):**
```powershell
cd C:\Users\ghdls\Documents\study-claude-personal-ai-assistant-11xcgh\study-claude-personal-ai-assistant-11xcgh
git pull origin claude/personal-ai-assistant-11xcgh
```
**① 매매봇** (매매봇 창 Ctrl+C 후):
```powershell
cd C:\Users\ghdls\Documents\study-claude-personal-ai-assistant-11xcgh\study-claude-personal-ai-assistant-11xcgh
.\.venv\Scripts\Activate.ps1
python -m scripts.run_portfolio
```
**② 알람봇(스캐너)** (알람봇 창 Ctrl+C 후):
```powershell
cd C:\Users\ghdls\Documents\study-claude-personal-ai-assistant-11xcgh\study-claude-personal-ai-assistant-11xcgh
.\.venv\Scripts\Activate.ps1
python -m scripts.run_scanner
```
**③ 대시보드** (대시보드 창 Ctrl+C 후):
```powershell
cd C:\Users\ghdls\Documents\study-claude-personal-ai-assistant-11xcgh\study-claude-personal-ai-assistant-11xcgh
.\.venv\Scripts\Activate.ps1
python -m scripts.serve_dashboard
```

---

## 0. 커뮤니케이션 규약 (최우선 · 압축되어도 유지)

1. **항상 존댓말을 쓴다.** 사용자에게 보고·설명·질문할 때 반말·구어체 금지.
   대화가 압축되어 말투가 리셋된 것처럼 느껴져도, 이 규칙을 먼저 확인하고 존댓말로 답한다.
2. **중간 진행 상황은 항상 한글로 보고한다.** 작업을 시작할 때·단계가 바뀔 때·끝났을 때
   무엇을 하고 있는지 한글로 짧게 알린다. (코드·커밋 메시지·식별자는 예외)
3. **진행 중인 업무는 압축의 영향을 받지 않게 관리한다.**
   - 큰 작업은 시작 전에 계획을 남기고, 단계마다 커밋해 중간 상태를 디스크에 보존한다.
   - 미완료 작업이 있으면 그 상태(무엇을 어디까지 했고 다음이 무엇인지)를 한글로 명확히 남긴다.
   - 압축 후 이어받을 때는 이 파일 → 최근 커밋 → 코드 상태 순으로 확인하고, 사용자에게
     되묻지 말고 마지막 작업을 이어서 진행한다.
   - **컨텍스트 관리(압축 대비)**: 컨텍스트 사용이 대략 **70%를 넘겼다고 판단되면**, ① 먼저 아래
     「🧭 진행상황 스냅샷」을 현재 상태로 갱신·커밋해 디스크에 보존한 뒤 ② 사용자에게 `/compact`
     실행을 짧게 안내한다(안전한 지점 — 서브에이전트 대기 없고 미커밋 변경 없을 때). 실제 자동
     compact 트리거는 하네스(자동 압축)가 담당하므로, 내가 할 일은 **압축 전에 스냅샷을 최신화**해
     압축돼도 상태가 유실되지 않게 하는 것이다. 큰 사이클(발굴·백테스트 등)을 새로 시작하기 전에도
     스냅샷을 한 번 갱신한다.
4. **`git pull` 안내 시 재구동 코드를 항상 함께 준다.** 코드 변경을 push하고 `git pull`을
   안내할 땐, 어떤 프로세스(①매매봇/②알람봇/③대시보드)를 재시작해야 하는지 유형별로 명시하고
   붙여넣기용 명령 블록을 같이 준다. 상세 규약은 §1의 「재구동 안내 규약」 참조.

---

## 1. 프로젝트 개요

**crypto-trader** — 개인용 자동 암호화폐 트레이딩 봇.
- 사용자 Windows PC에서 로컬 구동, **바이낸스 선물 USDT-M 테스트넷** 대상.
- 자체 완결형 HTML 라이브 대시보드(인라인 SVG 차트·인라인 JS, Pretendard CDN 외 외부 라이브러리 없음).
- **단타 전용 포트폴리오(100%)** — **15m 단독 슬리브(100%)**. 10m은 전 구간 약해 제거(78d874c, 2026-07-13),
  중기·스윙은 손익비 미달로 제외(정의는 git 이력에 있어 복원 가능). 헤지·isolated 모드.
- 시장 스캐너(급등락·거래량/OI 급증·펀딩 극단 감지) → 텔레그램 알림 + 대시보드 갱신.

### 현재 전략·리스크 설정 (2026-07 기준, 바뀌면 여기 갱신)
- **단타 슬리브**: `scalp15m`(15m 진입/1h 확인) **단독**. 볼린저 이탈 강봉+거래량 급증 돌파.
  진입필터 `vol_spike≥5·squeeze≤35%·body≥1.3ATR·regime=False`. 메이커 진입(post-only), 10분할×20초.
  청산: **ATR 트레일링×1.0**(`scalp_exit_mode=trailing`, 신고가−ATR×1.0) + 횡보전환 시 모멘텀 꺾이면 청산.
  SL=신호봉 시가(stop_mult=1.0). (구 split tp1/tp2 청산은 트레일링으로 교체됨 — scalp-exit-mode-ab 백테스트.)
- **레버리지**: 시총상위(major_bases) **30x**, 알트 **10x** (`Settings.leverage_for`).
- **동적 유니버스**: 24h 거래대금 **$10M 이상**(SOL 제외). 사이징은 증거금 기준(`position_margin_pct`).
- **15m은 바이낸스 네이티브** TF라 리샘플 불필요(제거된 옛 10m 슬리브는 5m에서 리샘플했었음, `TF_RULE`).
- 백테스트 검증(교차검증): 3·5분 적자, **15분이 가장 유효(흑자)**, 10분은 국면 의존. 발굴 전략
  ATR-RSI는 우리 프레임워크에서 FAIL(외부 Sharpe 주장 재현 안 됨).

### 백테스트 데이터 소스 (fapi 지역차단 대응 — 중요)
- 이 클라우드 컨테이너에선 **바이낸스 선물 fapi가 451(지역차단)** — klines·ticker 모두 불가.
- **선물 캔들**: `https://data.binance.vision/data/futures/um/{monthly|daily}/klines/<SYM>/<TF>/...zip`
  덤프를 받는다. **선물 전용 알트(XPIN·TAC·EVAA·LAB 등)까지 포함**되므로 실제 유니버스 백테스트 가능.
  (현물 미러 `data-api.binance.vision`는 선물전용 알트가 없어 부적합 — 유니버스는 '선물' 기준이다.)
- **선물 유니버스·24h 거래량**: CoinGecko 파생 API(`/derivatives/exchanges/binance_futures?include_tickers=all`,
  `converted_volume.usd`)로 순위. 10M 이상 ≈ 116종목.
- 라이브 봇은 스캐너/`connectors/universe.py`로 유니버스 선정(fapi→CoinGecko→현물미러 폴백).

### 정기 리서치 루틴 & 최근 반영된 개선
- **정기 루틴 4종**(온체인 트렌딩 조기경보(舊 KOL 하이프워치, kol-watch) 2h·선물 리서치(futures-scout) 2h·시장브리핑 12h·전략발굴 12h)은 **이 상시 세션에 바인딩**
  (`persistent_session_id`)돼 발화 → 이 세션에서 서브에이전트 실행 후 커밋·push. 폰 완료알림은 없음
  (fresh-session 방식은 새 세션에 레포 쓰기권한이 없어 push 403 → 상시세션 바인딩으로 해결).
  결과물: `research/kol/`, `research/market/`, `research/strategies/`, `research/backtests/`.
- **시장브리핑 루틴이 갱신하는 파일 4종**: `research/market/brief.{md,json}` + `research/kol/chartist_views.json`
  (대시보드 '상위 차티스트 현재 뷰' — 인물 고정, 뷰만 갱신) + `research/etf/flows.json`(대시보드
  'ETF Flow' — 과거 이력 보존, 최신일만 append). 이 둘은 원래 자동갱신 루틴이 없어 멈춰 있던 것을
  시장브리핑 루틴에 편입함. 커밋 시 `research/market/ research/kol/chartist_views.json research/etf/` 함께 add.
- **⚠️ 날짜 환각 주의**: kol-watch·market-brief 서브에이전트는 Bash가 없어 현재 날짜를 스스로 못 구해
  ts/날짜를 자주 하루 이상 앞서 환각한다(예: watch.json ts·watch.md 헤더가 미래 날짜로 오기). 이들
  에이전트를 실행할 땐 **반드시 `date -u` 로 현재 UTC 시각을 먼저 확인해 프롬프트에 명시**하고, 완료
  후 커밋 전 저장된 ts/헤더 날짜가 현재와 맞는지 확인한다. 슬롯 판정용으로 어차피 매번 `date -u` 를
  찍으므로 그 값을 그대로 넘기면 된다.
- **최근 수정**(git 이력): 청산 짜바리 방지(분할청산 스텝사이즈 정합), 수동/외부 청산 정확 반영
  (`fetch_realized_close`로 실제 체결가·실현손익), 대시보드 '최근청산' 청산시각 정렬, 일별/누적손익이
  실잔고 이력 부족 시 저널 기준으로 폴백, 고아 슬리브 리컨실(제거·개명된 옛 슬리브로 열린 포지션은
  워커가 안 훑어 영영 '열림'으로 남던 것 → 엔진 `_reconcile_orphan_exits`가 거래소 실체결로 청산 반영).

### 로컬 실행 환경 (사용자 Windows PC)
- 프로젝트 경로(고정): `C:\Users\ghdls\Documents\study-claude-personal-ai-assistant-11xcgh\study-claude-personal-ai-assistant-11xcgh`
- 실행 명령 안내 시 이 경로를 그대로 쓴다(플레이스홀더 `C:\경로\study` 대신).
- PowerShell은 `&&` 미지원 → 줄을 나누거나 `;` 사용. 가상환경: `.\.venv\Scripts\Activate.ps1`.
- 3개 프로세스는 각각 별도 창: ① 매매봇 `python -m scripts.run_portfolio`
  ② 알람봇(스캐너) `python -m scripts.run_scanner` ③ 대시보드 `python -m scripts.serve_dashboard`.

### 재구동 안내 규약 (git pull 안내 시 항상 함께 준다)
코드 변경 후 `git pull`을 안내할 땐 **반드시 어떤 프로세스를 재시작해야 하는지 유형별로 명시**하고
아래 붙여넣기용 블록을 함께 준다. 재시작은 **해당 창에서 Ctrl+C로 멈추고 → 재실행**.
- 어느 프로세스를 재시작할지는 변경 위치로 판단:
  - `portfolio/`·`strategy/`·`risk/`·`connectors/`·`execution/`·`config/` → **① 매매봇**
  - `scanner/`·`monitoring/`(스캐너 경로)·detectors → **② 알람봇**
  - `monitoring/dashboard.py`·`serve_dashboard`·대시보드 API → **③ 대시보드**
  - 대시보드는 저널만 읽으므로, 매매 로직만 바뀌면 매매봇만 재시작하면 새로고침에 반영된다.

**공통(한 번, 아무 창):**
```powershell
cd C:\Users\ghdls\Documents\study-claude-personal-ai-assistant-11xcgh\study-claude-personal-ai-assistant-11xcgh
git pull origin claude/personal-ai-assistant-11xcgh
```
**① 매매봇** / **② 알람봇** / **③ 대시보드** (각 창에서 Ctrl+C 후):
```powershell
cd C:\Users\ghdls\Documents\study-claude-personal-ai-assistant-11xcgh\study-claude-personal-ai-assistant-11xcgh
.\.venv\Scripts\Activate.ps1
python -m scripts.run_portfolio      # ① 매매봇 (또는 run_scanner=② / serve_dashboard=③)
```

### 주요 실행 스크립트
- `python -m scripts.run_scanner` — 시장 스캐너 상시 구동 + `state/dashboard.html` 갱신.
- `python -m scripts.serve_dashboard` — 로컬 대시보드 웹서버(`http://localhost:8787`).
  파생 지표(`/api/derivs`)·캔들(`/api/klines`)·심볼(`/api/symbols`) API는 이 서버 모드에서만 실시간 동작.
- `python -m scripts.recompute_pnl` — 청산 손익 수수료 소급 보정(idempotent).
- `python -m scripts.yt_transcript <url|id> [--meta] [--out f]` — 유튜브 자막(트랜스크립트) 추출.
  **유튜브 영상 분석·전략 발굴은 반드시 이 도구로 트랜스크립트를 먼저 확보한다**(서드파티
  트랜스크립트 사이트는 CAPTCHA로 자주 막힘). yt-dlp 기반, `HTTPS_PROXY` 자동 사용.

---

## 2. 보안 · 상시 제약 (반드시 준수)

- **`.env` 는 절대 커밋하지 않는다** (`.gitignore` 에 포함됨). 테스트넷 키·텔레그램 토큰은
  사용자 본인의 저위험 자격증명이지만 저장소에 올리지 않는다.
- **모델 식별자(`claude-opus-4-8` 등)를 커밋 메시지·PR·코드·주석 등 저장소 산출물에
  절대 넣지 않는다.** 채팅 답변에만 사용.
- 모든 커밋은 지정된 `Co-Authored-By` / `Claude-Session` 푸터로 끝낸다.
- **명시적 요청 없이는 PR을 만들지 않는다.**
- 테스트넷에는 **한 번에 하나의 봇만** 구동한다.
- **Crescent Hook HR 프로젝트는 건드리지 않는다.**

---

## 3. Git

- 개발·푸시 브랜치: **`claude/personal-ai-assistant-11xcgh`** (여기 외 다른 브랜치에 푸시 금지).
- 푸시: `git push -u origin claude/personal-ai-assistant-11xcgh`, 네트워크 실패 시 지수 백오프(2/4/8/16s)로 최대 4회 재시도.
- 커밋 메시지는 한글로 명확하게. 변경이 완결되면 커밋·푸시.

---

## 4. 코드 스타일

- Python 3.11, `from __future__ import annotations`.
- 주석·로그·사용자 노출 문자열은 한글. 넓은 예외 포착에는 `# noqa: BLE001`.
- 대시보드는 외부 의존성 없는 자체 완결형 HTML을 유지한다(인라인 SVG/JS).
- 기존 코드의 관용구·네이밍·주석 밀도를 따른다.

---

## 5. 작업 원칙 (Karpathy 4원칙 — LLM 코딩 함정 방지)

출처: `multica-ai/andrej-karpathy-skills`. Andrej Karpathy가 지적한 LLM 코딩의 흔한 실수
(멋대로 가정·과잉설계·불필요한 수정)를 막기 위한 규약. **속도보다 신중함이 우선.**
사소한 작업엔 판단껏 완화하되, 비트는 판단이 필요할수록 이 원칙을 지킨다.

1. **코딩 전에 생각한다 (Think Before Coding)** — "가정하지 말고, 혼란을 숨기지 말고, 트레이드오프를 드러내라."
   - 요구가 모호하면 **멋대로 정하지 말고** 해석 후보를 제시하고 되묻는다.
   - 리스크·전략 파라미터(레버리지·유니버스·손절 등)는 **사용자 결정 사항**이다. 내가 임의로
     '안전장치'를 바꾸지 않는다. 우려가 있으면 데이터로 **짧게 정보 제공**만 하고 실행은 요청대로.
2. **단순함이 먼저 (Simplicity First)** — "문제를 푸는 최소 코드, 투기적인 것 없이."
   - 요청 안 한 기능·추상화·안 일어날 엣지케이스 방어를 덧붙이지 않는다.
3. **수술적 변경 (Surgical Changes)** — "꼭 필요한 것만 건드리고, 네가 만든 흔적만 치워라."
   - 무관한 코드 리팩터·스타일 변경 금지. 기존 관용구·네이밍 보존. 내가 만든 미사용물만 정리.
4. **목표 기반 실행 (Goal-Driven Execution)** — "성공 기준을 정의하고, 검증될 때까지 반복하라."
   - 작업을 **검증 가능한 성공 기준**으로 바꾸고, 테스트·백테스트·재현으로 확인한 뒤 '완료'라 말한다.
   - 실패·미검증은 정직하게 보고. 예: 백테스트는 단위·정합 버그가 흔하니(ns/ms, 유니버스 소스,
     TWAP 근사) **결과가 이상하면 먼저 계측·의심하고** 수치로 확인한 뒤 결론낸다.
   - **백테스트 결과는 반드시 `backtest-reviewer` 서브에이전트로 한 번 더 검증한다.** 어떤 백테스트든
     (전략 발굴 루틴·수동 스윕·라이브 반영 검토 등) 결과가 나오면, 그 결과를 신뢰·반영·보고하기 전에
     `backtest-reviewer`(정합·단위·룩어헤드·OOS·표본·유니버스편중·수수료·데이터소스·외부주장 재현
     감사)를 돌리고 그 리뷰(VALID/SUSPECT/INVALID)를 세션에 공유한다. INVALID/SUSPECT면 라이브에
     반영하지 않고 재검증한다.

성공의 지표: 불필요한 diff·재작성이 줄고, 구현 전에 확인 질문이 나오고, '완료'가 검증을 동반한다.
